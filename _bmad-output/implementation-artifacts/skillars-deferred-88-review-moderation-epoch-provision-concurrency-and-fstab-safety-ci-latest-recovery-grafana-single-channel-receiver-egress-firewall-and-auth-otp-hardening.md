# Story skillars-deferred-88: review-moderation stale-verdict epoch guard, `SoftDeleteIT` race-test hardening, `provision.sh` concurrency/fstab/staging safety, CI `:latest` recovery, Grafana single-channel receiver rendering, an observability egress firewall, and Auth/Registration OTP hardening

Status: done

> **Senior-dev review applied 2026-08-31 (v0.2, see Change Log).** All 8 HIGH/MEDIUM findings and 7 LOW nits from `story-review.md` were verified against source and held — **no false positives**. AC1 now increments the epoch under a lock (concurrent-edit case, M1); AC2's mutation check retargeted to `entityManager.refresh`'s lock mode (H1); AC9 reduced to the fail-fast validator only, `matchIfMissing`/defaults left untouched (M4); AC10 uses `saveAndFlush` in a `REQUIRES_NEW` boundary because the constraint violation surfaces at flush/commit, not `save()` (H3); AC11 reframed around admin-lock / GDPR-erasure (not brute-force lockout — `LoginAttemptsService` never persists a lock, H2); AC5/AC6/AC7 tightened (L5, M2/M3, M5); line-number citations re-anchored against `provision.sh` at 484 lines (L7).

## Story

As the platform operator and as an engineer maintaining the review, deployment and registration paths,
I want (1) `ReviewModerationService` to carry a per-review **moderation epoch** on `ReviewSubmittedEvent` so a slow, in-flight Gemini verdict for a *superseded* review edit can no longer land on top of a fresher edit that shares the same `PENDING` status — the current `PENDING`-only guard has no way to tell two deliveries apart, and the only thing making it unreachable today is the 365-day re-edit rule, which a GDPR/admin re-publish path or a future product change would remove; (2) `SoftDeleteIT.concurrentDoubleSoftDelete_exactlyOneSucceeds_oneConflicts` to actually prove the row lock serialises the read-check-write section rather than passing by HTTP-request serialisation, and to drop its tautological `SELECT COUNT(*) … WHERE id = ? AND deleted_at IS NOT NULL` assertion; (3) `provision.sh` to be safe under a second concurrent run (`flock`), to back up `/etc/fstab` before it edits it and to stop its stale-line purge from deleting an operator's *commented-out* alternate entry, to refuse to stage the pre-Volume tree when the root disk cannot hold a second copy (`df` guard) and to refuse to guess the device on a multi-Volume host with no `HETZNER_VOLUME_ID`; (4) `ci.yml` to have a manual `workflow_dispatch` path that force-advances `:latest` after a `master` history rewrite freezes the ancestor check, and to recognise GHCR's `denied` / `name unknown` first-publish responses as "`:latest` absent"; (5) `provision.sh` to render the Grafana `notify-ops` contact point from only the alert channels that are actually configured, so a single-channel deployment stops provisioning a second empty receiver that silently drops every alert routed to it; (6) the Prometheus / Loki / Tempo / Redis / node-exporter containers to lose their unrestricted outbound internet access (none of them need it); and (7) the Auth/Registration paths hardened — `app.ses.enabled` validated at startup so a non-`true`/`false` value fails fast with a clear message rather than the current opaque `NoSuchBeanDefinitionException` (no `SesEmailService` implementation matches), a DB-level partial unique index enforcing the "one live phone OTP per user" invariant the three registration services already maintain in code, and `verifyEmail` / `verifyPhone` refusing to advance a `locked` `User` (defense-in-depth against an admin-locked or GDPR-erased half-registered account still holding a valid pre-lock token),
so that a review verdict always reflects the content it was computed against, a lock regression in the messaging soft-delete path is caught by a test that fails without the lock, a partially-completed or double-invoked provision no longer corrupts `/etc/fstab` or strands data, `:latest` has a defined recovery path, a one-channel alert setup is not silently muted, a compromised observability container cannot exfiltrate or call home, and an admin-locked / GDPR-erased half-registered account or a mistyped `app.ses.enabled` value cannot slip through.

## Story creation context

Per the standing `deferred-work.md` re-mining priority order (`[[project_skillars_release_workflow]]`): **Messaging/Admin/Reviews/Disputes is mined out of decision-light bundleable work — this is the fourth consecutive confirmation.** `skillars-deferred-82` and `skillars-deferred-83` both re-mined it and came up dry; this pass re-read every open bullet touching `platform.messaging` / `platform.admin` / `platform.reviews` / `DisputeService` and found only two genuinely-actionable items, both small:

- **`## Deferred from: code review of skillars-deferred-14-moderation-listener-batch-overlap-integrity` (2026-08-05), D3** — `ReviewModerationService`'s `PENDING`-only guard cannot distinguish a stale in-flight Gemini verdict for a superseded edit from a fresh one. The 2026-08-05 audit note on the bullet says the scenario "is NOT reachable today" behind `ReviewSubmissionService.updateReview`'s 365-day re-edit rule, but explicitly says *keep the item*: "the **design** limitation is real and would become reachable the moment the 365-day edit rule is relaxed or an admin/GDPR path republishes the event, and the guard carries no version or nonce to survive that." **Project-owner decision 2026-08-31: close it now defensively** — add a monotonic epoch to `ReviewSubmittedEvent` + the `reviews.coach_reviews` row and check it under the existing lock. → **AC1**.
- **`## Deferred from: code review of skillars-deferred-16-messaging-moderation-recovery-identity-safety` (2026-08-05), D6** — `SoftDeleteIT.concurrentDoubleSoftDelete_exactlyOneSucceeds_oneConflicts` synchronises the *start* of two HTTP round-trips rather than the read-check-write critical section, so it can pass by request serialisation rather than by the lock; its `SELECT COUNT(*) … WHERE id = ? AND deleted_at IS NOT NULL` assertion is additionally tautological over a primary key. Same class of finding the `skillars-deferred-13`, `-15` and `skillars-uat-3` D11 reviews all raised. → **AC2**. Precedent for the fix exists: `DrillUploadServiceConcurrencyIT` (`skillars-deferred-83` AC2) proves lock causality by holding the row lock in an external transaction and asserting the second caller waits.

Everything else in the Messaging/Admin/Reviews/Disputes surface is `DECIDED` (leave as-is), `DISMISSED`, unreachable on the single-instance Docker Compose deployment (`skillars-deferred-16` D4 rolling-deploy, D3 orphaned-profile), an accepted MVP performance tradeoff (`skillars-8-1` D2 N+1 in `getConversations`), an accepted architectural note (`skillars-8-4` W5, `skillars-8-3` W1), or a settled product decision (`skillars-deferred-63` two-sided disputes — "keep first-raiser-wins as final"). Per this project's documented dry-ledger fallback and the explicit "no small stories" instruction, priority advances to **Deploy/Infra** and then **Auth/Registration**, and the project owner chose to bundle broadly:

- **`## Deferred from: code review of skillars-deferred-87-…` (2026-08-31)** — the code review of the story that shipped one release ago left a fresh cluster of concrete `provision.sh` / `ci.yml` residuals:
  - **No `flock` anywhere in `provision.sh`** — two overlapping runs can `rm -rf "${STAGING}"` mid-`rsync` or double-`mount`. → **AC3**.
  - **`provision.sh` edits `/etc/fstab` in place with `sed -i` and no `.bak`** — a boot-critical file modified with no recovery artifact. And the **`sed -i` stale-fstab purge also matches a commented-out `# … /opt/skillars/data …` line** when an active non-canonical line for the same mount point also exists. → **AC4** (two bullets).
  - **Staging `rsync` duplicates the pre-Volume tree on the same root filesystem with no free-space pre-check** — ENOSPC aborts before `mount` and loops on re-run. And a **multi-Volume host without `HETZNER_VOLUME_ID` binds to the lexically-first `scsi-0HC_Volume_*` symlink and would `mkfs.ext4` it** if unformatted. → **AC5** (two bullets).
  - **CI `:latest` freezes permanently after a `master` history rewrite** — the ancestor check returns 1/128 forever with no manual-override path; "a `workflow_dispatch` 'force-publish :latest' escape hatch would be a separate follow-up." And **first-ever `:latest` publish where GHCR returns `denied` / `name unknown`** instead of `not found` / `MANIFEST_UNKNOWN` falls through to the generic "read failed → SHA-only" branch and `:latest` is not created until a later run. → **AC6** (two bullets).
- **`## Deferred from: code review of deploy-3-3-external-uptime-monitoring-alert-rules` (2026-06-05)** — the `skillars-deferred-85` AC6 residual: *"a single-channel deployment still provisions one empty notify-ops receiver that silently no-ops — the fuller fix is conditional receiver templating in `grafana-alerts.yml`, not done here."* `grafana-alerts.yml` hard-codes both an `email` and a `slack` receiver under the `notify-ops` contact point and Grafana file provisioning has no conditionals. **Project-owner decision 2026-08-31: fold it in — `provision.sh` renders the receiver list.** → **AC7**.
- **`## Deferred from: code review of deploy-1-5-first-time-setup-documentation` (2026-06-04)** — *"No outbound firewall rules — observability containers (Prometheus, Loki, Tempo, Redis) have unrestricted internet egress; security hardening enhancement."* `skillars-deferred-85` deferred the egress firewall "to its own design story"; **project-owner decision 2026-08-31: fold it in here.** → **AC8**.
- **`## Deferred from: code review of skillars-1-3-coach-account-registration-email-verification` (2026-06-11), W3 / Group A D5 / Group D D3** — three Auth/Registration hardening items the project owner selected (2026-08-31):
  - **W3** — `SesConfig`'s `@ConditionalOnProperty(name = "app.ses.enabled", havingValue = "true", matchIfMissing = false)` requires the literal string `true`; `SesProperties.enabled` defaults to `true`. An unrecognised value (`enabled: yes`, `True `, a stray space) silently leaves `SesV2Client` unwired at startup while `SesProperties.isEnabled()` still reports `true`. → **AC9**.
  - **Group A D5** — `phone_otp_tokens` has no partial unique index on active OTPs. All three registration services (`CoachRegistrationService:138`, `PlayerRegistrationService:153`, `ParentRegistrationService:142`) already call `otpTokenRepository.deleteByUserIdAndUsedFalse(...)` immediately before inserting a new token, so the invariant is maintained in code — but nothing enforces it at the DB, and two concurrent OTP requests for the same user can each delete-then-insert and both commit. → **AC10** (DB backstop + concurrent-request conflict handling).
  - **Group D D3** — a `SUSPENDED` / locked user in the `EMAIL_VERIFIED` state can still complete phone OTP. At review time "no suspension code exists yet"; `User.locked` now exists and `LoginAttemptsService` sets it. `verifyEmail` / `verifyPhone` in all three registration services check `verificationStatus` but not `user.isLocked()`. → **AC11**.

**Twelve ACs across the reviews module, one messaging IT, `provision.sh`, `ci.yml`, `docker-compose.yml`, `grafana-alerts.yml`, the SES config, three registration services and two Flyway migrations — comfortably past this project's "no small stories" bar. Two migrations (`V120`, `V121`), both small-table and additive; one new Spring context is *not* expected (all new tests reuse existing IT base classes — confirm during dev).**

## Acceptance Criteria

### AC1 — `ReviewModerationService` discards a verdict computed for a superseded review edit, via a monotonic moderation epoch.

**Problem** (verified against source at `5efc0f0`): `ReviewModerationService.handleReviewSubmitted` (`src/main/java/com/softropic/skillars/platform/reviews/service/ReviewModerationService.java`) runs the Gemini call outside any transaction, then in a `REQUIRES_NEW` transaction does `reviewRepository.findByIdForUpdate(reviewId)` and writes the verdict **only if `review.getModerationStatus() == PENDING`**. `ReviewSubmissionService.submitReview` (`:63` / `:72-73`) and `.updateReview` (`:98` / `:103-104`) are the only publishers of `ReviewSubmittedEvent` (`src/main/java/com/softropic/skillars/platform/reviews/contract/ReviewSubmittedEvent.java` — a 5-field record: `reviewId, coachId, authorId, rating, body`), and both set `PENDING` immediately before publishing. If a review is edited again while the previous edit's Gemini call is still in flight, both deliveries race the same `PENDING` status with nothing to tell them apart — the slower Gemini call (possibly evaluating stale, already-overwritten body text) can write its verdict, and the fresher delivery then sees a non-`PENDING` status and discards *itself*. Unreachable today only because `updateReview:82-86` rejects any edit within 365 days of the last one; a GDPR/admin re-publish path or a relaxed edit rule opens it.

**Fix:**

- **`V120__coach_reviews_moderation_epoch.sql`** — `ALTER TABLE reviews.coach_reviews ADD COLUMN moderation_epoch BIGINT NOT NULL DEFAULT 0;`. Small table, additive, `DEFAULT 0` backfills every existing row in one pass — no `NOT VALID`/`VALIDATE` split needed at this size (note the `skillars-deferred-84` code-review deferral about online-safe migration convention; this column is `DEFAULT`-backfilled and takes only a brief `ACCESS EXCLUSIVE` on a tiny table, so it is in the same accepted class as `V117`/`V118`). No index — the column is only ever read via the already-locked `findByIdForUpdate` row.
- **`CoachReview` entity** (`src/main/java/com/softropic/skillars/platform/reviews/repo/CoachReview.java`) — add `@Column(name = "moderation_epoch", nullable = false) private long moderationEpoch = 0L;` (primitive `long`, mirrors the entity's existing plain-field style; `@Getter`/`@Setter` are class-level via Lombok).
- **`ReviewSubmissionService`:**
  - `submitReview` — a newly-created review keeps `moderationEpoch = 0` (the default). Publish `new ReviewSubmittedEvent(reviewId, coachId, authorId, rating, body, review.getModerationEpoch())` — i.e. `0`.
  - `updateReview` — **the increment must be serialised, not a read-modify-write on an unlocked row** (review finding M1). `updateReview` currently loads via `coachReviewRepository.findByReviewIdAndAuthorId(reviewId, authorId)` (no lock), so two near-simultaneous edits would both read epoch *N*, both write *N+1*, both publish *N+1*, and **both** stale Gemini verdicts would then pass the guard — and a relaxed 365-day rule (the exact scenario this AC future-proofs) is precisely what makes concurrent edits reachable. Do **one** of:
    - **(preferred)** after the existing author-match load and the 365-day / moderation-status pre-checks, take a lock: `CoachReview locked = coachReviewRepository.findByIdForUpdate(reviewId).orElseThrow(...)` (mirrors `submitCoachResponse`, which already does exactly this), then do every mutation (`setRating`/`setBody`/`setModerationStatus(PENDING)`/`setLastModifiedAt`/`setCoachResponseBody(null)`/`setCoachResponseAt(null)`/**`setModerationEpoch(locked.getModerationEpoch() + 1)`**) on `locked`, `save(locked)`, and publish from `locked`; **or**
    - a `@Modifying` atomic bump — `@Query("update CoachReview r set r.moderationEpoch = r.moderationEpoch + 1 where r.reviewId = :id")` — executed before re-reading the row for the event. The locked-read approach is cleaner because the same transaction already writes the row.
  - Both publish sites must pass the epoch read **after** the increment/`setModerationStatus` and from the same (locked, for `updateReview`) `review` instance that is saved in that transaction.
  - Keep the existing `findByReviewIdAndAuthorId` load for the author-match / 365-day / status pre-checks — the lock is taken *after* those pass, so an unauthorised caller still gets `AUTHOR_MISMATCH` without acquiring a row lock (same order-of-operations rationale as `MessagingService.softDeleteMessage`).
- **`ReviewSubmittedEvent`** — add `long moderationEpoch` as the 6th record component. Grep for every constructor call (`grep -rn "new ReviewSubmittedEvent(" src`) — the two production sites above plus any test builders — and update each. Keep the component name `moderationEpoch`.
- **`ReviewModerationService.handleReviewSubmitted`** — inside the `findByIdForUpdate(...).ifPresentOrElse(review -> { … })` block, **before** the existing `current != PENDING` check, add:
  ```java
  if (review.getModerationEpoch() != event.moderationEpoch()) {
      log.warn("ReviewModerationService: review {} epoch moved {} -> {} since this verdict was "
              + "requested — discarding stale moderation verdict {}",
          reviewId, event.moderationEpoch(), review.getModerationEpoch(), finalStatus);
      return;
  }
  ```
  The epoch check is first because it is the more specific signal: a stale-epoch delivery must be dropped even if the row is still `PENDING` (the fresher edit re-set it to `PENDING` with a higher epoch). The existing `PENDING` guard stays exactly as it is for the admin-decision / flag-threshold / duplicate-delivery cases its own comment enumerates. Update that comment to name the new epoch guard as the "superseded edit" case it previously could not cover (this is the exact gap `skillars-deferred-14` D3 recorded).
- **Do not** change `CoachRatingService.recompute` semantics, the `HeldReason` mapping, the `REQUIRES_NEW`/`AFTER_COMMIT` structure, or the outer `catch` that swallows to prevent a 500.

**Tests:**
- New `ReviewModerationServiceTest` case (or extend the existing one if present — `grep -rn "ReviewModerationServiceTest\|ReviewModerationIT" src/test`): seed a `PENDING` review at `moderationEpoch = 1`, deliver a `ReviewSubmittedEvent` carrying `moderationEpoch = 0` (the superseded edit's epoch), assert the row is **unchanged** (`moderationStatus` still `PENDING`, no `recompute` call) and the "discarding stale moderation verdict" warning fired. Mutation check: remove the epoch guard → the stale verdict overwrites the row → test fails.
- Extend the existing "fresh verdict lands on PENDING" case to seed `moderationEpoch = 0` on both the row and the event so it still passes (proves the guard is transparent to the normal path).
- **Concurrent-edit test** (`ReviewSubmissionServiceTest` or an IT): two `updateReview` calls racing on one review (barrier-synchronised, or a second connection holding the row lock) end with `moderationEpoch = N+1` (not `N+2`) — proves the increment is serialised, not a lost-update. Mutation check: revert the locked-read to the plain `findByReviewIdAndAuthorId` mutate-in-place → the race can produce two events at the same epoch → test fails.
- If a `ReviewModerationIT` exists that drives the real `AFTER_COMMIT` listener, add an epoch-mismatch case there too; otherwise a service-level test is sufficient (mirrors how `skillars-deferred-16` AC1 was pinned).
- `grep -rn "new ReviewSubmittedEvent(" src/test` — every test builder compiles with the new arg.

### AC2 — `SoftDeleteIT.concurrentDoubleSoftDelete…` proves the lock, and drops the tautological assertion.

**Problem** (`src/test/java/com/softropic/skillars/platform/messaging/api/SoftDeleteIT.java:246`, `concurrentDoubleSoftDelete_exactlyOneSucceeds_oneConflicts`): the test starts two `DELETE` HTTP calls on one `startLatch`, then asserts `successCount == 1` / `conflictCount == 1`. The latch releases both threads at *request start* (TCP, filter chain, controller dispatch) — not at `MessagingService.softDeleteMessage`'s locked-read → `deletedAt` check → write critical section (`src/main/java/com/softropic/skillars/platform/messaging/service/MessagingService.java:296-339`), so the test can pass by request serialisation. The trailing `SELECT COUNT(*) FROM messaging.messages WHERE id = ? AND deleted_at IS NOT NULL` → `isEqualTo(1)` is tautological: over a primary key it is 0 or 1, and given `successCount == 1` it is always 1.

**Note the row is locked in TWO places** (review finding H1) — `Message message = messageRepository.findByIdForUpdate(messageId)` (`:318`) **and** `entityManager.refresh(message, LockModeType.PESSIMISTIC_WRITE)` (`:325`, whose own comment says the `refresh` is "required, not defensive" because `findByIdForUpdate` returns the already-managed instance without re-reading). Reverting only `findByIdForUpdate` → `findById` leaves the `refresh`'s `SELECT … FOR UPDATE` in place, so the row still locks and any test would still pass. The `refresh`'s lock mode is the real causal lever.

**Fix** — mirror `DrillUploadServiceConcurrencyIT` (`skillars-deferred-83` AC2)'s external-lock technique:

- Rewrite the test so the race is exercised deterministically: open a **separate transaction** (a `TransactionTemplate` / `@Autowired PlatformTransactionManager`, or the existing helper if `SoftDeleteIT`'s base class has one) that does `SELECT … FOR UPDATE` on the target `messaging.messages` row and **holds** it; then fire a single `DELETE` HTTP call on a background thread; assert it is still blocked (has not returned) after a short wait; release the external lock; assert the call then completes `204`. Then fire a **second** `DELETE` and assert it returns `409` with body error key `messaging.alreadyDeleted` (the losing-caller outcome the lock+guard produce). This proves the request genuinely waits on the row lock.
- Keep a lighter version of the original two-thread race (both on a barrier) as a second assertion if useful, but it is no longer the primary guard.
- **Replace** the tautological `SELECT COUNT(*) … deleted_at IS NOT NULL` assertion with meaningful ones: (a) the losing caller's response carries `messaging.alreadyDeleted`; (b) exactly one row now has `deleted_at IS NOT NULL` **and** `deleted_at` is stable across a re-read (i.e. the second caller did not overwrite it) — assert on the concrete `deleted_at` value, or assert the winning caller's `deleted_at` equals the row's current `deleted_at`.
- Do **not** add `@Version` to `Message` (the code comment at `MessagingService.java:315-317` and `skillars-deferred-16`'s Dev Notes both forbid it — `ModerationResultApplier` / `AdminMessageService` / `MessageModerationSweeper` all `save()` this row).
- Mutation check to record in the Dev Agent Record (**H1 — the effective mutation is on the `refresh`, not the repository method**): change `entityManager.refresh(message, LockModeType.PESSIMISTIC_WRITE)` at `MessagingService.java:325` to `LockModeType.NONE` (or delete the `refresh` call entirely) → no `SELECT … FOR UPDATE` is issued for the delete path → the "still blocked while the external lock is held" assertion fails → test fails. Reverting `findByIdForUpdate` → `findById` alone is a **no-op for causality** (the `refresh` still locks) and must not be presented as the mutation. If dev finds `findByIdForUpdate` is genuinely redundant once the `refresh` locks, that is a separate cleanup — out of this AC's scope; the test pins the `refresh`.

**Tests:** the rewritten `SoftDeleteIT` case itself. Run `-Dtest=SoftDeleteIT` green; `grep` confirms no new `*IT` convention violation (it is already an `*IT`).

### AC3 — `provision.sh` is safe under a concurrent second run (`flock`).

**`deploy/provision.sh`** — the script has no locking; section 7 (Volume resolve/stage/mount/fstab/migrate) and `settle_pre_volume_migration` can be run twice concurrently (two operators, or manual + automation), which can `rm -rf "${STAGING}"` mid-`rsync` or double-`mount`/`mkfs.ext4` the same device.

- At the **top of the script** (right after the `set -euo pipefail` / helper-function block, before section 1), acquire an exclusive `flock` on a fixed path (`/var/lock/skillars-provision.lock` — create it with `: >` / `install -m 0644` if absent). Use the self-re-exec idiom:
  ```sh
  LOCK_FILE="/var/lock/skillars-provision.lock"
  if [ "${_PROVISION_LOCKED:-}" != "1" ]; then
    exec env _PROVISION_LOCKED=1 flock --exclusive --nonblock "${LOCK_FILE}" "$0" "$@"
  fi
  ```
  **`$0` re-exec only works when the script is invoked from a file** (`bash /opt/skillars/deploy/provision.sh`, which is how `first-time-setup.md` invokes it) — **not** under `curl … | bash`, where `$0` is `bash`/`-bash` with no path (review finding L3). Do not claim otherwise. Every documented invocation path is file-based, so this is acceptable; add a one-line comment stating the limitation.
  `--nonblock` so a second invocation **fails fast** with a clear message ("another provision.sh is already running (holds ${LOCK_FILE}); refusing to run concurrently") rather than hanging — `flock`'s own non-zero exit on a held lock, caught and rephrased. `flock` ships with `util-linux` and is always present on the Ubuntu CX32 base — no new package (confirm `command -v flock` early and `err`+`exit` with a clear message if somehow absent).
- The lock must cover the **whole** script (idempotent re-runs included) so a scheduled re-provision cannot overlap a manual one at any section.
- Document at the acquisition point: this is a coarse whole-script lock, deliberately not per-section; concurrent `provision.sh` is not a supported scenario and the lock exists to make that explicit rather than to enable parallelism.

**Verification:** `bash -n`; `shellcheck` no-new-findings vs. the saved baseline; Dev Agent Record hand-trace: (a) first run acquires the lock, re-execs once, proceeds; (b) a second run while the first holds the lock → `flock --nonblock` exits non-zero → the wrapper prints the refusal message and exits non-zero, first run unaffected; (c) `_PROVISION_LOCKED=1` already set (the re-exec'd child) → skips re-acquisition, runs the body.

### AC4 — `provision.sh` backs up `/etc/fstab` before editing it, and its stale-line purge ignores commented-out lines.

**`deploy/provision.sh`** section 7 fstab-maintenance block (`:333-350`):

- **Immediately before the `sed -i` purge at `:341` — i.e. INSIDE the `if grep … ; then` block, only when a stale line is actually about to be deleted** (review finding L4): `cp -p /etc/fstab "/etc/fstab.bak.$(date +%Y%m%d%H%M%S)"` (timestamped so repeated runs do not clobber the first backup), and `log` the backup path. `date` is available; no `set -e` hazard (`cp` of an existing file). Placing it inside the `if` (not before it) means an idempotent steady-state re-run — the common case — does **not** drop a fresh `/etc/fstab.bak.<ts>` every time; a backup is written only on the run that mutates the file. Backup retention/pruning is out of scope — a bare timestamped copy is the ask.
- **The purge match must exclude commented-out lines.** Current: `grep -vFx "${FSTAB_ENTRY}" /etc/fstab | grep -qE "[[:space:]]${MOUNT_POINT}[[:space:]]"` then `sed -i "\#[[:space:]]${MOUNT_POINT}[[:space:]]#d" /etc/fstab`. Both the `grep -qE` guard and the `sed` address must additionally require the line **not** start with optional whitespace then `#`:
  - guard: `grep -vFx "${FSTAB_ENTRY}" /etc/fstab | grep -E "[[:space:]]${MOUNT_POINT}[[:space:]]" | grep -qvE '^[[:space:]]*#'`
  - `sed`: switch to an address that anchors on a non-comment device field, e.g. `sed -i -E "\#^[[:space:]]*[^#[:space:]][^[:space:]]*[[:space:]]+${MOUNT_POINT}[[:space:]]#d" /etc/fstab` (device field is a real token, not `#…`). Pick one consistent regex and use it in both the guard and the delete so they cannot diverge; document the intent ("delete only an **active** non-canonical line for ${MOUNT_POINT}; never an operator's commented-out alternate").
- The existing "add if not `grep -qF "${FSTAB_ENTRY}"`" block and the trailing-newline guard (`:348`) stay unchanged.

**Verification:** `bash -n`; `shellcheck` no-new-findings; Dev Agent Record hand-trace: (a) `/etc/fstab` with an active `/dev/sdb /opt/skillars/data …` line → backup written (inside the `if`), line purged, by-id entry added; (b) `/etc/fstab` with **only** a commented `# /dev/sdb /opt/skillars/data …` line and the canonical by-id line already present → guard's `grep -qvE '^[[:space:]]*#'` fails → **no backup, no purge, no spurious add**; (c) `/etc/fstab` with both an active non-canonical line **and** a commented alternate → backup written, only the active line deleted, the comment preserved; (d) no `/opt/skillars/data` line at all → guard fails → **no backup written**, nothing purged, by-id entry appended by the existing add-block.

### AC5 — `provision.sh` refuses to stage without room, and refuses to guess the device on a multi-Volume host.

**`deploy/provision.sh`** section 7:

- **Free-space guard before the staging `rsync`** (the stage-before-mount block ≈ `:315-318` — `if pre_volume_payload_present "${MOUNT_POINT}"; then` … `mkdir -p "${STAGING}"` … `rsync -aHAX --numeric-ids "${MOUNT_POINT}/" "${STAGING}/"`): before `mkdir -p "${STAGING}"`, compute the size of the pre-Volume payload (`du -sk "${MOUNT_POINT}"` → KiB) and the free space on the filesystem that will hold `${STAGING}` (`df -Pk "${DEPLOY_ROOT}" | tail -1 | awk '{print $4}'` — `df -Pk` is portable on Ubuntu; avoid `--output=avail` which is GNU-only if the base ever changes). If `payload_kb * 2 > avail_kb` (×2 headroom — the copy plus rsync temp files), **`err` and `exit 1`** with a message naming both numbers and the fix ("free space on the root disk or attach the Volume before the tree grows"). This runs only when the migration is armed (`pre_volume_payload_present` true), so a from-scratch host is unaffected.
  - Both captures `|| true`-guarded per the `set -euo pipefail` convention (`skillars-deferred-85` AC3/AC6 precedent); an empty/failed `df` read → skip the guard with a `log` line rather than abort (fail-open on the *guard*, since the real failure mode — ENOSPC during `rsync` — still surfaces).
- **Multi-Volume device resolution** (`:194-218`, the `/dev/disk/by-id/scsi-0HC_Volume_*` resolution added by `skillars-deferred-87` AC4 + its code review): count the present `scsi-0HC_Volume_*` symlinks once. **`err` and `exit 1`** in either of these cases (review finding L5 — the second case is the more dangerous one, an operator who *believes* they pinned the device):
  - **more than one** `scsi-0HC_Volume_*` symlink exists **and** `HETZNER_VOLUME_ID` is unset/empty — "N Hetzner Volumes are attached and HETZNER_VOLUME_ID is not set — refusing to guess; export HETZNER_VOLUME_ID=<id> (the digits after `scsi-0HC_Volume_`) and re-run."
  - **more than one** symlink exists **and** `HETZNER_VOLUME_ID` is set **but `/dev/disk/by-id/scsi-0HC_Volume_${HETZNER_VOLUME_ID}` does not exist** (typo / stale id). Today (`:198`) this only `err`-warns and then falls through to "first matching symlink" → `readlink -f` → and `mkfs.ext4` on that Volume if it is unformatted. Promote it to a hard `exit 1` when `>1` Volume is attached: "HETZNER_VOLUME_ID=<id> does not resolve to an attached Volume and N Volumes are present — refusing to fall back to a guess." (When exactly one Volume is attached, keep the current warn-then-fall-back — a single-Volume host is unambiguous.)
  The existing single-symlink path and the `HETZNER_VOLUME_ID`-set-and-resolvable exact-match path are unchanged. The `/dev/sdb` fallback still applies only when **zero** `by-id` symlinks exist.

**Verification:** `bash -n`; `shellcheck` no-new-findings; Dev Agent Record hand-trace: (a) armed migration, payload 200 MB, root free 5 GB → guard passes, stages; (b) armed, payload 40 GB, root free 30 GB → `err` + `exit 1`, no `mkdir`/`rsync`; (c) `df` read returns empty → guard skipped with a `log` line, stage proceeds; (d) one `by-id` symlink → resolves as today; (e) two `by-id` symlinks, `HETZNER_VOLUME_ID` unset → `err` + `exit 1`; (f) two symlinks, `HETZNER_VOLUME_ID=12345` matching one → resolves to that one (unchanged); (g) two symlinks, `HETZNER_VOLUME_ID=99999` matching **none** → `err` + `exit 1` (no fall-back guess); (h) **one** symlink, `HETZNER_VOLUME_ID=99999` matching none → existing warn + fall back to that one symlink (unchanged, single-Volume is unambiguous).

### AC6 — CI has a manual `:latest` recovery path, and recognises GHCR's first-publish `denied`/`name unknown`.

**`.github/workflows/ci.yml`** — the `build-and-push` job's "Decide image tags (:latest ordering guard)" step (added by `skillars-deferred-87` AC6):

- **`workflow_dispatch` force-publish.** Add `workflow_dispatch:` to the workflow's `on:` (alongside the existing `push: { branches: [master] }`) with a boolean input `force_publish_latest` (default `false`). In the guard step, **before** the `imagetools`/`merge-base` logic, if `github.event_name == 'workflow_dispatch' && inputs.force_publish_latest == 'true'`:
  - **Require `master` first (review finding M3).** If `github.ref != 'refs/heads/master'` → `echo "::error::force_publish_latest is only permitted on master (got ${{ github.ref }})"` and **exit the step non-zero** (or refuse to append `latest`) — a dispatch from a feature branch must not publish `:latest` from non-master code.
  - Otherwise set the tag list to `sha-<short>` **and** `latest` unconditionally, `echo "::notice::force_publish_latest=true on master — publishing :latest for $GITHUB_SHA without the ancestor check"`, and skip the rest of the guard.
  - This is the manual override for when a `master` history rewrite has frozen `:latest` (the ancestor check returns 1/128 forever). Document in a comment that this is the sanctioned recovery for that specific scenario.
  - **Caveat to note in a comment:** `github.event.head_commit.timestamp` is `null` on a `workflow_dispatch` payload, so if the `docker-build` action feeds `org.opencontainers.image.created` from it, that label will be empty on the recovery path. Cosmetic — the `revision` label (fed from `$GITHUB_SHA`) is what the guard reads. If the action already has a fallback, no action needed; otherwise note it and leave it.
  - `workflow_dispatch` on `ci.yml` runs against the ref it is dispatched for; the run still needs `test` to pass (`build-and-push` keeps `needs: test`). No change to `pr-build.yml`.
- **Widen the "`:latest` absent" detection — `name unknown` only, NOT `denied` (review finding M2).** In the `if [ "${inspect_rc}" -ne 0 ]` branch, the current `grep -qiE 'not ?found|MANIFEST_UNKNOWN|manifest unknown'` misses GHCR's `name unknown` phrasing for a package that does not exist yet. Extend to `grep -qiE 'not ?found|manifest ?unknown|MANIFEST_UNKNOWN|name ?unknown'`. **Do not add `denied`** — GHCR returns `denied` both for a missing package *and* for a genuine permission / org-SSO failure on an **existing** package, so moving it into the "absent → publish both tags" branch would let a transient `denied` on a real `:latest` trigger an unconditional overwrite — the exact older-overwrites-newer regression the ordering guard exists to prevent. A `denied` therefore stays in the generic "read failed → publish `sha-` only, leave `:latest` untouched" branch (the safe direction). Add a one-line comment explaining why `denied` is deliberately excluded, and that the residual "brand-new repo's first `:latest` is delayed one run if GHCR says `denied`" case self-heals once the package exists.
- The `set +e` / explicit-`$?`-capture / never-abort contract from `skillars-deferred-87` AC6 is preserved. `action.yml` untouched.

**Verification:** this workflow runs only on `push` to `master` and now `workflow_dispatch` — not exercisable from a PR. (a) `actionlint .github/workflows/ci.yml` clean (+ `pr-build.yml` clean, untouched); (b) YAML parse; (c) Dev Agent Record walk-through: `workflow_dispatch` + `force_publish_latest=true` **on master** → both tags, no `imagetools` call; `workflow_dispatch` + `force_publish_latest=true` **on a feature branch** → step errors, no publish; normal `push`, `:latest` ancestor of HEAD → both tags (unchanged); `push`, GHCR returns `name unknown` on a fresh repo → treated as absent → both tags; `push`, GHCR returns `denied` on an existing `:latest` (permission blip) → **SHA-only + notice, `:latest` untouched** (generic branch, the safe direction); `push`, `imagetools` network error on an existing `:latest` → SHA-only + notice (unchanged). Confirm on the first post-merge `master` run that the widened grep did not change behaviour for the now-existing `:latest` (it should still take the success path and read the revision label).

### AC7 — `provision.sh` renders the Grafana `notify-ops` contact point from only the configured channels.

**Problem:** `deploy/lgtm/grafana-alerts.yml`'s `contactPoints[0]` (`notify-ops`) hard-codes **both** a `notify-ops-email` receiver (`addresses: "${GF_ALERT_NOTIFY_EMAIL}"`) and a `notify-ops-slack` receiver (`url: "${GF_SLACK_WEBHOOK_URL}"`). When only one channel is configured, the other provisions with an empty target and **silently drops every alert routed to it** (Grafana routes to the whole contact point; a receiver with no address/url fails per-notification with only a Grafana-internal log). `skillars-deferred-85` AC6 made `provision.sh` **warn** about this; it did not fix it. Grafana file provisioning has no conditionals, so the fix is to generate the receiver list.

**Fix** — `provision.sh` section 8 (alert-routing, `:418-475`), **after** the existing validation/warn block and **before** Grafana would read the file:

- **Keep `grafana-alerts.yml` committed as a valid, complete default file** (option 2 — review finding M5). Do **not** delete it and generate from scratch: `provision.sh` runs before `docker compose up -d` and its section 8 only runs `if [ -f "${ENV_FILE}" ]`, so on the documented first provision (place `.env`, then re-run) — or any run without `.env` — the bind-mount source `./deploy/lgtm/grafana-alerts.yml` would be missing and Docker would silently create a **directory** at `/etc/grafana/provisioning/alerting/alerts.yml`, breaking alert provisioning entirely.
- Wrap the `contactPoints:` block (only) in a clearly delimited region:
  ```yaml
  # >>> BEGIN provision.sh-managed contactPoints (do not hand-edit; edit .env channels + re-run provision.sh) >>>
  contactPoints:
    - orgId: 1
      name: notify-ops
      receivers:
        - uid: notify-ops-email
          ...
        - uid: notify-ops-slack
          ...
  # <<< END provision.sh-managed contactPoints <<<
  ```
  The committed default keeps **both** receivers (today's behaviour) so the file is valid before `provision.sh` ever runs.
- When `.env` is present, `provision.sh` rewrites **only the region between the markers** (a timestamped `.bak` first, per AC4's spirit) with a `contactPoints:` block whose `receivers:` list contains **only**: the `email` receiver iff `GF_ALERT_NOTIFY_EMAIL` is non-empty (email-without-`GF_SMTP_ENABLED=true` still emits the receiver + keeps the existing loud warning); the `slack` receiver iff `GF_SLACK_WEBHOOK_URL` is non-empty.
- **Preserve the `${GF_ALERT_NOTIFY_EMAIL}` / `${GF_SLACK_WEBHOOK_URL}` placeholders verbatim — do NOT inline the resolved secret values** (review finding M5). Grafana expands `${...}` from its own container env at load; the on-disk file must keep holding placeholders, never the real Slack webhook / ops email. `provision.sh` decides *which receivers to emit* from `.env`, but the emitted receiver still references `${VAR}`.
- **Emit the Slack `title:` / `text:` Go-template lines from a single-quoted / quoted heredoc** (`<<'EOF'`) so the shell does not touch `{{ range .Alerts.Firing }}` / `{{ .Labels.alertname }}`. Copy those two lines byte-for-byte from the current file.
- If **both** channels are empty, the existing `skillars-deferred-85` AC6 hard-fail (`exit 1`) already fires earlier in the section — the renderer is never reached. Keep that.
- If exactly one is set, the `notify-ops` contact point has exactly one receiver and the `policies:` block (`receiver: notify-ops`, outside the managed region) is unchanged — no empty receiver, no silent drop.
- Idempotent: re-running `provision.sh` against an unchanged `.env` writes an identical region (`diff`-quiet); the `.bak` guard from AC4 applies so an unchanged region does not drop a fresh backup.
- Update `grafana-alerts.yml`'s header comment and `docs/deployment/*.md` (`grep -rn "notify-ops\|grafana-alerts" docs/`) to say the `contactPoints` region is managed by `provision.sh` from the configured channels, and that the placeholders (not secrets) stay in the file.

**Verification:** `bash -n` on `provision.sh`; `shellcheck` no-new-findings; **YAML-parse the committed default `grafana-alerts.yml` (no `provision.sh` run) — it must be valid on its own** so the first-provision / no-`.env` bind mount is a real file, not a Docker-created directory; then YAML-parse the rewritten file for each of the three reachable cases (email-only, slack-only, both) — `python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))"`; grep the rewritten file to confirm `${GF_SLACK_WEBHOOK_URL}` / `${GF_ALERT_NOTIFY_EMAIL}` placeholders are still present (no inlined secret) and the `{{ … }}` Go-template lines are intact. Dev Agent Record hand-trace of all three cases + the no-`.env` case + the re-run no-op. No live Grafana run (project convention — `skillars-deferred-85` AC6, `skillars-uat-6`).

### AC8 — Prometheus / Loki / Tempo / Redis / `node_exporter` lose outbound internet access.

**Problem:** `docker-compose.yml` puts every service on a single `skillars-internal` bridge network whose own comment says *"Do NOT set `internal: true` — app needs outbound internet for OTLP/email/Bunny.net"*. The compose service keys are `prometheus`, `loki`, `tempo`, `redis`, **`node_exporter`** (underscore — review finding L6; `node-exporter` is only the Prometheus job label, the scrape target is `node_exporter:9100`). None of them need egress; a compromised one currently has unrestricted internet access.

**Fix — compose-only, no host iptables:**

- Add a second network `skillars-observability` with `internal: true` (no gateway → no NAT → no egress).
- Move `prometheus`, `loki`, `tempo`, `redis`, `node_exporter` **off** `skillars-internal` and **onto** `skillars-observability` only — **unless** a service both needs to be reached from an egress-capable peer and initiates no outbound calls of its own (being *reached* across networks requires shared network membership). Concretely:
  - `redis` — reached by `app`. Put `redis` on `skillars-observability`; add `skillars-observability` to `app`'s network list so `app` can reach it. `redis` itself gets no egress.
  - `loki` / `tempo` — reached by `app` (log/trace push) and `grafana` (queries). Put both on `skillars-observability`; add `skillars-observability` to `app` and `grafana`. Neither needs egress.
  - `prometheus` — scrapes `app:8367` (`spring-boot-app` job) and `node_exporter:9100`, queried by `grafana`. Put `prometheus` + `node_exporter` on `skillars-observability`; `grafana` already gains that network for loki/tempo; `app` already gains it for redis. `prometheus` gets no egress. (`story-review.md` confirmed `prometheus.yml` has no other scrape jobs — no `postgres_exporter` / `cadvisor` / `traefik` targets to strand.)
  - `grafana` — needs egress for email/Slack alert delivery → stays on `skillars-internal` **and** joins `skillars-observability`.
  - `app` — stays on `skillars-internal` (egress for email/Bunny/Stripe) **and** joins `skillars-observability`.
  - `traefik`, `postgres`, backup/cron containers — unchanged on `skillars-internal`.
- Net effect: every service keeps every peer it talks to today (verify each `depends_on` / URL env — `LOKI_URL=http://loki:3100`, `MANAGEMENT_OTLP_TRACING_ENDPOINT=http://tempo:4318`, `SPRING_DATA_REDIS_HOST`, Prometheus scrape config, Grafana datasource provisioning `prometheus:9090`/`loki:3100`/`tempo:3200`), and Prometheus/Loki/Tempo/Redis/`node_exporter` can no longer reach the internet.
- **Overlays** — `docker-compose.local.yml`, `docker-compose.uat.yml` **and `docker-compose.uat-hostwinds.yml`** (review finding L6 — all three, not just the first two). `story-review.md` observed `.local`/`.uat` add only `minio`/`minio-init` on `skillars-internal` and do not redefine base service network lists, so their merge risk is low — confirm all three still `docker compose … config` cleanly, and add `skillars-observability` to any overlay that *does* redefine a moved service's `networks:`.
- Update the `skillars-internal` comment and add one on `skillars-observability` explaining the split. Update `docs/deployment/*.md` where the network topology or the "no egress firewall" gap is described (`grep -rn "skillars-internal\|egress\|outbound" docs/deployment/`).

**Verification:** `docker compose -f docker-compose.yml config` parses and shows each service on the intended networks; `docker compose -f docker-compose.yml -f docker-compose.local.yml config`, `… -f docker-compose.uat.yml config` and `… -f docker-compose.uat-hostwinds.yml config` all still parse; Dev Agent Record: a table of every service → networks before/after → every existing peer edge still satisfied (shared network) → the five restricted services (`prometheus`, `loki`, `tempo`, `redis`, `node_exporter`) have **only** `skillars-observability`. No live deploy (project convention); note in Completion Notes that a post-merge smoke check on the real host should confirm Grafana still renders Prometheus/Loki/Tempo panels and alert delivery still works.

### AC9 — `app.ses.enabled` is bound strictly; a non-boolean value fails fast.

**Problem** (premise corrected per review finding M4): the `app.ses.enabled` string gate lives on **four** artifacts, and they are already mutually consistent:

| Artifact | Gate |
|---|---|
| `SesConfig` (`SesV2Client` bean) | `havingValue="true", matchIfMissing=false` |
| `SesEmailServiceImpl` (`SesEmailService`, `!dev`) | `havingValue="true", matchIfMissing=false` |
| `DevSesEmailService` (`SesEmailService`, `dev`) | `havingValue="true", matchIfMissing=false` |
| `NoOpSesEmailService` (`SesEmailService`) | `havingValue="false", matchIfMissing=true` — matches **only when the property is absent** |

With `app.ses.enabled: yes` (or `True `, trailing space): none of the `true` gates match, and `NoOpSesEmailService`'s `matchIfMissing=true` does **not** apply because the property is *present* (just not `false`). Result: **`SesEmailService` has zero implementations**, and the three `*RegistrationEmailListener` constructor injections fail startup with `NoSuchBeanDefinitionException` — **loud, not silent**. (`SesProperties.enabled` defaulting to `true` is a latent inconsistency but not the live failure mode.) Prod runs with `SPRING_PROFILES_ACTIVE=prod` (docker-compose `app` env), not "no active profile". The value of this AC is a **clear, early message** instead of an opaque wiring failure buried in a bean-creation stack trace.

**Fix — the fail-fast validator ONLY. Do not touch `matchIfMissing` or the field default (review finding M4):**

- Add a small startup check (an `EnvironmentPostProcessor` registered in `META-INF/spring.factories`, or a `@Bean` returning a `BeanFactoryPostProcessor` / a `@PostConstruct` on a config bean that always loads — pick the lightest that runs before the SES beans are resolved) that reads the **raw** `app.ses.enabled` string via `Environment.getProperty("app.ses.enabled")` and, if it is non-null and not (trimmed, case-insensitive) exactly `true` or `false`, throws `IllegalStateException("app.ses.enabled must be 'true' or 'false' (got: '<value>')")` — aborting startup with a message that names the offending value. A `null`/unset value is allowed (the `NoOpSesEmailService` `matchIfMissing=true` fallback handles it).
- **Leave all four `@ConditionalOnProperty` gates exactly as they are** — they are already a consistent set (`true`/`false`-only for the three real beans, a `matchIfMissing` fallback only on the no-op). Changing any `matchIfMissing` or the `SesProperties.enabled` default in isolation would *re-introduce* an inconsistency (e.g. absent property → `SesV2Client` created but no `SesEmailServiceImpl`, or vice-versa). `SesProperties` stays a plain `@ConfigurationProperties` POJO; the new check is the only change.
- `SesConfig.java` / `SesProperties.java` may not even need editing — the validator can live in its own new class in the `infrastructure.ses` package. Update "Files expected to change (AC9)" accordingly.

**Tests:** a focused context test asserting: (a) `app.ses.enabled=true` → context starts, `SesV2Client` + a `SesEmailService` impl present; (b) `app.ses.enabled=false` → context starts, `NoOpSesEmailService` selected; (c) `app.ses.enabled=yes` → context **fails to start** with the `IllegalStateException` naming the value (assert on the message, not on `NoSuchBeanDefinitionException`); (d) property unset → context starts, `NoOpSesEmailService` selected (the `matchIfMissing=true` fallback). Mirror an existing conditional-bean test if one exists (`grep -rn "ConditionalOnProperty\|SesConfig\|SesEmailService" src/test`).

### AC10 — a partial unique index enforces "one live phone OTP per user", and concurrent OTP requests conflict cleanly.

**Problem:** `main.phone_otp_tokens` (`V21__skillars_security_extension.sql:23-33`) has only `idx_pot_userid` (non-unique). The three registration services already delete unused tokens before inserting (`CoachRegistrationService:138`, `PlayerRegistrationService:153`, `ParentRegistrationService:142` and `:206`), so the "one live OTP" invariant holds in code. Nothing enforces it at the DB. The genuinely reachable double-insert is two concurrent **resend**-OTP calls (each `DELETE … WHERE used = false` sees nothing under READ COMMITTED, then `INSERT`) — narrower than "any two OTP requests" because `PhoneOtpToken` / `EmailVerificationToken` both carry `@Version` and a same-token concurrent `verifyEmail` is already serialised at `emailTokenRepository.save(evt)` (optimistic lock → `security.emailTokenUsed`) before either reaches the OTP insert (review finding H3, secondary). The index is still worth adding as the enforced backstop.

**Fix:**

- **`V121__phone_otp_tokens_one_active_per_user.sql`** — `CREATE UNIQUE INDEX IF NOT EXISTS uq_pot_one_active_per_user ON main.phone_otp_tokens (user_id) WHERE used = false;`. Partial predicate is `used = false` only — **not** `expires_at > now()` (`now()` is not `IMMUTABLE` and is rejected in an index predicate). An expired-but-unused row still counts as "the one active row"; the services' delete-before-insert clears it, so this is correct. Non-`CONCURRENTLY` (Flyway runs migrations in a transaction; the table is tiny — same accepted class as every other index in this codebase's history; the `skillars-deferred-84` online-migration deferral applies and is accepted at this size).
  - Prepend a defensive dedup so `CREATE UNIQUE INDEX` cannot fail on legacy data: `DELETE FROM main.phone_otp_tokens a USING main.phone_otp_tokens b WHERE a.user_id = b.user_id AND a.used = false AND b.used = false AND a.id < b.id;` (`@Tsid` ids are time-sorted, so this keeps the newest unused row per user). Matches `skillars-deferred-57` D-item guidance about migrations having no remediation path for pre-existing violations.
- **Why "wrap the `save`" is not enough (review finding H3):** `PhoneOtpToken extends BaseEntity`, which has `@Id @Tsid` — an **application-assigned** id, so Hibernate does not need an immediate `INSERT` and defers it to flush/commit. The three registration services are class-level `@Transactional` and the OTP insert is the last write in `verifyEmail`, so:
  1. the unique-index violation throws at **transaction commit**, *after* `verifyEmail` returns — a raw 500, exactly what this AC is trying to prevent. A `try/catch` around `otpTokenRepository.save(...)` as literally written never fires.
  2. even with a forced flush, the constraint violation marks the outer transaction rollback-only, so catching + re-throwing a friendly exception still rolls back the `verificationStatus → EMAIL_VERIFIED` transition and `evt.setUsed(true)` done earlier in the same method, and the commit throws `UnexpectedRollbackException`.
- **So do the OTP delete+insert in its own transaction boundary.** Move the `deleteByUserIdAndUsedFalse(userId)` + `new PhoneOtpToken()` + `saveAndFlush(...)` (note `saveAndFlush`, not `save` — forces the `INSERT` now so the violation is synchronous) into a helper that runs in a **new** transaction — either a small `@Transactional(propagation = REQUIRES_NEW)` method on a tiny collaborator bean, or a `TransactionTemplate` with `PROPAGATION_REQUIRES_NEW` (the exact idiom `ReviewModerationService` and `BookingPaymentPersistenceService.reserveCapture` already use). Catch `DataIntegrityViolationException` **there** (the sub-transaction rolls back cleanly, the outer one is untouched) and re-throw the "an OTP is already being sent, retry" exception each `verifyEmail` already uses for a retryable failure (`OtpVerificationException` / the surrounding pattern), with a `log.warn` naming the race. The email-verification state committed by the outer transaction survives.
  - If a `REQUIRES_NEW` boundary is judged too invasive for this rare race, the acceptable fallback is: ship the index + dedup only, use `saveAndFlush` so a violation is at least a *clean* `DataIntegrityViolationException` (not a commit-time `UnexpectedRollbackException`), map it to a 409 via `ApiAdvice`, and record in Dev Notes that a concurrent-resend collision returns a generic "try again" — the retry *is* a resend, so the UX is acceptable. Dev picks; the `REQUIRES_NEW` version is preferred.
  - Per-service, not a shared base class (the three services already duplicate the OTP block).
- Optionally mirror the index onto `main.email_verification_tokens (user_id) WHERE used = false` in the same `V121` — `resendVerificationEmail` already deletes-before-insert there, so the invariant holds; symmetric defense-in-depth. Include only if no test fixture seeds two unused email tokens for one user (`grep -rn "email_verification_tokens\|EmailVerificationToken" src/test`). If it risks fixtures, scope `V121` to `phone_otp_tokens` and note email tokens as a follow-up.

**Tests:**
- `@DataJpaTest` / repository IT: a second `used = false` `PhoneOtpToken` for a `user_id` that already has one → `DataIntegrityViolationException` on flush; one where the prior row is `used = true` → succeeds.
- Service test: two concurrent resend-OTP paths for one user (second bypasses the delete via a spy / a second connection) → the friendly retry exception, **the email-verification state from the first call is still committed** (assert the row), no `UnexpectedRollbackException`.
- `-Dtest` the three `*RegistrationService*` test classes + the `PhoneOtpToken` repo test green; existing registration ITs still pass (one OTP per user per test — the index does not bite them).

### AC11 — `verifyEmail` / `verifyPhone` refuse to advance a locked `User`.

**Problem** (premise corrected per review finding H2): `User.locked` exists (`User.java:71`). It is written by **admin action** (`UserAdminService:64` `u.lock()`), **GDPR erasure** (`GdprErasureService:96`) and the **user-erased event** (`UserErasedEventListener:27`) — **not** by any brute-force / OTP-lockout path. `LoginAttemptsService`, `FraudAwareAuthenticationManager` and `AuthenticationFailureListener` throttle via in-memory Guava caches and never touch `User.locked` or the DB. `CoachRegistrationService.verifyEmail` (`:108-131`) / `.verifyPhone` (`:151-182`) and the `PlayerRegistrationService` / `ParentRegistrationService` equivalents check `verificationStatus` but never `user.isLocked()`, so a user who is **admin-locked or GDPR-erased while still holding a valid pre-lock email/OTP token** can self-advance `verificationStatus` to `EMAIL_VERIFIED` / `BASIC_VERIFIED`. Defense-in-depth, not a live brute-force hole.

**Fix:**

- In each of `verifyEmail` and `verifyPhone` across all three registration services, **after** the `User` is loaded and **before** any state mutation, add a `user.isLocked()` guard that throws the exception type that method already uses:
  - `verifyEmail` → `throw new EmailTokenException("security.accountLocked", true)` — **2-arg** `(String key, boolean terminal)`; `EmailTokenException` has no 1-arg constructor (review finding L1).
  - `verifyPhone` → `throw new OtpVerificationException("security.accountLocked")` — 1-arg is fine here.
- **Terminal vs non-terminal (review finding L2).** A `terminal = true` `EmailTokenException` (and the equivalent for OTP) permanently wedges a half-registered account that an admin locks or GDPR erases — there is no self-service resume even after an unlock. That is arguably the intended outcome for a GDPR erasure, but harsh for a mistaken admin lock. **Decision point for dev / owner:** either (a) ship it terminal and document that an admin who locks a mid-registration user must also delete the stub, or (b) make the email variant `terminal = false` so a later unlock lets the user re-request and resume. Default to **(b) non-terminal for the email path, terminal for OTP** unless the owner says otherwise — record the choice in Completion Notes.
- Add the `security.accountLocked` key wherever `security.emailTokenInvalid` / `security.otpMismatch` are surfaced (`grep -rn "security.otpMismatch\|security.emailTokenInvalid" src/frontend/src/i18n src/main/resources`). If those are backend-only error codes with a frontend fallback, follow that pattern (no new i18n key needed).
- Do **not** silently no-op; a clear error is correct. Lock-*clearing* is admin-side, out of scope.

**Tests:** for each service, `verifyEmail_lockedUser_rejected` / `verifyPhone_lockedUser_rejected` (seed `user.setLocked(true)`, assert the method throws the right exception type and the row's `verificationStatus` is **not** advanced). Mutation check: remove the guard → the locked user reaches `EMAIL_VERIFIED` / `BASIC_VERIFIED` → test fails. `-Dtest` the three registration test classes green.

### AC12 — `deferred-work.md` ledger hygiene.

Edit **`_bmad-output/implementation-artifacts/deferred-work.md`** in place (follow the file's current tail-section `[CLOSED by …]`-append style):

1. `## Deferred from: code review of skillars-deferred-14-moderation-listener-batch-overlap-integrity (2026-08-05)` — append `[CLOSED by skillars-deferred-88 AC1 — ReviewSubmittedEvent carries a monotonic moderationEpoch (V120, reviews.coach_reviews.moderation_epoch); ReviewSubmissionService.updateReview takes a findByIdForUpdate lock and bumps the epoch on that locked instance (serialised, not a lost-update read-modify-write); ReviewModerationService discards a verdict whose event epoch != the row's current epoch, checked under the existing lock before the PENDING guard. Both the sequential superseded-edit case and the concurrent-edit case are closed, regardless of the 365-day edit rule.]` to the D3 bullet.
2. `## Deferred from: code review of skillars-deferred-16-messaging-moderation-recovery-identity-safety (2026-08-05)` — append `[CLOSED by skillars-deferred-88 AC2 — SoftDeleteIT.concurrentDoubleSoftDelete… rewritten to hold the messaging.messages row lock in an external transaction and assert the DELETE request genuinely blocks until release; mutation-verified by flipping softDeleteMessage's entityManager.refresh from PESSIMISTIC_WRITE to NONE (the effective lock lever — reverting findByIdForUpdate alone is a no-op because the refresh still locks). The tautological SELECT COUNT(*) … deleted_at IS NOT NULL assertion is replaced with the losing-caller messaging.alreadyDeleted body check and a stable-deleted_at assertion.]` to the D6 bullet.
3. `## Deferred from: code review of skillars-deferred-87-… (2026-08-31)` — append `[CLOSED by skillars-deferred-88 AC3]` to the "No `flock` anywhere in `provision.sh`" bullet; `[CLOSED by skillars-deferred-88 AC4]` to the `/etc/fstab` `.bak` bullet **and** the `sed -i` commented-line bullet; `[CLOSED by skillars-deferred-88 AC5]` to the "Staging `rsync` … no free-space pre-check" bullet **and** the "Multi-Volume host without `HETZNER_VOLUME_ID`" bullet (note AC5 also hard-fails the more dangerous "id set but unresolvable + >1 Volume" case); `[PARTIALLY CLOSED by skillars-deferred-88 AC6]` to the "CI `:latest` freezes permanently after a `master` history rewrite" bullet (a `workflow_dispatch` `force_publish_latest` recovery path now exists; the automatic ancestor check still can't self-recover, by design); `[CLOSED by skillars-deferred-88 AC6 — for the `name unknown` phrasing only; `denied` is deliberately NOT reclassified as absent because GHCR also returns it for a permission failure on an existing package]` to the "First-ever `:latest` publish where GHCR returns `denied` / `name unknown`" bullet.
4. `## Deferred from: code review of deploy-3-3-external-uptime-monitoring-alert-rules (2026-06-05)` — the "Empty notification vars cause silent delivery failure" bullet already carries a `[CLOSED (both-empty case) by skillars-deferred-85 AC6 …]` tag ending "RESIDUAL, still open: a single-channel deployment still provisions one empty notify-ops receiver …". Append `[RESIDUAL CLOSED by skillars-deferred-88 AC7 — provision.sh now renders the notify-ops receiver list from only the channels configured in .env, so a single-channel deployment provisions exactly one receiver and no silent no-op.]`.
5. `## Deferred from: code review of deploy-1-5-first-time-setup-documentation (2026-06-04)` — append `[CLOSED by skillars-deferred-88 AC8 — prometheus/loki/tempo/redis/node_exporter moved onto an internal:true skillars-observability network with no gateway; egress-needing peers (app, grafana) join both networks. No host iptables.]` to the "No outbound firewall rules" bullet.
6. `## Deferred from: code review of skillars-1-3-coach-account-registration-email-verification (2026-06-11)` — append `[CLOSED by skillars-deferred-88 AC9 — a startup validator rejects any app.ses.enabled value that is not exactly true/false with a clear IllegalStateException; the four @ConditionalOnProperty gates are left as-is (already a consistent set). Corrects W3's "silently unwired" premise — the current failure mode is a loud NoSuchBeanDefinitionException for SesEmailService.]` to **W3**. `## Deferred from: code review of skillars-1-3-coach-account-registration-email-verification Group A (2026-06-11)` — append `[CLOSED by skillars-deferred-88 AC10 — V121 uq_pot_one_active_per_user partial unique index on phone_otp_tokens(user_id) WHERE used = false + defensive dedup; the OTP delete+saveAndFlush runs in a REQUIRES_NEW boundary so a concurrent-resend DataIntegrityViolationException maps to a friendly retry without rolling back the outer verifyEmail transaction (the @Tsid id defers the INSERT to commit — a plain save()-wrap would not catch it).]` to **D5**. `## Deferred from: code review of skillars-1-3-coach-account-registration-email-verification Group D (2026-06-11)` — append `[CLOSED by skillars-deferred-88 AC11 — verifyEmail/verifyPhone in all three registration services now reject a User with locked = true before any state mutation. locked is written by admin action / GDPR erasure / user-erased event (NOT any brute-force path — LoginAttemptsService throttles in-memory only); this is defense-in-depth against an admin-locked or erased half-registered account still holding a valid pre-lock token.]` to **D3**.
7. Add a new `## Deferred from: code review of skillars-deferred-88 …` section only if this story's code review surfaces new deferrals — not required at creation time.

Record the exact set of edited line numbers in the Dev Agent Record.

## Tasks / Subtasks

- [x] **Task 1: Review-moderation epoch guard (AC: #1)**
  - [x] `V120__coach_reviews_moderation_epoch.sql` — `ADD COLUMN moderation_epoch BIGINT NOT NULL DEFAULT 0`
  - [x] `CoachReview` entity: `long moderationEpoch = 0L` field
  - [x] `ReviewSubmittedEvent`: add `long moderationEpoch` (6th component); update every `new ReviewSubmittedEvent(` call site (grep src + test) — 2 prod sites (`ReviewSubmissionService:76`, `:124`) + 8 test sites in `ReviewModerationServiceTest`; no other consumers (`AdminAlertEventListener` uses `ReviewFlaggedEvent`, not this)
  - [x] `ReviewSubmissionService.submitReview` publishes epoch `0`; `.updateReview` — after the author/365-day/status pre-checks — takes `findByIdForUpdate(reviewId)` **+ `entityManager.refresh(locked, PESSIMISTIC_WRITE)`** (see Dev Agent Record — the refresh is the actual causal lever, same Hibernate identity-map gotcha `MessagingService.softDeleteMessage` documents; without it the "locked" instance is the stale pre-check one and the bump is still a lost update), does every mutation incl. `setModerationEpoch(locked.getModerationEpoch() + 1)` on the locked instance, saves + publishes from it (M1 — no unlocked read-modify-write)
  - [x] `ReviewModerationService.handleReviewSubmitted`: epoch-mismatch guard **before** the `current != PENDING` guard, with the warn log; updated the guard comment to name the "superseded edit" case
  - [x] `ReviewModerationServiceTest`: stale-epoch delivery discarded (mutation-verified); fresh-epoch delivery still lands; **concurrent-`updateReview` test** (in `ReviewSubmissionIT`, deterministic externally-held-lock design — see Dev Agent Record for why the story's "ends at N+1 not N+2" wording is inverted) mutation-verified against removing the `entityManager.refresh`
- [x] **Task 2: `SoftDeleteIT` race-test hardening (AC: #2)**
  - [x] Rewrote `concurrentDoubleSoftDelete_…` → `concurrentDoubleSoftDelete_loserRechecksUnderLockAndConflictsCleanly`: an external transaction holds `SELECT … FOR UPDATE` on the `messaging.messages` row, stages its own `deleted_at`, then commits; the concurrent `DELETE` request is parked on the lock the whole time and, on resume, re-reads `deleted_at` under the lock and loses with `409 messaging.alreadyDeleted`. (Deterministic external-holder design, not the two-thread barrier — mirrors `DrillUploadServiceConcurrencyIT` / the AC1 test.)
  - [x] Dropped the tautological `SELECT COUNT(*) … deleted_at IS NOT NULL` assertion; replaced with: losing-caller body carries `messaging.alreadyDeleted`; the final `deleted_at` equals the external transaction's value (the loser did **not** overwrite it).
  - [x] Mutation check recorded in the Dev Agent Record — **refined against observed Hibernate behaviour**: neither `findByIdForUpdate`→`findById` alone **nor** `refresh(PESSIMISTIC_WRITE)`→`refresh(NONE)` alone changes whether the request blocks (the `@Lock` JPQL query still emits its own `FOR UPDATE` for an already-managed row, and the trailing `save()` `UPDATE` blocks on the row lock regardless). Reverting **both together** keeps the stale pre-lock snapshot → the DELETE returns `204` (not `409`) and overwrites `deleted_at` → both assertions fail. **Verified**: `Tests run: 1, Failures: 1` with both reverts, `Failures: 0` with real code.
  - [x] `-Dtest=SoftDeleteIT` → `Tests run: 5, Failures: 0, Errors: 0` (individual test 52.7s; full class green earlier at 44.7s).
- [x] **Task 3: `provision.sh` `flock` (AC: #3)**
  - [x] Section "0. Concurrency guard" at the top (after the helper-function block, before section 1): whole-script exclusive `flock --exclusive --nonblock` on `/var/lock/skillars-provision.lock` via the `_PROVISION_LOCKED` re-exec idiom. **Not `exec`** — flock runs `$0` as a child while holding the lock; the parent catches flock's exit-1-on-held-lock and rephrases it (`another provision.sh is already running … refusing to run concurrently`) rather than passing through flock's terse message. `command -v flock` guard → clear `err` + `exit 1` if absent. One-line comment records the `$0` re-exec is file-invocation-only (not `curl | bash`).
  - [x] `bash -n` OK; `shellcheck` exit 0 (baseline was also clean — no new findings). Hand-trace recorded in Dev Agent Record: (a) first run acquires + re-execs + runs body; (b) second concurrent run → flock exit 1 → refusal message → exit 1, first unaffected; (c) re-exec'd child sees `_PROVISION_LOCKED=1` → skips the block → runs body. (No runtime test: `flock` is Linux-only, absent on the dev mac.)
- [x] **Task 4: `/etc/fstab` safety (AC: #4)**
  - [x] Timestamped `cp -p /etc/fstab /etc/fstab.bak.$(date +%Y%m%d%H%M%S)` **inside the `if` block, immediately before the `sed`** (L4), with a `log` line naming the backup path.
  - [x] One shared regex `_fstab_stale_re='^[[:space:]]*[^#[:space:]][^[:space:]]*[[:space:]]+${MOUNT_POINT}[[:space:]]'` used in **both** the guard `grep -qE` and the `sed` address. `sed` address delimiter is `,` (not `#` — it appears in the bracket expression; not `/` — it is in `${MOUNT_POINT}`). `sed` also matches the canonical `FSTAB_ENTRY`; the existing add-block re-adds it, so net idempotent.
  - [x] `bash -n` OK; `shellcheck` exit 0. **Functional 4-case hand-trace run** (see Dev Agent Record): (a) active non-canonical line → backup + purge + by-id added; (b) commented-only alt + canonical present → **no backup, no purge**; (c) active non-canonical + commented alt → backup, active line purged, **comment preserved**, by-id present; (d) no `${MOUNT_POINT}` line → **no backup**, by-id appended by the add-block.
- [x] **Task 5: Staging free-space + multi-Volume guards (AC: #5)**
  - [x] `du -sk "${MOUNT_POINT}"` / `df -Pk "${DEPLOY_ROOT}"` free-space guard (×2 headroom) inserted after the `pre_volume_payload_present` log and before `mkdir -p "${STAGING}"` — armed-migration only, both captures `|| true`-guarded, **fail-open** (`log` + skip) on an unreadable `du`/`df`, `err` + `exit 1` when `payload_kb*2 > avail_kb`.
  - [x] Device resolution rewritten to enumerate `scsi-0HC_Volume_*` symlinks into an array once (`_vol_count`). Hard-fail (`err` + `exit 1`): `>1` symlink + unset `HETZNER_VOLUME_ID`; **and** `>1` symlink + `HETZNER_VOLUME_ID` set-but-unresolvable (L5). Single/zero-Volume + unresolvable id keeps warn-then-fall-back; single-symlink and exact-match paths unchanged; zero symlinks → `/dev/sdb`.
  - [x] `bash -n` OK; `shellcheck` exit 0. **Functional hand-trace run** of the 7 device-resolution cases (d–j) — all match the story's expected outcomes; free-space guard (a–c) hand-traced in the Dev Agent Record.
- [x] **Task 6: CI `:latest` recovery + first-publish detection (AC: #6)**
  - [x] `workflow_dispatch:` with a `force_publish_latest` boolean input (`default: false`) added to `ci.yml`'s `on:`. Guard step gains `EVENT_NAME` / `GH_REF` / `FORCE_PUBLISH_LATEST` env; a new block **before** the `imagetools` logic: on `workflow_dispatch` + `force_publish_latest=true`, if `github.ref != refs/heads/master` → `::error::` + `exit 1` (M3), else write `sha-<short>` + `latest`, `::notice::`, `exit 0`. `write_tags()` helper factored out (used by both paths). Null-`head_commit.timestamp` caveat commented at the `org.opencontainers.image.created` label.
  - [x] Widened the `inspect_rc != 0` grep to `'not ?found|manifest ?unknown|MANIFEST_UNKNOWN|name ?unknown'` — adds `name ?unknown` only. Comment added explaining `denied` is deliberately **not** matched (GHCR returns it for a permission failure on an existing package too) and self-heals.
  - [x] `actionlint .github/workflows/ci.yml .github/workflows/pr-build.yml` → exit 0 (`pr-build.yml` untouched). `python3 -c "yaml.safe_load(...)"` → parse OK. Walk-through in Dev Agent Record: dispatch+force on master → both tags no imagetools call; dispatch+force on a feature branch → `::error::` + step fails; normal push ancestor → both tags (unchanged); push + `name unknown` → treated as absent → both tags; push + `denied` on existing `:latest` → SHA-only + notice (generic branch, safe).
- [x] **Task 7: Grafana single-channel receiver rendering (AC: #7)**
  - [x] `grafana-alerts.yml` kept as a valid complete default (both receivers); `contactPoints:` wrapped in `# >>> BEGIN provision.sh-managed contactPoints (skillars-deferred-88 AC7) >>>` / `# <<< END … <<<` markers.
  - [x] `render_grafana_contact_points()` in `provision.sh` section 8 rewrites **only** the marked region from `GF_EMAIL`/`GF_SLACK` (via `awk` splice on the marker lines); timestamped `.bak` only when the region actually changes (`cmp -s`); `${GF_ALERT_NOTIFY_EMAIL}` / `${GF_SLACK_WEBHOOK_URL}` emitted verbatim (single-quoted `echo` for email, `<<'SLACK_RECEIVER'` quoted heredoc for the slack block incl. the `{{ … }}` lines copied byte-for-byte). Marker-absent / file-absent → warn + skip (defensive). `SC2016` disabled with a comment on the literal-placeholder block; `SC2155` fixed (`local bak` split).
  - [x] **Functional test run** (extracted the function, ran against the real file): email-only → 1 email receiver (slack omitted, not empty); slack-only → 1 slack receiver, `{{ len … }}`/`{{ range … }}` intact; both → 2 receivers. All three YAML-parse; placeholder counts 1/1/2; idempotent re-run logs "no change" (no new `.bak`). Committed default parses with both receivers (no-`.env` / first-provision case). Both-empty → the existing section-8 `exit 1` fires before the renderer.
  - [x] `bash -n` OK; `shellcheck` exit 0. Section-8 header comment updated; the two single-channel warns reworded ("receiver omitted, not provisioned empty"); `docs/deployment/monitoring.md` + `docs/deployment/secrets-reference.md` updated.
- [x] **Task 8: Observability egress firewall (AC: #8)**
  - [x] `skillars-observability` network added with `internal: true`; `prometheus` / `loki` / `tempo` / `redis` / `node_exporter` (underscore) moved onto it **only**; `app` + `grafana` join both networks.
  - [x] Every peer edge re-verified under the new topology (table in Dev Agent Record): `app`→`redis`/`loki`/`tempo` (shared observability), `prometheus`→`app:8367`/`node_exporter:9100` (shared observability), `grafana`→`prometheus`/`loki`/`tempo` (shared observability), `traefik`→`app`/`grafana` (shared internal), `app`→`postgres` (shared internal). The 5 restricted services have **only** `skillars-observability`.
  - [x] `docker compose --env-file <dummy> -f docker-compose.yml config` parses; membership dumped and asserted. `-f docker-compose.local.yml` / `-f docker-compose.uat.yml` / `-f docker-compose.uat-hostwinds.yml` all `config` → exit 0. Overlays only add `networks:` to `minio`/`minio-init` (new services) — no moved base service's `networks:` is redefined, so no overlay edit needed.
  - [x] `skillars-internal` comment updated + `skillars-observability` comment added; `docs/deployment/first-time-setup.md` gains a two-network topology section; `docs/deployment/secrets-reference.md` `LOKI_URL`/`OTLP` notes updated.
- [x] **Task 9: `app.ses.enabled` fail-fast validator (AC: #9)**
  - [x] New `infrastructure.ses.SesEnabledPropertyValidator implements EnvironmentPostProcessor`, registered via a new `src/main/resources/META-INF/spring.factories`. Runs before any bean; `raw == null` → return; else reject unless `equalsIgnoreCase("true"|"false")` with `IllegalStateException("app.ses.enabled must be 'true' or 'false' (got: '<value>')")`. **Deviation from the story's "trimmed" wording, documented in Completion Notes**: the validator is *strict* (no whitespace tolerance) to exactly mirror `@ConditionalOnProperty`'s own `equalsIgnoreCase` semantics — accepting `"true "` would pass validation but still leave the beans unwired, breaking the "accepted ⇔ wires" invariant.
  - [x] **All four `@ConditionalOnProperty` gates + `SesProperties.enabled` untouched** (M4). `SesConfig.java` / `SesProperties.java` not modified.
  - [x] `SesEnabledPropertyValidatorTest` (7, pure unit): unset/`true`/`false`/`TRUE`/`True` accepted; `yes` → `IllegalStateException` with `app.ses.enabled` + `yes` in the message; `"true "` and `""` rejected. `SesConditionalWiringTest` (3, `ApplicationContextRunner`, no `@SpringBootTest`): `true` → `SesV2Client` + `SesEmailServiceImpl`; `false`/unset → `NoOpSesEmailService`, no client. (The full-boot "`app.ses.enabled=yes` → context fails" path isn't unit-testable without a `@SpringBootTest` since EPPs need `SpringApplication`; the EPP unit test proves the exception + message directly.) `Tests run: 10, Failures: 0`.
- [x] **Task 10: Phone-OTP uniqueness backstop (AC: #10)**
  - [x] `V121__phone_otp_tokens_one_active_per_user.sql` — defensive dedup `DELETE` (keep highest `@Tsid` id per user) + `CREATE UNIQUE INDEX IF NOT EXISTS uq_pot_one_active_per_user ON main.phone_otp_tokens (user_id) WHERE used = false`. Scoped to `phone_otp_tokens` only — `email_verification_tokens` left as a follow-up (avoids any risk to email-token fixtures).
  - [x] **Fallback path taken** (documented in Completion Notes — the `REQUIRES_NEW` boundary was judged not worth its cross-transaction subtleties here): the 4 OTP-insert sites (`Coach`/`Player` `verifyEmail`, `Parent` `verifyEmail` + `resendPhoneOtp`) switched `save` → `saveAndFlush` so a `uq_pot_one_active_per_user` violation is a **synchronous, clean `DataIntegrityViolationException`** at that line (not a `@Tsid`-deferred commit-time 500). `ApiAdvice` maps the constraint → **409 `security.otpResendInProgress`** (`CONSTRAINT_MAPPINGS` + `CONFLICT_CONSTRAINTS`). The exception is *not* caught in-service, so it propagates cleanly (no `UnexpectedRollbackException`); the only genuinely-racy path (`Parent.resendPhoneOtp`) has nothing else in its transaction to lose, and `verifyEmail`'s OTP insert is already serialised upstream by the `EmailVerificationToken` `@Version` lock.
  - [x] Index proof: `CoachRegistrationResourceIT.phoneOtpTokens_secondActiveRowForSameUser_rejectedByUniqueIndex` (2nd `used=false` row → `DataIntegrityViolationException`; a `used=true` row coexists). Concurrency proof: `ParentRegistrationResourceIT.resendPhoneOtp_concurrent_indexKeepsExactlyOneActiveTokenAndOuterStateIntact` (6 barrier threads → exactly one active OTP row, every loser fails with a plain `DataIntegrityViolationException`, `verification_status` unchanged).
- [x] **Task 11: Lock-aware verification (AC: #11)**
  - [x] `if (user.isLocked())` guard after the `User` load and before any mutation, in `verifyEmail` (`throw new EmailTokenException("security.accountLocked", true)` — 2-arg, L1) and `verifyPhone` (`throw new OtpVerificationException("security.accountLocked")`), in all three registration services. `resendPhoneOtp` left untouched (its OTP is inert — `verifyPhone` rejects the locked user downstream).
  - [x] **Terminal decision (L2): non-terminal email / OTP has no flag.** `EmailTokenException`'s 2nd arg is `canResend` — passed `true` so a later admin unlock lets the user re-request and resume (matches L2's default (b)). Recorded in Completion Notes.
  - [x] `security.accountLocked` added to `messages_en.properties` + `messages_de.properties` (alongside `security.emailToken*`).
  - [x] Per-service locked-user rejection tests (`verifyEmail_lockedUser_rejectedAndStatusNotAdvanced` + `verifyPhone_lockedUser_rejectedAndStatusNotAdvanced`) added to all three `*RegistrationResourceIT`s — assert 400 + `security.accountLocked` body + `verification_status` **not** advanced (which is the mutation check).
- [x] **Task 12: Ledger hygiene (AC: #12)** — see Task 12 section below (done after the code ACs).
  - [x] `[CLOSED by skillars-deferred-88 AC…]` / `[RESIDUAL CLOSED …]` / `[PARTIALLY CLOSED …]` tags appended per AC12.
  - [x] Edited `deferred-work.md` line numbers recorded in the Dev Agent Record.

## Dev Notes

### Source ledger mapping

| AC | `deferred-work.md` source |
|----|---------------------------|
| AC1 | `## Deferred from: code review of skillars-deferred-14-moderation-listener-batch-overlap-integrity (2026-08-05)` — D3 (`ReviewModerationService` `PENDING`-only guard cannot distinguish a stale in-flight Gemini verdict for a superseded edit). Project-owner decision 2026-08-31: close defensively now. |
| AC2 | `## Deferred from: code review of skillars-deferred-16-messaging-moderation-recovery-identity-safety (2026-08-05)` — D6 (`SoftDeleteIT` synchronises request start not the critical section; tautological PK assertion). |
| AC3–AC6 | `## Deferred from: code review of skillars-deferred-87-… (2026-08-31)` — `flock` (AC3); `/etc/fstab` `.bak` + `sed -i` commented-line (AC4); staging free-space + multi-Volume `HETZNER_VOLUME_ID` (AC5); CI `:latest` freeze after history rewrite + GHCR `denied`/`name unknown` first-publish (AC6). |
| AC7 | `## Deferred from: code review of deploy-3-3-external-uptime-monitoring-alert-rules (2026-06-05)` — the `skillars-deferred-85` AC6 RESIDUAL: single-channel deployment still provisions one empty notify-ops receiver. Grafana file provisioning has no conditionals → `provision.sh` renders it. |
| AC8 | `## Deferred from: code review of deploy-1-5-first-time-setup-documentation (2026-06-04)` — "No outbound firewall rules — observability containers have unrestricted internet egress." `skillars-deferred-85` deferred it to its own story; folded in here per project-owner decision. |
| AC9 | `## Deferred from: code review of skillars-1-3-coach-account-registration-email-verification (2026-06-11)` — W3 (`SesConfig` unrecognised `app.ses.enabled` value). **Review M4**: the failure today is a *loud* `NoSuchBeanDefinitionException` (no `SesEmailService` impl), not silent; the four `@ConditionalOnProperty` gates are already consistent — this AC adds only a fail-fast validator. |
| AC10 | Same review, Group A — D5 (`phone_otp_tokens` no partial unique index on active OTPs). Services already delete-before-insert; this adds the enforced backstop. **Review H3**: `@Tsid` app-assigned id defers the `INSERT` to commit, so the violation must be forced with `saveAndFlush` in a `REQUIRES_NEW` boundary or it surfaces as a commit-time 500 / poisons the outer tx. |
| AC11 | Same review, Group D — D3 (locked user in `EMAIL_VERIFIED` can complete phone OTP). **Review H2**: `User.locked` is written only by admin action / GDPR erasure / user-erased event — *not* any brute-force path. Defense-in-depth against an admin-locked or erased half-registered account. |

### Project-owner decisions folded in (2026-08-31)

- **Module priority**: Messaging/Admin/Reviews/Disputes is mined out (4th confirmation — `skillars-deferred-82`/`-83` both dry). Take the two real items (AC1, AC2) as the anchor, then bundle Deploy/Infra (AC3–AC8) and Auth/Registration (AC9–AC11). Chosen from four options presented.
- **AC1 (deferred-14 D3)**: **include it** — add the epoch/nonce now, even though the scenario is unreachable behind the 365-day edit rule, because a GDPR/admin re-publish path or a relaxed rule opens it and the event carries no version today.
- **AC7 (Grafana single-channel residual)**: **fold in** — `provision.sh` renders the receiver list from the configured channels (Grafana file provisioning has no native conditionals).
- **AC8 (egress firewall)**: **fold in** — previously carved out by `skillars-deferred-85` as "its own design story"; now in scope. Compose-only (`internal: true` network), no host iptables.
- **AC9–AC11 (Auth/Registration)**: **include all three** — `app.ses.enabled` strict binding, `phone_otp_tokens` partial unique index, locked-user verification guard. The related "`resendVerificationEmail` accepts `EMAIL_VERIFIED` users" item (skillars-1-3 Group A D7) was **verified already fixed** (`CoachRegistrationService.resendVerificationEmail:185-193` only re-sends for `null`/`UNVERIFIED`) and is **excluded**.

### Architecture / conventions to follow

- **No local `mvn verify`** — `[[feedback_no_local_mvn_verify]]`. Backend changes (AC1, AC9, AC10, AC11) are verified by targeted `-Dtest` runs recorded in the Dev Agent Record; the full suite is CI's gate. Shell/YAML/compose changes (AC3–AC8) are verified by `bash -n`, `shellcheck` (no-new-findings vs. a saved baseline), `actionlint`, `docker compose config`, and YAML parses — matching the established `deploy/**` verification path (`skillars-deferred-85`, `-87`, `skillars-uat-6`).
- **Two Flyway migrations**: `V120` (reviews epoch), `V121` (phone-OTP unique index). Both additive, both small-table. Max existing migration is `V119`. No `NOT VALID`/`VALIDATE` split at this table size (accepted class — see the `skillars-deferred-84` online-migration deferral, which stays open as a codebase-wide convention item).
- **`ReviewModerationService` structure is load-bearing** — the `REQUIRES_NEW` `TransactionTemplate` exists specifically to suspend the stale `AFTER_COMMIT` `EntityManager`; the `findByIdForUpdate` is the first read in that transaction so it needs no `entityManager.refresh` (contrast `BookingService.createBookingRequest`). The outer `catch` swallows to stop an `AFTER_COMMIT` exception becoming an HTTP 500. AC1 adds one guard clause inside the existing `ifPresentOrElse` block plus a migration/entity/event field; on the **write** side it adds a `findByIdForUpdate` lock to `ReviewSubmissionService.updateReview` (mirroring `submitCoachResponse`, which already locks) so the epoch bump is serialised (review finding M1) — `submitReview` is unchanged (a fresh row, no race).
- **`Message` must not get `@Version`** (AC2) — `MessagingService.java:315-317` and `skillars-deferred-16` Dev Notes: `ModerationResultApplier` / `AdminMessageService` / `MessageModerationSweeper` all `save()` the row and optimistic locking would turn benign moderation interleavings into `OptimisticLockingFailureException`s on a request thread with SSE callbacks and no retry.
- **`provision.sh` is idempotent and re-run-safe by contract** — AC3/AC4/AC5/AC7 all sit in or around section 7–8; every change must keep first-provision behaviour unchanged and re-runs a no-op. The `flock` (AC3) wraps the whole script including the idempotent re-run path.
- **`deploy/backup/*.sh` / `provision.sh` `set -euo pipefail`** — every command substitution whose failure must not abort is `|| true`-guarded; extend that, do not remove it (`skillars-deferred-85` AC3/AC6 Dev Notes).
- **The three registration services deliberately duplicate near-identical OTP/verification blocks** — do not introduce a shared base class for AC10's catch helper or AC11's lock guard; add the small block to each, matching how the delete-before-insert is already triplicated.
- **CI**: `.github/actions/docker-build/action.yml` stays a dumb "push these tags" action (AC6 logic lives in `ci.yml`); `pr-build.yml` is untouched; `:latest` is an OCI image index (default provenance attestation) — AC6's widened grep is in the `inspect_rc != 0` branch and does not touch the index-aware success path.

### Files expected to change

- **Backend (AC1)**: `src/main/resources/db/migration/V120__coach_reviews_moderation_epoch.sql` (new), `src/main/java/com/softropic/skillars/platform/reviews/repo/CoachReview.java`, `.../reviews/contract/ReviewSubmittedEvent.java`, `.../reviews/service/ReviewSubmissionService.java`, `.../reviews/service/ReviewModerationService.java`, `src/test/.../reviews/**` (test + any event builders).
- **Backend (AC1, write side)**: `ReviewSubmissionService.updateReview` also gains the `findByIdForUpdate` lock (M1).
- **Backend (AC9)**: a **new** small startup-validator class in `infrastructure.ses` (registered via `META-INF/spring.factories` if an `EnvironmentPostProcessor`); `src/test/.../ses/**`. `SesConfig.java` / `SesProperties.java` are **not** expected to change (review finding M4 — the four `@ConditionalOnProperty` gates stay as-is).
- **Backend (AC10)**: `src/main/resources/db/migration/V121__phone_otp_tokens_one_active_per_user.sql` (new), `.../security/service/CoachRegistrationService.java`, `PlayerRegistrationService.java`, `ParentRegistrationService.java` (OTP delete+`saveAndFlush` moved into a `REQUIRES_NEW` boundary — possibly a tiny new collaborator bean), `src/test/.../security/**`.
- **Backend (AC11)**: the same three registration services (`verifyEmail` + `verifyPhone`); i18n bundles under `src/frontend/src/i18n/**` / `src/main/resources` if `security.*` keys are client-rendered; `src/test/.../security/**`.
- **IT (AC2)**: `src/test/java/com/softropic/skillars/platform/messaging/api/SoftDeleteIT.java`.
- **Ops (AC3–AC5)**: `deploy/provision.sh`.
- **Ops (AC6)**: `.github/workflows/ci.yml`.
- **Ops (AC7)**: `deploy/provision.sh`, `deploy/lgtm/grafana-alerts.yml` (committed default + delimited `contactPoints` region — **not** deleted), `docs/deployment/*.md`.
- **Ops (AC8)**: `docker-compose.yml`; `docker-compose.local.yml` / `docker-compose.uat.yml` / `docker-compose.uat-hostwinds.yml` only if any redefines a moved service's `networks:`; `docs/deployment/*.md`.
- **Ledger (AC12)**: `_bmad-output/implementation-artifacts/deferred-work.md`, `_bmad-output/implementation-artifacts/sprint-status.yaml`, this story file.

### Files being created

- `src/main/resources/db/migration/V120__coach_reviews_moderation_epoch.sql`
- `src/main/resources/db/migration/V121__phone_otp_tokens_one_active_per_user.sql`
- One small SES startup-validator class (AC9), and possibly a tiny `REQUIRES_NEW` OTP-persist collaborator bean (AC10) — dev's call. **No** `grafana-alerts.rules.yml` split — the file stays a single committed default with a `provision.sh`-managed `contactPoints` region (review finding M5).

### Project Structure Notes

- Reviews module lives under `src/main/java/com/softropic/skillars/platform/reviews/**` (schema `reviews`). Messaging under `platform/messaging/**` (schema `messaging`). Registration under `platform/security/service/**` (table `main.phone_otp_tokens` / `main.email_verification_tokens` / `main."user"`).
- Migration numbering: next is `V120`, then `V121`. Zero other migrations pending.
- New tests should reuse existing IT base classes (`SoftDeleteIT` already extends the messaging IT base; SES/reviews/security tests should mirror existing siblings) — **no new Spring `@SpringBootTest` context configuration** is expected; if a new context is unavoidable, flag it (context-count ceiling matters — see `skillars-deferred-86` Dev Notes).

### References

- [Source: `src/main/java/com/softropic/skillars/platform/reviews/service/ReviewModerationService.java`] — `handleReviewSubmitted`, `AFTER_COMMIT` + `REQUIRES_NEW`, `findByIdForUpdate` + `PENDING`-only guard (the guard AC1 extends).
- [Source: `src/main/java/com/softropic/skillars/platform/reviews/service/ReviewSubmissionService.java`] — publish sites `:63` / `:72-73` (`submitReview`) and `:98` / `:103-104` (`updateReview`); `updateReview:83-87` is the 365-day edit rule; `updateReview` loads via `findByReviewIdAndAuthorId` (no lock — M1); `submitCoachResponse` already uses `findByIdForUpdate` (the pattern AC1 copies).
- [Source: `src/main/java/com/softropic/skillars/platform/reviews/contract/ReviewSubmittedEvent.java`] — 5-field record; AC1 adds `moderationEpoch` (6th).
- [Source: `src/main/java/com/softropic/skillars/platform/reviews/repo/CoachReview.java`] — entity; `@Column(name="moderation_status")`, `last_modified_at`; add `moderation_epoch`.
- [Source: `src/main/java/com/softropic/skillars/platform/reviews/repo/CoachReviewRepository.java:23-25`] — `@Lock(PESSIMISTIC_WRITE) findByIdForUpdate`.
- [Source: `src/test/java/com/softropic/skillars/platform/messaging/api/SoftDeleteIT.java:246`] — `concurrentDoubleSoftDelete_exactlyOneSucceeds_oneConflicts`, the test AC2 rewrites.
- [Source: `src/main/java/com/softropic/skillars/platform/messaging/service/MessagingService.java:296-339`] — `softDeleteMessage`: unlocked read + authz, then `findByIdForUpdate` (`:318`) **and** `entityManager.refresh(message, LockModeType.PESSIMISTIC_WRITE)` (`:325` — the causal lock for AC2's mutation check), then `deletedAt` check + write; `@Version` on `Message` forbidden at `:315-317`.
- [Source: `src/test/java/com/softropic/skillars/platform/session/api/DrillUploadServiceConcurrencyIT.java`] — the external-lock lock-causality pattern AC2 mirrors (`skillars-deferred-83` AC2).
- [Source: `deploy/provision.sh` (484 lines)] — `by-id` resolution `≈:194-218` (`HETZNER_VOLUME_ID` exact match `:194-200`, first-match loop `:202-215`, `/dev/sdb` fallback `:216`); stage-before-mount `≈:315-318` (`if pre_volume_payload_present` `:315`, `mkdir -p "${STAGING}"` `:317`, staging `rsync` `:318`); `/etc/fstab` maintenance `:333-350` (`sed -i` purge inside `if` at `:340-341`, trailing-newline guard `:348`); section 8 alert-routing `≈:418-475` (both-blank hard-fail `:457`, per-channel warns `:464-474`).
- [Source: `.github/workflows/ci.yml`] — `build-and-push` "Decide image tags (:latest ordering guard)" step (AC6); the workflow `on:` block (AC6 adds `workflow_dispatch`).
- [Source: `deploy/lgtm/grafana-alerts.yml`] — `contactPoints[0]` (`notify-ops`) hard-codes both an `email` and a `slack` receiver, split cleanly from `groups:` (rules) above and `policies:` below (AC7).
- [Source: `docker-compose.yml`] — the single `skillars-internal` network `:293-296` with the "Do NOT set internal: true" comment (AC8); the `grafana-alerts.yml` bind mount `:234`; service keys `prometheus`/`loki`/`tempo`/`redis`/`node_exporter` (underscore).
- [Source: `src/main/java/com/softropic/skillars/infrastructure/ses/`] — four `@ConditionalOnProperty("app.ses.enabled", …)` gates: `SesConfig` + `SesEmailServiceImpl` + `DevSesEmailService` (`havingValue="true", matchIfMissing=false`), `NoOpSesEmailService` (`havingValue="false", matchIfMissing=true`). `SesProperties.enabled` defaults `true` (latent, not the live failure — M4). AC9 adds a validator, changes none of these.
- [Source: `src/main/resources/db/migration/V21__skillars_security_extension.sql:23-33`] — `main.phone_otp_tokens` DDL, `idx_pot_userid` (non-unique) (AC10). Both `phone_otp_tokens` and `email_verification_tokens` carry `version BIGINT` and the entities have `@Version` (H3 secondary — same-token concurrent `verifyEmail` already serialises).
- [Source: `src/main/java/com/softropic/skillars/infrastructure/persistence/BaseEntity.java:27`] — `@Id @Tsid` — application-assigned id, so `PhoneOtpToken`'s `INSERT` is deferred to flush/commit (H3: a plain `save()`-wrap can't catch the unique violation; use `saveAndFlush` in a `REQUIRES_NEW` boundary).
- [Source: `src/main/java/com/softropic/skillars/platform/security/service/CoachRegistrationService.java`] — delete-before-insert OTP path (`:138` delete, `:140-145` build+save); `verifyEmail` `:108-131` / `verifyPhone` `:151-182` guards (AC11); `resendVerificationEmail` `:185-193` already `null`/`UNVERIFIED`-only (D7 excluded). `PlayerRegistrationService` (`:153`, `:173`) and `ParentRegistrationService` (`:142`, `:206`, `:162`) mirror it.
- [Source: `src/main/java/com/softropic/skillars/platform/security/repo/User.java:71`] — `private boolean locked`; setters `:156`, domain `lock()` `:259`. Writers: `UserAdminService:64` (admin), `GdprErasureService:96`, `UserErasedEventListener:27` — **no brute-force path** (AC11 / H2). `AuthService`/`LoginAttemptsService` throttle in-memory only.
- [Source: `src/main/java/com/softropic/skillars/platform/security/repo/PhoneOtpTokenRepository.java`] — `deleteByUserIdAndUsedFalse` (`@Modifying`), `findFirstByUserIdAndUsedFalseOrderByExpiresAtDesc` (AC10).
- [Source: `src/main/java/com/softropic/skillars/platform/security/contract/exception/EmailTokenException.java`] — only constructor is `(String key, boolean terminal)` — AC11's `verifyEmail` guard must pass 2 args (L1).

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (dev-story workflow)

### Debug Log References

- `mvn -o test-compile` → BUILD SUCCESS (whole project, incl. frontend build).
- **AC1**: `mvn -o test -Dtest=ReviewModerationServiceTest` → `Tests run: 8, Failures: 0, Errors: 0` (6 pre-existing + 2 new: `staleEpochDelivery_forSupersededEdit_isDiscardedEvenWhilePending`, `freshEpochDelivery_landsOnPending`).
  `mvn -o test -Dtest=ReviewSubmissionIT` → `Tests run: 7, Failures: 0, Errors: 0` (6 pre-existing + 1 new: `updateReview_epochBumpAppliesToFreshLockedState_notStaleInstance`).
  `mvn -o test -Dtest=ReviewModerationIT` → `Tests run: 5, Failures: 0, Errors: 0` (real AFTER_COMMIT listener path unbroken).
- **AC2**: `mvn -o test -Dtest=SoftDeleteIT` → `Tests run: 5, Failures: 0, Errors: 0`. Mutation check: with `MessagingService.softDeleteMessage`'s `findByIdForUpdate`→`findById` **and** `entityManager.refresh(message, PESSIMISTIC_WRITE)`→`refresh(NONE)` reverted together → `concurrentDoubleSoftDelete_loserRechecksUnderLockAndConflictsCleanly` → `Tests run: 1, Failures: 1` (DELETE returns 204 instead of 409). `MessagingService.java` restored to `HEAD` (confirmed via `git diff`).
- **AC3–AC5, AC7 (`provision.sh`)**: `bash -n deploy/provision.sh` OK; `shellcheck deploy/provision.sh` exit 0 (baseline was also clean — no new findings). Functional hand-traces run for AC4 (4 fstab cases), AC5 (7 device-resolution cases), AC7 (3 channel cases + committed-default + idempotent re-run). `flock` not runtime-testable on the dev mac (Linux-only) — hand-traced.
- **AC6 (`ci.yml`)**: `actionlint .github/workflows/ci.yml .github/workflows/pr-build.yml` → exit 0; `yaml.safe_load` on `ci.yml` OK. `pr-build.yml` untouched.
- **AC8 (`docker-compose.yml`)**: `docker compose --env-file <dummy> -f docker-compose.yml config` OK; `+docker-compose.local.yml` / `+docker-compose.uat.yml` / `+docker-compose.uat-hostwinds.yml` all OK. Service→network membership dumped and asserted.
- **AC9**: `mvn -o test -Dtest=SesEnabledPropertyValidatorTest,SesConditionalWiringTest` → `Tests run: 10, Failures: 0, Errors: 0`.
- **AC10 + AC11**: `mvn -o test -Dtest=CoachRegistrationResourceIT,ParentRegistrationResourceIT,PlayerRegistrationResourceIT` → `Tests run: 32 (14+14+4), Failures: 0, Errors: 0`. (Combined AC9–11 run: `Tests run: 42, BUILD SUCCESS`.) Migrations `V120` + `V121` apply cleanly on every IT boot (Flyway runs all on context start).
- **No `mvn verify`** — per `docs/validation-strategy.md` / `[[feedback_no_local_mvn_verify]]`; GitHub CI is the full-suite gate.
- **v0.4 (review fixes)**: verified `if false; then …; fi; echo $?` → `0` (confirms the HIGH flock-wrapper bug). Post-fix `bash -n` + `shellcheck` clean on `provision.sh`; `render_grafana_contact_points` re-run for all 3 channel cases → valid YAML, `policies:` block preserved, idempotent. `mvn -o test -Dtest=ParentRegistrationResourceIT,CoachRegistrationResourceIT,PlayerRegistrationResourceIT,SoftDeleteIT,ReviewSubmissionIT,ReviewModerationServiceTest,ReviewModerationIT,SesEnabledPropertyValidatorTest,SesConditionalWiringTest` → all green (see below). The first review-fix run flagged the 6-thread `resendPhoneOtp` race as non-contending (0 losers) — that test was replaced with a **deterministic `@MockitoSpyBean`-stubbed** collision proof.

### Completion Notes List

**AC1 — review-moderation epoch guard.** Implemented as specified with one deliberate deviation, documented here:

- **`entityManager.refresh(locked, PESSIMISTIC_WRITE)` added to `ReviewSubmissionService.updateReview`** after `findByIdForUpdate`. The story's M1 fix ("take `findByIdForUpdate` after the pre-checks, mutate the locked instance") is a **no-op without the refresh**: `updateReview` keeps the earlier unlocked `findByReviewIdAndAuthorId` load (per the story, so an unauthorised caller never takes a row lock), so the row is already managed in the persistence context. A `@Lock` JPQL query then takes the DB lock but returns the **existing, stale instance** without re-reading — the identical Hibernate identity-map gotcha `MessagingService.softDeleteMessage:325` documents and solves the same way. Without the refresh, two concurrent edits both read epoch N off the stale instance and both write N+1 (lost update); with it, the second edit reads fresh committed state under the lock. This is the same class of insight as the story's own AC2/H1 finding (the `refresh`, not the `findByIdForUpdate` call, is the causal lock lever). `EntityManager` injected as a `final` field via `@RequiredArgsConstructor` (matches `MessagingService`).
- **Story wording note (not a code issue):** AC1's concurrent-edit test bullet says the fixed outcome is "epoch `N+1`, not `N+2`". That is inverted — two edits both landing `N+1` **is** the lost-update bug (and would publish two events at the same epoch, defeating the `ReviewModerationService` guard). The correct serialised outcome is two **distinct** epochs, final `N+2`. The test I wrote (`updateReview_epochBumpAppliesToFreshLockedState_notStaleInstance`) proves this deterministically without the 365-day-rule timing hazard a barrier race would carry: an external transaction holds the row lock, bumps `moderation_epoch` 5 → 99, commits; `updateReview`'s locked read then observes 99 and writes **100** (with the refresh) vs. 6 (stale, without) — a direct mutation check on the refresh line.
- `V120` is a plain `ADD COLUMN … NOT NULL DEFAULT 0` (no index — the column is only read via the already-locked `findByIdForUpdate` row). `CoachReview.moderationEpoch` is a primitive `long` default `0L`. `ReviewSubmittedEvent` gained `long moderationEpoch` as the 6th component; `submitReview` publishes `0` (fresh row), `updateReview` publishes the post-increment value from the locked instance. The `ReviewModerationService` epoch guard runs **before** the `PENDING` guard (more specific signal) and `return`s with a `log.warn` on mismatch; the `PENDING`-guard comment was updated to record that the "superseded edit" case is now covered.

**AC2 — `SoftDeleteIT` mutation-check, refined.** The story's H1 note ("`entityManager.refresh`'s lock mode is the real causal lever; reverting `findByIdForUpdate`→`findById` alone is a no-op") is only half right against this codebase's Hibernate. Empirically: the `@Lock(PESSIMISTIC_WRITE)` JPQL query *does* still emit its own `SELECT … FOR UPDATE` for an already-managed row, and the trailing `save()` `UPDATE` blocks on the row lock regardless — so **neither revert alone** changes whether the DELETE request *blocks*. What the pessimistic read uniquely buys is the `deletedAt` re-check on *fresh* committed state. The mutation check therefore requires reverting **both** (`findById` + `refresh(NONE)`) together: the DELETE then keeps its stale pre-lock snapshot and returns `204` instead of `409`. The test was designed around this (external tx stages its own `deleted_at` while the request is parked) so it is a real causal proof, not a scheduling coincidence.

**AC3–AC8 — infra.** All in `provision.sh` / `ci.yml` / `docker-compose.yml` / `grafana-alerts.yml` + docs. No `src/**` change, so no `mvn` involved. Verified by `bash -n`, `shellcheck` (no new findings vs. the clean baseline), `actionlint`, `docker compose … config`, YAML parses, and hand-traces (recorded per-task above). `flock` (AC3) could not be runtime-tested — it is Linux-only and absent on the dev mac; the `_PROVISION_LOCKED` re-exec idiom + `bash -n` + shellcheck + a 3-case hand-trace stand in.

**AC9 — `app.ses.enabled` validator.** New `SesEnabledPropertyValidator` (`EnvironmentPostProcessor`, registered in a new `src/main/resources/META-INF/spring.factories`). **Deliberate deviation from the story's "trimmed, case-insensitive" wording:** the validator is *strict* — it rejects unless the raw value `equalsIgnoreCase("true"|"false")`, with **no** whitespace tolerance — precisely mirroring `@ConditionalOnProperty`'s own `OnPropertyCondition` semantics. Accepting `"true "` (trailing space) would pass validation but still leave `SesV2Client` / `SesEmailServiceImpl` / `NoOpSesEmailService` all unmatched, breaking the "accepted here ⇔ beans wire" invariant the AC exists to protect. `null`/unset is allowed (the no-op fallback's `matchIfMissing = true`). The four `@ConditionalOnProperty` gates and `SesProperties.enabled` are **untouched** (M4). The full-boot "`app.ses.enabled=yes` → context fails to start" path isn't unit-testable without a `@SpringBootTest` (EPPs need `SpringApplication`); the EPP unit test asserts the `IllegalStateException` + offending value directly, and `SesConditionalWiringTest` (`ApplicationContextRunner`) pins the wiring.

**AC10 — phone-OTP uniqueness. Fallback path taken (not `REQUIRES_NEW`).** The story marks `REQUIRES_NEW` "preferred" but sanctions the fallback. Chosen because: (a) the only genuinely-reachable concurrent double-insert is `ParentRegistrationService.resendPhoneOtp` (no email-token `@Version` upstream), and its whole `@Transactional` *is* the OTP delete+insert — a rollback-only + propagated `DataIntegrityViolationException` → 409 loses nothing; (b) `verifyEmail`'s OTP insert is already serialised upstream by the `EmailVerificationToken` `@Version` optimistic lock, so a collision there is effectively unreachable; (c) a cross-transaction `REQUIRES_NEW` OTP-issue would commit a stray OTP row if the outer `verifyEmail` tx later rolled back — extra subtlety for no real gain here. Implementation: `V121` partial unique index `uq_pot_one_active_per_user` + defensive dedup `DELETE`; the 4 OTP-insert sites use `saveAndFlush` (synchronous `@Tsid` INSERT → clean `DataIntegrityViolationException` at the call site, not a commit-time poison); `ApiAdvice` maps the constraint → **409 `security.otpResendInProgress`**; the exception is not caught in-service, so it propagates without an `UnexpectedRollbackException`. `email_verification_tokens` mirror index deferred (out of scope, avoids fixture risk). Concurrency IT: 6 barrier threads on `resendPhoneOtp` → exactly one active OTP row survives, every loser fails with a plain `DataIntegrityViolationException`, `verification_status` untouched.

**AC11 — lock-aware verification. Terminal decision (L2): non-terminal email / OTP has no flag.** `EmailTokenException`'s 2nd constructor arg is `canResend` (not literally `terminal`); passed `true` in the locked-user guard so a later admin unlock lets the user re-request and resume — this *is* L2's default (b) "non-terminal for the email path". `OtpVerificationException` is 1-arg (no such flag). Guards inserted after the `User` load and before any state mutation in `verifyEmail` + `verifyPhone` across all three services (identical block, script-inserted). `resendPhoneOtp` left untouched — its OTP is inert because `verifyPhone` rejects the locked user. `security.accountLocked` added to `messages_en`/`messages_de`. Per-service rejection ITs assert 400 + `security.accountLocked` + `verification_status` **not** advanced (the mutation check).

**AC12 — ledger.** 14 bullets in `deferred-work.md` tagged (`[CLOSED …]` / `[RESIDUAL CLOSED …]` / `[PARTIALLY CLOSED …]`), following the file's backtick-wrapped tail-append convention. Edited bullets (by section): `skillars-deferred-14` D3 (AC1); `skillars-deferred-16` D6 (AC2); `skillars-deferred-87` — `flock` (AC3), `.bak` (AC4), `sed` commented-line (AC4), staging free-space (AC5), multi-Volume (AC5), CI `:latest` freeze → **PARTIALLY** (AC6), GHCR `denied`/`name unknown` (AC6); `deploy-3-3` "Empty notification vars" residual → **RESIDUAL CLOSED** (AC7); `deploy-1-5` "No outbound firewall rules" (AC8); `skillars-1-3` W3 (AC9), Group A D5 (AC10), Group D D3 (AC11). File stayed at 1363 lines (append-in-place, no bullets removed).

### File List

**AC1 (Task 1):**
- `src/main/resources/db/migration/V120__coach_reviews_moderation_epoch.sql` (new)
- `src/main/java/com/softropic/skillars/platform/reviews/repo/CoachReview.java` (modified — `moderationEpoch` field)
- `src/main/java/com/softropic/skillars/platform/reviews/contract/ReviewSubmittedEvent.java` (modified — 6th record component)
- `src/main/java/com/softropic/skillars/platform/reviews/service/ReviewSubmissionService.java` (modified — `submitReview` publish arg; `updateReview` locked-read + refresh + epoch bump; `EntityManager` field)
- `src/main/java/com/softropic/skillars/platform/reviews/service/ReviewModerationService.java` (modified — epoch guard in `handleReviewSubmitted`)
- `src/test/java/com/softropic/skillars/platform/reviews/service/ReviewModerationServiceTest.java` (modified — 8 call sites + 2 new tests + `reviewWith` helper)
- `src/test/java/com/softropic/skillars/platform/reviews/api/ReviewSubmissionIT.java` (modified — new concurrent epoch-bump test + `ReviewSubmissionService` autowire)

**AC2 (Task 2):**
- `src/test/java/com/softropic/skillars/platform/messaging/api/SoftDeleteIT.java` (modified — `concurrentDoubleSoftDelete_…` rewritten; tautological assertion removed; `Timeout`/`Duration` imports; unused `AtomicInteger` import dropped)

**AC3–AC5, AC7 (Tasks 3, 4, 5, 7):**
- `deploy/provision.sh` (modified — new "section 0" `flock` guard; multi-Volume symlink enumeration + hard-fails; staging free-space guard; comment-safe fstab purge + timestamped `.bak`; `render_grafana_contact_points()` + call)
- `deploy/lgtm/grafana-alerts.yml` (modified — `# >>> BEGIN/END provision.sh-managed contactPoints` markers around the committed both-receiver default)
- `docs/deployment/monitoring.md`, `docs/deployment/secrets-reference.md` (modified — AC7 notify-ops rendering; AC8 loki/tempo network note)

**AC6 (Task 6):**
- `.github/workflows/ci.yml` (modified — `workflow_dispatch` + `force_publish_latest` input; guard-step force-publish branch (master-gated) + `write_tags()` helper; widened `name unknown` grep; `head_commit.timestamp` caveat comment)

**AC8 (Task 8):**
- `docker-compose.yml` (modified — new `skillars-observability` `internal: true` network; `prometheus`/`loki`/`tempo`/`redis`/`node_exporter` moved onto it only; `app` + `grafana` join both; comments)
- `docs/deployment/first-time-setup.md` (modified — two-network topology section)

**AC9 (Task 9):**
- `src/main/java/com/softropic/skillars/infrastructure/ses/SesEnabledPropertyValidator.java` (new)
- `src/main/resources/META-INF/spring.factories` (new — registers the EPP)
- `src/test/java/com/softropic/skillars/infrastructure/ses/SesEnabledPropertyValidatorTest.java` (new)
- `src/test/java/com/softropic/skillars/infrastructure/ses/SesConditionalWiringTest.java` (new)

**AC10 (Task 10):**
- `src/main/resources/db/migration/V121__phone_otp_tokens_one_active_per_user.sql` (new)
- `src/main/java/com/softropic/skillars/platform/security/api/ApiAdvice.java` (modified — `uq_pot_one_active_per_user` → `security.otpResendInProgress` in `CONSTRAINT_MAPPINGS` + `CONFLICT_CONSTRAINTS`)
- `src/main/java/com/softropic/skillars/platform/security/service/CoachRegistrationService.java`, `PlayerRegistrationService.java`, `ParentRegistrationService.java` (modified — OTP insert sites `save` → `saveAndFlush`; **also AC11** below)
- `src/main/resources/i18n/messages_en.properties`, `messages_de.properties` (modified — `security.otpResendInProgress`; **also AC11** `security.accountLocked`)
- `src/test/java/com/softropic/skillars/platform/security/api/CoachRegistrationResourceIT.java` (modified — index test; **also AC11** tests)
- `src/test/java/com/softropic/skillars/platform/security/api/ParentRegistrationResourceIT.java` (modified — concurrent-resend test; `ParentRegistrationService` autowire; **also AC11** tests)

**AC11 (Task 11):**
- `CoachRegistrationService.java` / `PlayerRegistrationService.java` / `ParentRegistrationService.java` (modified — `user.isLocked()` guard in `verifyEmail` + `verifyPhone`)
- `src/test/java/com/softropic/skillars/platform/security/api/PlayerRegistrationResourceIT.java` (modified — locked-user tests + `TransactionTemplate`/`hashOtp`/`VERIFY_PHONE_ENDPOINT` plumbing)
- (i18n + Coach/Parent ITs listed under AC10)

**AC12 (Task 12):**
- `_bmad-output/implementation-artifacts/deferred-work.md` (modified — 14 bullets tagged)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (modified — status `ready-for-dev` → `in-progress` → `review`; comment corrected in v0.4)
- this story file

**v0.4 — code-review fixes (additional touches, files already listed above unless noted):**
- `deploy/provision.sh` — §0 flock wrapper rewritten (direct `|| _flock_rc=$?` capture, `--conflict-exit-code 99`, `bash "$0"`); AC5 free-space guard numeric-validation; `render_grafana_contact_points` anchored grep + post-splice marker sanity check + grafana-recreate log line
- `src/main/java/.../reviews/service/ReviewSubmissionService.java` — post-`refresh` BLOCKED/UNDER_REVIEW re-check; `orElseThrow` → `ResourceNotFoundException`
- `CoachRegistrationService.java` / `PlayerRegistrationService.java` / `ParentRegistrationService.java` — `verifyEmail` locked exception made terminal (`canResend=false`); `resendVerificationEmail` locked silent no-op (×3); `resendPhoneOtp` locked guard (Parent)
- `src/test/java/.../messaging/api/SoftDeleteIT.java` — `externalDeletedAt` truncated to millis; wall-clock timing assertion removed; `Duration` import → `ChronoUnit`
- `src/test/java/.../security/api/ParentRegistrationResourceIT.java` — 6-thread race test replaced with a deterministic `@MockitoSpyBean`-stubbed collision test (409 `security.otpResendInProgress` + clean `DataIntegrityViolationException` + state-intact; mutation-verified by dropping V121's index); new `resendPhoneOtp_lockedUser_rejected` + `resendVerificationEmail_lockedUser_isSilentNoOp`; unused concurrency imports removed
- `src/test/java/.../security/api/CoachRegistrationResourceIT.java` / `PlayerRegistrationResourceIT.java` — new `resendVerificationEmail_lockedUser_isSilentNoOp`
- `docs/deployment/monitoring.md` — grafana `--force-recreate` note

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-08-31 | 1.1 | **CI fix — Spring context-count ceiling.** The first `master` PR build (#133) passed all 1023 tests but failed the post-suite context guard: 38 contexts built vs. a ceiling of 37. Cause: `ParentRegistrationResourceIT`'s AC10 test used a `@MockitoSpyBean PhoneOtpTokenRepository` to neutralise `deleteByUserIdAndUsedFalse` and force a `uq_pot_one_active_per_user` collision — a bean override forks a new TestContext-cache entry (the exact hazard the Dev Notes flagged). Replaced with a spy-free test (`secondActiveOtpInsert_forSameUser_isRejectedByPartialUniqueIndex`) that exercises V121 directly: seed one active OTP row (committed), then insert a second `used=false` row for the same user in its own transaction and assert `DataIntegrityViolationException` + exactly one active row survives + `verification_status` untouched. The `ApiAdvice` 409 `security.otpResendInProgress` mapping rides the same already-tested `DataIntegrityViolationException` handler path as the other `CONSTRAINT_MAPPINGS` entries, so no dedicated HTTP assertion is needed. No production-code change; no new context. | Mbah (release-workflow) |
| 2026-08-31 | 1.0 | **Story marked done.** All 12 ACs implemented; senior-dev review (v0.2) + bmad-code-review (v0.4) both applied with every finding resolved (11 `[Review][Patch]` + 1 decision, 3 deferred pre-existing/spec-sanctioned, 9 dismissed). All task/subtask checkboxes complete. Verification: targeted `-Dtest` green (reviews/messaging/ses/registration), `bash -n` + `shellcheck` (no-new-findings) + `actionlint` + `docker compose config` all clean; no local `mvn verify` per project convention — CI is the full-suite gate. Staged, committed, pushed; PR opened for CI-gated merge to master. | Mbah (release-workflow) |
| 2026-08-31 | 0.1 | Story created via story-creation process. Messaging/Admin/Reviews/Disputes confirmed mined out (4th time) — anchor is the two real items (AC1 review-moderation epoch guard, AC2 SoftDeleteIT race-test hardening); bundled with Deploy/Infra from the fresh skillars-deferred-87 code-review vein (AC3 provision.sh flock, AC4 /etc/fstab .bak + comment-safe purge, AC5 staging df guard + multi-Volume HETZNER_VOLUME_ID hard-fail, AC6 CI :latest workflow_dispatch recovery + GHCR first-publish grep) plus two previously-carved-out design items the project owner folded in (AC7 Grafana single-channel receiver rendering, AC8 observability egress firewall) and three Auth/Registration items (AC9 app.ses.enabled strict binding, AC10 phone_otp_tokens partial unique index, AC11 locked-user verification guard). 2 Flyway migrations (V120, V121), both additive small-table. resendVerificationEmail flow-regression item (skillars-1-3 Group A D7) verified already fixed and excluded. Status: ready-for-dev. | Mbah (create-story) |
| 2026-08-31 | 0.4 | **Applied bmad-code-review findings (11 `[Review][Patch]`, 1 decision-resolved).** **[HIGH]** `provision.sh` §0 flock wrapper always `exit 0`'d on failure — `if cmd; then …; fi` then `$?` yields the if-construct status (0), not `cmd`'s (empirically confirmed). Rewritten to `flock --nonblock --conflict-exit-code 99 … bash "$0"` with direct `|| _flock_rc=$?` capture; `99` → refusal + `exit 1`, other non-zero → child failure propagated. **[MED]** `updateReview` now re-checks BLOCKED/UNDER_REVIEW on the post-`refresh` locked instance (a concurrent admin BLOCK / flag auto-hold was being silently reset to PENDING); its `findByIdForUpdate.orElseThrow` now throws `ResourceNotFoundException` not `AUTHOR_MISMATCH`. **[MED]** `SoftDeleteIT` AC2 test — `externalDeletedAt` truncated to millis (PG µs vs `Timestamp.equals` nanos → CI flake); wall-clock timing assertion dropped (the deterministic external-hold + 409 + stable-`deleted_at` are the proof). **[MED, decision-resolved] AC11 resend paths**: `resendPhoneOtp` (Parent) throws `OtpVerificationException("security.accountLocked")`; `resendVerificationEmail` (×3) is a **silent no-op** for locked users (NOT a throw — deviation from the patch text — to preserve the endpoint's always-200 anti-enumeration contract); `verifyEmail` locked exception made **terminal** (`canResend=false`, reversing the story's L2 default per the review decision). **[LOW-MED]** Parent concurrent-resend IT now asserts `losers ≥ 1` (fails loudly if the race wasn't exercised). **[LOW]** `provision.sh` AC5 guard numeric-validates `du`/`df` before the arithmetic (fail-open as its comment promises); `render_grafana_contact_points` grep anchored to column 1 + post-splice one-BEGIN/one-END sanity check; grafana `--force-recreate` note added to the rewrite log line + `monitoring.md`; `sprint-status.yaml` comment corrected. 3 findings deferred (pre-existing / spec-sanctioned), 9 dismissed with rationale. `bash -n`/`shellcheck`/`actionlint` clean; targeted `-Dtest` green. Status stays review. | Mbah (apply-review) |
| 2026-08-31 | 0.3 | **dev-story implementation — all 12 ACs complete, status → review.** AC1: V120 + `moderationEpoch` on `CoachReview`/`ReviewSubmittedEvent`; `updateReview` takes `findByIdForUpdate` **+ `entityManager.refresh(…PESSIMISTIC_WRITE)`** (the refresh is the actual causal lever — without it the "locked" instance is the stale pre-check one, so the M1 fix as literally worded is a no-op); `ReviewModerationService` epoch guard before the PENDING guard. Story's "N+1 not N+2" test wording is inverted (N+1 = the lost-update bug) — test rewritten as a deterministic external-bump 5→99→100 mutation check on the refresh. AC2: `SoftDeleteIT` rewritten (external tx stages `deleted_at`, concurrent DELETE re-reads under lock → 409); mutation check needs **both** `findByIdForUpdate`→`findById` AND `refresh`→`NONE` reverted (each alone is a no-op in this Hibernate — the `@Lock` query and the `save()` UPDATE both still block). AC3–AC7 in `provision.sh` (flock section 0, multi-Volume enumeration + hard-fails, `du`/`df` staging guard, comment-safe fstab purge + timestamped `.bak`, `render_grafana_contact_points`); AC6 `workflow_dispatch force_publish_latest` (master-gated) + `name unknown` grep in `ci.yml`; AC8 `skillars-observability` `internal:true` network in `docker-compose.yml`. AC9: strict `SesEnabledPropertyValidator` EPP (strict, not "trimmed" — mirrors `@ConditionalOnProperty` exactly); gates untouched. AC10: **fallback path** (V121 partial unique index + `saveAndFlush` + `ApiAdvice` 409 `security.otpResendInProgress`; not `REQUIRES_NEW` — the only racy path `resendPhoneOtp` has nothing else in its tx, `verifyEmail`'s insert is already `@Version`-serialised). AC11: `user.isLocked()` guard in `verifyEmail`(canResend=true, non-terminal per L2)/`verifyPhone` × 3 services. AC12: 14 `deferred-work.md` bullets tagged. Verified: targeted `-Dtest` runs (reviews/messaging/ses/registration all green — combined 42 + review/messaging classes), `bash -n`/`shellcheck` clean, `actionlint` clean, `docker compose config` clean for base + 3 overlays. No `mvn verify` (per validation-strategy). | Mbah (dev-story) |
| 2026-08-31 | 0.2 | Applied senior-dev review (`story-review.md`). All 8 HIGH/MEDIUM + 7 LOW findings verified against source — **no false positives**. **H1** AC2: mutation check retargeted from `findByIdForUpdate`→`findById` (a no-op — `entityManager.refresh(…, PESSIMISTIC_WRITE)` at `MessagingService:325` still locks) to flipping the `refresh` lock mode to `NONE`. **H2** AC11: reframed — `User.locked` is written by admin action / GDPR erasure / user-erased event, NOT `LoginAttemptsService` (in-memory only); guard is defense-in-depth, not a brute-force fix. **H3** AC10: `@Tsid` app-assigned id defers the `INSERT` to commit, so the unique violation surfaces at commit (raw 500) and poisons the outer `@Transactional`; fix now uses `saveAndFlush` inside a `REQUIRES_NEW` boundary (fallback: index + 409-map documented). Also: same-token concurrent `verifyEmail` already serialised via `@Version` — "both commit" softened. **M1** AC1: `updateReview` epoch bump moved onto a `findByIdForUpdate`-locked instance (was an unlocked read-modify-write → lost update under concurrent edits); concurrent-edit test added. **M2** AC6: `denied` NOT reclassified as ":latest absent" (GHCR also returns it for a permission failure on an existing package) — only `name unknown` added. **M3** AC6: `workflow_dispatch` force-publish now requires `github.ref == refs/heads/master`; null `head_commit.timestamp` caveat noted. **M4** AC9: reduced to the fail-fast validator only — the four `@ConditionalOnProperty` gates + `SesProperties.enabled` default left untouched (already a consistent set); premise corrected (loud `NoSuchBeanDefinitionException`, not silent; prod runs `SPRING_PROFILES_ACTIVE=prod`). **M5** AC7: keep `grafana-alerts.yml` as a committed valid default with a `provision.sh`-managed delimited `contactPoints` region (not delete-and-generate — Docker would mkdir the bind path); preserve `${GF_*}` placeholders (no inlined secrets); quoted heredoc for the `{{…}}` Go templates. **L1** AC11 `EmailTokenException` is 2-arg. **L2** AC11 terminal-vs-non-terminal decision surfaced. **L3** AC3 dropped the "works when piped" claim (`$0` re-exec is file-only). **L4** AC4 `.bak` moved inside the `if` (not every re-run). **L5** AC5 hard-fail also covers "`HETZNER_VOLUME_ID` set-but-unresolvable + >1 Volume". **L6** AC8 `node_exporter` (underscore) + `docker-compose.uat-hostwinds.yml` added to the merge check. **L7** `provision.sh` (484 lines) citations re-anchored. Status stays ready-for-dev. | Mbah (apply-review) |

## Review Findings

_bmad-code-review 2026-08-31 — 3-layer adversarial (Blind Hunter / Edge Case Hunter / Acceptance Auditor). Acceptance Auditor: **0 AC violations, all 12 ACs implemented**; 3 deviations all documented/spec-sanctioned (AC9 strict-not-trimmed validator, AC10 sanctioned fallback vs REQUIRES_NEW, AC1 documented `entityManager.refresh` addition). 1 decision-needed, 10 patch, 3 defer, 9 dismissed._

### Decision-needed → resolved to Patch

- [x] [Review][Decision] **Extend AC11 locked-user guard to the OTP/email *resend* paths?** — **RESOLVED 2026-08-31: option (b).** Add `user.isLocked()` guard to `resendPhoneOtp` + `resendVerificationEmail` in all 3 registration services, **and** make the `verifyEmail` locked exception terminal (`canResend=false`, overriding the story's L2 non-terminal decision). Tracked as the `[Review][Patch]` item below.

### Patch (resolved decision-needed)

- [x] [Review][Patch] **[MEDIUM]** ✅ APPLIED (with one deviation) — Extend the AC11 locked-user guard to the resend paths + make the email lock terminal. (1) Add `if (user.isLocked()) throw new OtpVerificationException("security.accountLocked");` after the `findOneById` in `resendPhoneOtp` (Coach/Player/Parent). (2) Add `if (user.isLocked()) throw new EmailTokenException("security.accountLocked", false);` after the user load in `resendVerificationEmail` (Coach/Player/Parent). (3) Change the AC11 `verifyEmail` guard from `new EmailTokenException("security.accountLocked", true)` to `false` (terminal — a locked account cannot self-resume) in all 3 services; update the Task 11 note / Completion Notes to record the L2 reversal. [CoachRegistrationService.java / PlayerRegistrationService.java / ParentRegistrationService.java]

### Patch

- [x] [Review][Patch] **[HIGH]** ✅ APPLIED — `provision.sh` §0 flock wrapper always exits 0 on failure — `if env … flock … "$0" "$@"; then exit 0; fi` then `_flock_rc=$?` captures the *if-construct* status (bash returns 0 when no branch matched), never flock's. Empirically confirmed. Effect: (1) a failed re-exec'd provisioning run (any non-zero child exit) reports `exit 0` — a broken provision looks successful; (2) a concurrent second run (`flock -n` exit 1) also `exit 0`, and the "another provision.sh is already running" refusal never prints — AC3's stated behaviour is not delivered. Fix: capture directly — `_flock_rc=0; env _PROVISION_LOCKED=1 flock --exclusive --nonblock "${LOCK_FILE}" "$0" "$@" || _flock_rc=$?` then branch on `$_flock_rc`; consider `flock --conflict-exit-code 99` to tell a held lock from a genuine child failure. [deploy/provision.sh:~74-80]
- [x] [Review][Patch] **[MEDIUM]** ✅ APPLIED — `ReviewSubmissionService.updateReview` never re-validates `moderationStatus` on the post-`refresh` locked instance — the BLOCKED/UNDER_REVIEW reject guard runs only on the stale unlocked `findByReviewIdAndAuthorId` instance; after `entityManager.refresh(locked, PESSIMISTIC_WRITE)` it unconditionally sets PENDING + bumps the epoch. A concurrent admin BLOCK / `ReviewFlagService.flag` auto-hold / moderation-listener verdict committed between the unlocked read and the lock acquisition is silently reset to PENDING (admin-queue item lost, or a policy-BLOCKED review re-enters auto-approve). The sibling it mirrors, `MessagingService.softDeleteMessage`, re-checks `getDeletedAt()` post-refresh. Fix: repeat the BLOCKED/UNDER_REVIEW guard on `locked` after the refresh. [ReviewSubmissionService.java:~92-117]
- [x] [Review][Patch] **[MEDIUM]** ✅ APPLIED — `SoftDeleteIT.concurrentDoubleSoftDelete_loserRechecksUnderLockAndConflictsCleanly` has two fragile assertions. (a) `assertThat(finalDeletedAt).isEqualTo(externalDeletedAt)` where `externalDeletedAt = Timestamp.from(Instant.now().minusSeconds(30))` — `Instant.now()` on Linux CI can carry nanoseconds, PG `timestamp` stores microseconds, `java.sql.Timestamp.equals` compares nanos exactly → intermittent CI failure (green on a millisecond-resolution dev Mac). (b) `assertThat(elapsedMillis).isGreaterThanOrEqualTo(holdMillis - 400)` is a wall-clock timing assertion (the fragile class prior deferred-13/-15 reviews flagged) that adds no coverage over the 409-body + stable-`deleted_at` assertions. Fix: seed `externalDeletedAt` truncated to micros/millis (or compare truncated); drop or greatly loosen the timing assertion. [SoftDeleteIT.java:~1304-1374]
- [x] [Review][Patch] **[LOW-MED]** ✅ APPLIED — `ParentRegistrationResourceIT.resendPhoneOtp_concurrent_…` — the loser assertion (`catch (ExecutionException) → isInstanceOf(DataIntegrityViolationException)`) and the stated "drop V121's index → N rows" mutation check only hold when the 6 barrier threads genuinely contend; if they serialize (each deletes its predecessor's committed row, inserts, commits) there are zero losers and the test stays green even with the unique index dropped. Fix: count losers and `assertThat(losers).isGreaterThanOrEqualTo(1)` (fail loudly if the race was not exercised); keep `count(used=false)==1`. [ParentRegistrationResourceIT.java:~1838-1884]
- [x] [Review][Patch] **[LOW]** ✅ APPLIED — `provision.sh` AC5 free-space guard fails *closed* on a non-numeric-but-non-empty `du`/`df` reading — it only checks `[ -n "$_payload_kb" ]`, then `[ "$(( _payload_kb * 2 ))" -gt … ]`; a non-numeric value makes `$(( ))` error under `set -euo pipefail` and aborts *before* `mount` (the re-run loop AC5 exists to prevent — its comment promises "fail-OPEN on an unreadable du/df"). Fix: numeric-validate both captures (`case … in *[!0-9]*) fail-open ;; esac`) before the arithmetic. [deploy/provision.sh staging free-space guard]
- [x] [Review][Patch] **[LOW]** ✅ APPLIED — `render_grafana_contact_points` — marker presence is `grep -qF` (substring, any column) but the `awk` splice needs the marker at column 1 (`index($0,"…")==1`); an indented or mis-ordered BEGIN/END marker passes the guard then either silently no-ops (single-channel fix never applied, logs "already match") or, if only END loses column-1, truncates everything from BEGIN to EOF (the `policies:` block). Fix: anchor the `grep` (`grep -qE '^# >>> BEGIN provision\.sh-managed contactPoints'`), and assert exactly one BEGIN + one END line in the spliced output before `cp`. [deploy/provision.sh render_grafana_contact_points]
- [x] [Review][Patch] **[LOW]** ✅ APPLIED — `updateReview`'s `findByIdForUpdate(reviewId).orElseThrow` throws `ReviewErrorCode.AUTHOR_MISMATCH` ("Review not found or caller is not the author") — but author identity was already validated by the unlocked pre-check, so this branch can only be a concurrently-deleted row; a legitimate author gets an authorization error for a not-found condition. Fix: use a not-found error code/message for that `orElseThrow`. [ReviewSubmissionService.java:~1000-1004]
- [x] [Review][Patch] **[LOW]** ✅ APPLIED — `docs/deployment/monitoring.md` tells the operator to "edit the `.env` channels and re-run `provision.sh`" to change alert receivers, but Grafana reads the provisioning file only at container start — the change does not take effect until Grafana is recreated. Fix: add `docker compose up -d --force-recreate grafana` to the doc and to the `log` line `render_grafana_contact_points` emits on a rewrite. [docs/deployment/monitoring.md]
- [x] [Review][Patch] **[LOW]** ✅ APPLIED — `provision.sh` §0 re-execs via bare `"$0"` (needs the file's execute bit), but the only documented invocation is `bash /opt/skillars/deploy/provision.sh` (no `+x` required); a checkout that lost the exec bit makes the re-exec fail `EACCES` → `flock` exits 126. Fix: re-exec as `flock … bash "$0" "$@"`. [deploy/provision.sh:~74]
- [x] [Review][Patch] **[LOW]** ✅ APPLIED — `sprint-status.yaml` — the `skillars-deferred-88-…` entry's inline comment still reads "status remains ready-for-dev" after the value was changed to `review`. Fix: update the comment to match. [_bmad-output/implementation-artifacts/sprint-status.yaml]

### Review follow-up resolution notes (dev, 2026-08-31 v0.4)

All 11 `[Review][Patch]` items applied. Verification: `bash -n` + `shellcheck` clean on `provision.sh`; `mvn -o test` green for `ReviewModerationServiceTest`, `ReviewSubmissionIT`, `SoftDeleteIT`, `Coach/Parent/PlayerRegistrationResourceIT` (see Debug Log). Two deviations from the literal patch text:

- **[MEDIUM] AC11 resend paths — `resendVerificationEmail` is a silent no-op for locked users, NOT a throw.** The patch text said `throw new EmailTokenException("security.accountLocked", false)` after the user load. But `resendVerificationEmail` is deliberately anti-enumeration — `userRepository.findOneByEmail(...).ifPresent(...)`, always returns `200`, and silently skips non-`UNVERIFIED` users. Throwing a `400 security.accountLocked` for a locked-but-existing email would make a locked account distinguishable from a nonexistent / already-verified one (an enumeration oracle), and would diverge from how the endpoint already treats every other skip case. Resolution: `if (user.isLocked()) return;` inside the lambda (silent no-op) — the decision's intent ("a locked account cannot self-resume") is fully met, the always-200 contract is preserved. `resendPhoneOtp` (Parent only — Coach/Player have no such method) does throw `OtpVerificationException("security.accountLocked")` as specified: that endpoint already requires a valid `userId` + `EMAIL_VERIFIED` status and already returns a specific `security.*` code for `verifyPhone`'s own locked guard, so it is not an enumeration oracle.
- **[HIGH] flock wrapper** — used `flock --exclusive --nonblock --conflict-exit-code 99 … bash "$0"` and captured the status with `|| _flock_rc=$?` (direct capture, not the `if`-construct). `99` → the "already running" refusal + `exit 1`; any other non-zero → the re-exec'd child's real failure, propagated. `--conflict-exit-code` needs util-linux ≥ 2.35 (Ubuntu 22.04 base ships 2.37 — the script's stated target). Also folds in the LOW "`bash "$0"`" fix (no exec bit needed) and the LOW `git merge-base` context is unaffected.
- **[MEDIUM] `updateReview` moderation re-check** — the BLOCKED/UNDER_REVIEW guard is now repeated on the post-`refresh` `locked` instance (throws `EDIT_NOT_PERMITTED`), and the `findByIdForUpdate(...).orElseThrow` there now throws `ResourceNotFoundException("Review", …)` (matching `submitCoachResponse`) instead of `AUTHOR_MISMATCH` — a concurrently-deleted row is a not-found, not an authz failure.

### Deferred (pre-existing / spec-sanctioned)

- [x] [Review][Defer] `${DEPLOY_ROOT}`/`${MOUNT_POINT}` interpolated unescaped into the fstab `grep -E` / `sed -E` pattern — safe only while `DEPLOY_ROOT` is a regex-metacharacter-free constant; AC4 refined the regex body but the unescaped-variable pattern predates this story. [deploy/provision.sh fstab block] — deferred, pre-existing
- [x] [Review][Defer] AC5 single-Volume + explicit-but-unresolvable `HETZNER_VOLUME_ID` still warns-then-falls-back to the lone attached Volume (spec AC5 case (h)); an operator who *pinned* the wrong id is a risk signal even with one Volume. Spec-sanctioned; stricter handling is a follow-up. [deploy/provision.sh device resolution] — deferred, spec-sanctioned
- [x] [Review][Defer] The fstab add/purge block has no `[ -n "${VOLUME_LINK}" ]` precondition — if zero `scsi-0HC_Volume_*` by-id symlinks resolve but `/dev/sdb` is a block device, a device-less `FSTAB_ENTRY` (`" /opt/skillars/data …"`) can be purged-then-appended, breaking the next `mount -a`. Pre-existing from deferred-87; narrow on Hetzner (by-id symlink stable while attached). [deploy/provision.sh:~273,419-427] — deferred, pre-existing

### Dismissed (9)

Blind Hunter #7 `updateReview` transaction boundary — verified false, `ReviewSubmissionService` is class-level `@Transactional`. Blind Hunter #9 observability network peer edges — Edge Case Hunter verified with project access: every peer edge preserved, `prometheus.yml` scrapes only `app` + `node_exporter`, overlays touch only `minio`. Blind Hunter #10 EPP wiring / strict-validator CI risk — `spring.factories` key + FQN verified correct; a full-boot `@SpringBootTest` was rejected for the context-count ceiling (documented), CI is the gate for any existing IT that sets a non-boolean `app.ses.enabled` (per the no-local-`mvn verify` convention). Edge Case Hunter #8 / empty `org.opencontainers.image.created=` on the recovery path — spec AC6 explicitly sanctioned "note it and leave it"; `${{ … || github.run_started_at }}` is an optional nicety. Auditor #2 strict (no-trim) AC9 validator — documented, mirrors `@ConditionalOnProperty` semantics, catches `" true"` that would otherwise leave beans unwired. Auditor #3 no full-boot AC9 test — documented coverage trade-off. Auditor #4 AC10 `verifyEmail` OTP-collision rollback — spec-sanctioned fallback; reachability at the `verifyEmail` sites is ~0 (status mutual-exclusivity + `@Version` serialization). Auditor #5 AC1 `entityManager.refresh` — in the story's own Task 1 checkbox + Dev Notes, mutation-tested by `ReviewSubmissionIT`. First-run always rewrites the Grafana region + drops one `.bak` (committed-default comment block ≠ rendered output) — cosmetic, self-limiting.
