# Story skillars-deferred-91: A generic transactional outbox for AFTER_COMMIT reliability (money + notifications + SLU), a completion-gated coach-payout / CAPTURE_PENDING design pass, rolling-deploy migration-safety hardening, a full de-DE formal-register + fr-FR accent pass, N+1 batching for `getPublicProfile`, and the genuine one-off bugs still open in `deferred-work.md`

Status: done

<!-- v0.1 — story-creation pass (2026-09-03), immediately after skillars-deferred-90 merged (PR #145,
     master CI green) and 4 Dependabot PRs (#140/#142/#143/#144) merged. Project-owner-selected large
     cross-cutting bundle. Four multi-round decisions taken during creation (see § "Project-owner
     decisions folded in"): (1) FULL generalization of deferred-90's blob-deletion outbox to the money
     + notification + SLU AFTER_COMMIT paths; (2) FULL escrow / completion-gated-payout design pass,
     not just a CAPTURE_PENDING timeout; (3) FULL de-DE `Sie` consistency rewrite, not just the one
     new string; (4) ALL THREE rolling-deploy items — enum-gate + lint-widen + retroactive audit of the
     6 grandfathered ACCESS EXCLUSIVE migrations. Anchor one-off bugs AC12–AC19. This story is
     deliberately very large per the project owner's standing "no small stories" instruction. -->

## Story

As the **Skillars engineering team**,
I want a generic durable-outbox mechanism replacing the fire-and-forget `@TransactionalEventListener(AFTER_COMMIT)` pattern on every path that can silently lose money or a notification, a real design pass on completion-gated coach payout and the `CAPTURE_PENDING` dead-end, the rolling-deploy migration hazards that `skillars-deferred-90` AC10 only *catalogued* actually closed, one consistent formal register across the whole de-DE bundle plus accented fr-FR, and the genuine one-off bugs still sitting in `deferred-work.md`,
so that a post-commit Stripe refund or a cancellation email can no longer vanish with no retry, an unrecoverable `CAPTURE_PENDING` row stops holding a coach's slot forever, a rolling deploy stops carrying the enum-widen / `ACCESS EXCLUSIVE` hazards the convention doc names but does not fix, DE users stop reading a `du`/`Sie` mix, and the `ErrorLog` `%`-crash-in-the-exception-handler / the transaction-less GDPR video cascade / the same-segment `permitAll()` path leak are all fixed.

## Story creation context

Created 2026-09-03 via the story-creation process, immediately after `skillars-deferred-90` merged (PR #145, master CI green) and its four green Dependabot follow-ups. Project-owner instruction (unchanged from `skillars-deferred-89`/`-90`): *"Go through `deferred-work.md` and put together a story that touches Genuine one-off bugs & gaps. If they are exhausted, get the ones that touch the following buckets in the order listed: Rolling-deploy column-drop ordering; i18n / hardcoded-English / de-DE native review as well as French locale; N+1 / query batching. If there is an item that needs a decision from me, let's go through together. Do not create small stories."*

Genuine one-off bugs are **not** exhausted — AC12–AC19 anchor the story. The i18n and rolling-deploy buckets each have real residue that `skillars-deferred-90` deliberately deferred (deferred-90 AC10 wrote the migration-safety *convention + lint* but grandfathered every pre-`V121` migration; deferred-90 AC12 did a native pass on only the 55 marked strings and left the rest of the de-DE bundle informal). The N+1 bucket is nearly exhausted by deferred-90 AC13 — only `getPublicProfile` remains, and the ledger itself flags it as unmeasured, so AC11 measures before batching.

### Project-owner decisions folded in (2026-09-03)

Four decisions taken during a multi-round story-creation discussion. Each was offered as three options (narrow / medium / full); the project owner chose **full** on all four.

1. **AFTER_COMMIT-listener reliability — FULL generalization.** deferred-90 built a durable outbox for exactly one case (`PendingBlobDeletionService` / `pending_blob_deletions`). The pattern (`skillars-10-2` D1, `skillars-3-6` W3, `skillars-3-10` D2, `skillars-deferred-15` D1, `skillars-6-3` W3, `skillars-deferred-89` SLU under-report) recurs 6× and silently drops a refund, a cancellation/expiry email, or an SLU snapshot delta. This story extracts a **reusable** outbox and wires it onto **all three**: refunds (AC2), notification emails (AC3), and the SLU snapshot under-report path (AC4). Large blast radius accepted.
2. **Escrow / `CAPTURE_PENDING` — FULL design pass.** Not just an automated `CAPTURE_PENDING` timeout (`skillars-uat-3` D3, deferred-90 residual): also a design pass on **completion-gated coach payout** (`skillars-deferred-63` story creation) — fund-hold duration, interaction with `QuickCompleteTimeoutService`'s auto-completion, interaction with the dispute system (a coach still has no rebuttal *before* an automatic no-show refund fires). AC5 produces a design sub-doc first, then implements.
3. **de-DE register — FULL `Sie` consistency rewrite.** deferred-90 AC12 rewrote only the 55 `// TODO: native review` strings + backend bundles. Large untouched sections of `de-DE/index.js` (onboarding, dashboard, reviews, video) are informal `du`. AC9 rewrites every informal form to formal `Sie` so the whole bundle has one voice. AC10 additionally fixes pre-existing missing-accent fr-FR strings.
4. **Rolling-deploy bucket — ALL THREE items.** AC6 (gate `AdminAlertType.MODERATION_UNRESOLVED` / make `AdminQueueService` tolerate an unknown alert type per-row — `skillars-deferred-16` D4), AC7 (widen `MigrationConventionLintTest` to the 3 documented blind spots), **and** AC8 (retroactively make the 6 grandfathered `ACCESS EXCLUSIVE` migrations V60/V89/V94/V97/V98/V117 online-safe with follow-up `NOT VALID` + `VALIDATE CONSTRAINT` / `CREATE INDEX CONCURRENTLY` / batched-backfill migrations, or document why each is safe at any scale and leave it).

### Standing decisions recorded (not ACs)

- **`sessionManager.js` `startSessionMonitoring()` early-return-with-no-timer path** (`deferred-work.md` line 1318/1324) — project owner previously decided "leave as documented." Not revisited in this story.
- **No frontend test framework.** ~6 recorded coverage gaps (`5-4` W9, `deferred-17`/`-18`/`-30`/`-37`/`-38`/`-43` D6) depend on standing up Vitest/Vue Test Utils. Out of scope — its own initiative. Every `.vue` / `.js` change in this story is verified by ESLint + `quasar build` + code reading, per this project's established path for frontend-only changes.
- **`BookingService` hardcoded `"Unknown Player/Coach/Parent"` fallbacks** (`skillars-deferred-81`, DECIDED 2026-08-29) — stay as-is (unreachable orphaned-profile shape). AC19 only addresses the `MessagingService` sibling nit that deferred-90 explicitly deferred.
- **Two-sided disputes**, **`saveStep4` per-window timezone write**, **overnight availability windows**, **shared `stripe_customers` row across roles**, **`DrillMetadata.repDensity` primitive**, **`RadarDisplayService` skill-deactivation drop**, **40k-char ledger-line hygiene** — all previously DECIDED wont-fix / DISMISSED; not touched.

---

## Acceptance Criteria

### AC1 — A reusable transactional outbox, extracted from `PendingBlobDeletionService`.

A generic durable outbox that a producer writes **inside its own transaction** and a post-commit + scheduled drainer processes off the request path, with per-row `attempts` / `last_error` and a stuck-row ERROR alert. Modelled column-for-column and mechanism-for-mechanism on `skillars-deferred-90`'s `PendingBlobDeletionService` + `PendingBlobDeletionChunkProcessor` + `PendingBlobDeletionRepository`:

- **Table** (`outbox_messages` in `main`, or a small set of per-domain tables — dev's call, one migration): additive `CREATE TABLE` only, no FK to domain tables, no secondary index at creation. Passes `MigrationConventionLintTest` (it is a `V123+` migration, above the `V121` grandfather baseline). Columns at least: `id bigint identity PK`, `aggregate_type text`, `payload jsonb` (or typed columns per domain), `attempts int NOT NULL DEFAULT 0`, `last_error text`, `created_at timestamptz NOT NULL DEFAULT now()`.
- **Producer API:** `enqueue(...)` (called inside the business transaction) + `requestDrainAfterCommit()` (publishes a marker event inside the same transaction, so exactly one drain fires `AFTER_COMMIT`). Same two-method shape as `PendingBlobDeletionService`.
- **Drain:** a `@TransactionalEventListener(AFTER_COMMIT)` **non-`@Transactional`** listener that loops `chunkProcessor.processChunk()` until a chunk yields nothing (with a `MAX_CHUNKS_PER_DRAIN` safety stop), where `processChunk()` is a **separate bean** with `@Transactional(REQUIRES_NEW)` claiming ≤ `CHUNK_SIZE` rows via `SELECT … FOR UPDATE SKIP LOCKED` (JPA `@Lock(PESSIMISTIC_WRITE)` + `jakarta.persistence.lock.timeout = -2`), so the DB connection is released between chunks and never held across the external call. **This is the exact shape deferred-90's 3-layer code review forced onto `PendingBlobDeletionService`; do not regress to a self-invoked `@Transactional(REQUIRES_NEW) drain()`.**
- **Sweeper:** `@Scheduled(fixedDelayString = "${app.outbox.sweep-ms:300000}")` + `@SchedulerLock`; disabled under the test profile with every other job via `app.scheduling.enabled` (`infrastructure.config.SchedulingConfig`); tests call `drain()` directly. Crossing a `STUCK_ATTEMPTS_THRESHOLD` (10) logs `[OUTBOX_STUCK]` at ERROR — a row is **never dropped** (it may be money or a compliance-relevant delete).
- **Handler dispatch:** each outbox row names the operation it re-drives; the drainer dispatches to a handler that is **idempotent** (a repeat call on an already-completed operation is a documented no-op) — mirroring `QuotaService.release()` and the Stripe-refund idempotency key.
- Unit tests for chunking / SKIP LOCKED / loop-until-empty / stuck-row alert; one `@SpringBootTest @Testcontainers` IT proving a producer-enqueue → commit → drain round-trip and a forced-failure → `attempts++` → re-drive.
- **Reference:** `src/main/java/com/softropic/skillars/platform/filestorage/service/PendingBlobDeletionService.java` (+ `…ChunkProcessor`, `…repo/PendingBlobDeletionRepository.java`), `src/main/resources/db/migration/V122__pending_blob_deletions.sql`, `docs/deployment/migration-conventions.md`.

### AC2 — The refund AFTER_COMMIT listeners go through the outbox.

Every `@TransactionalEventListener(AFTER_COMMIT)` that issues a Stripe refund now enqueues an outbox row **inside the producing transaction** and drains post-commit; a refund whose gateway call fails stays in the outbox with `attempts++` / `last_error` for the next drain instead of being lost with only a log line.

- Call sites: `CancellationRefundService.onBookingCancelledByAdmin` (`skillars-10-2` D1), `onBookingCancelledByCoach`, `onCoachNoShow`, and any sibling listener in `platform.payment` following the same `REQUIRES_NEW` refund shape (grep `@TransactionalEventListener` in `platform.payment` — enumerate them in Dev Notes).
- The refund handler must be idempotent against a re-drive: reuse the Stripe idempotency key already keyed on the booking/payment id; a second attempt on an already-refunded charge is a no-op returning success.
- Regression IT: a listener whose refund call throws once, then succeeds on the next drain — the refund lands exactly once, the booking's terminal state is unchanged.
- **Do not** change the synchronous cancellation/refund paths (only the post-commit listeners).

### AC3 — The notification AFTER_COMMIT listeners go through the outbox.

`BookingEmailListener` (`skillars-3-6` W3) and `SessionPackEmailListener` (`skillars-3-10` D2, `skillars-deferred-15` D1) — the cancellation and pack-expiry-warning emails currently discarded on an `AFTER_COMMIT` listener failure (or on `fallbackExecution = false` when the publish happens outside a transaction) now go through the outbox.

- Keep `SessionPackExpiryNotifier.expiryWarnedAt` / the forfeiture-scheduler dedupe stamp — the "up to 14 warning emails per pack" behaviour (`skillars-deferred-15` implementation note) is what appears the moment delivery becomes reliable; the dedupe column must stay load-bearing.
- Email send is idempotent-enough: a duplicate send on a re-drive is acceptable (better than a lost notification), but the dedupe stamp should prevent the common case.
- Regression IT per listener: send fails once → re-driven on next drain → exactly one email (or an acceptable duplicate, asserted explicitly) → the `AFTER_COMMIT`-drop no longer occurs.

### AC4 — SLU weekly-snapshot under-report is reconciled.

`skillars-deferred-89` residual (`deferred-work.md` line 1288): detail rows saved, `SnapshotPersistenceRetrier.writeAllWithRetry` then exhausts its retries and its `@Recover` logs "rows lost" and returns — `player_slu_weekly_snapshot` **under-reports** for that session with no reconciliation.

- Wire the failed snapshot write onto the outbox (re-drive `writeAllWithRetry` for that `(session, isoYear, isoWeek)` bucket), **or** add a reconciliation sweep that, for a session with `player_skill_stats` rows but **no** matching `player_slu_weekly_snapshot_applied` marker for its bucket, recomputes and applies the missing `total_slu` delta (the `V119` marker table makes this idempotent).
- Must not re-introduce the *over*-report direction `skillars-deferred-89` AC2 closed (the snapshot write is skipped on `ALREADY_PERSISTED`; a reconciliation must respect the marker).
- IT: force `writeAllWithRetry` to `@Recover`, assert the snapshot is `0` for the bucket, run the outbox drain / reconciliation, assert `total_slu` now equals `SUM(player_skill_stats.slu_value)` for that session and the marker is present exactly once.

### AC5 — Completion-gated coach payout + `CAPTURE_PENDING` automated exit (escrow design pass).

**Task 0 of this AC is a design sub-doc** (`docs/architecture/payout-and-capture-pending.md` or an ADR) reviewed with the project owner before implementation, covering: fund-hold duration; the `BookingCompletedEvent` vs parent-confirmation vs `QuickCompleteTimeoutService` auto-completion signal that releases payout; interaction with the dispute system (whether a coach gets a rebuttal window *before* an automatic `NO_SHOW_COACH` refund fires); Stripe Connect settlement timing vs in-app completion. Then:

- **(a) `CAPTURE_PENDING` automated exit.** A `CAPTURE_PENDING` `booking_payments` row older than `${app.payment.capture-pending.max-hours}` transitions to a terminal state (`CHARGE_FAILED` or a new `CAPTURE_ABANDONED`) + emits `booking.payment_pending.unrecoverable{reason="CAPTURE_TIMEOUT"}`, **releasing the coach's slot** and unblocking the parent's cancel (`skillars-uat-3` D3, deferred-90 line 1325). No automatic charge/confirm — only a human can read the Stripe side, per uat-3's own reasoning; this AC bounds the *slot-hold* harm, it does not resolve the payment.
- **(b) Completion-gated payout.** Coach payout release is gated on a completion signal (per Task 0's decision) rather than settling independently at booking confirmation (`skillars-deferred-63` line 1189). Interacts with `QuickCompleteTimeoutService` (`src/main/java/com/softropic/skillars/platform/booking/service/QuickCompleteTimeoutService.java:36-61`) and `BookingService.recordNoShowCoach` (`:739-758`, which the project owner previously kept `UPCOMING`-only).
- Scope the ACs that fall out of Task 0's design; anything Task 0 identifies as needing its own story is filed to `deferred-work.md` under this story's residuals section (AC20), not implemented here.

### AC6 — Rolling-deploy: `AdminQueueService` tolerates an unknown `AdminAlertType` per-row.

`skillars-deferred-16` D4: an older instance reading a row carrying the newer `AdminAlertType.MODERATION_UNRESOLVED` value hits `Enum.valueOf` and 500s the **whole** `GET /api/admin/queue` and `/queue/summary` page, not just the affected row.

- `AdminQueueService.buildSummary` (`AdminQueueService.java:50-58,128-133`) — an unrecognised `admin_alerts.alert_type` value **skips that row with a WARN**, not `Enum.valueOf` on the request thread. The page renders every alert it *can* map.
- This is the general "enum widening one release ahead of the first write" rule from `docs/deployment/migration-conventions.md` applied to the one place a widened enum is *read*; audit `platform.admin` for any other `Enum.valueOf` on a DB string and apply the same tolerance.
- IT: seed an `admin_alerts` row with a bogus `alert_type`, assert `/api/admin/queue` returns 200 with the other rows and a WARN was logged.

### AC7 — Rolling-deploy: `MigrationConventionLintTest` covers its 3 documented blind spots.

`docs/deployment/migration-conventions.md` § "What the guard cannot catch" (deferred-90 AC10, line 1329) names three:

- **A backported migration below the `V121` grandfather baseline.** Flag any `V<n>__*.sql` where `n <= GRANDFATHER_BASELINE` that is **new in this commit** (not in `git show HEAD:…` / not otherwise present) — a backport past the baseline is how a `DROP` evades the guard. (`MigrationLint` already grew a `LOOKS_VERSIONED` / decimal-version rule in deferred-90's 3-layer review — extend the same class.)
- **An `R__` repeatable migration** containing `DROP` / a validating constraint / a blocking `CREATE INDEX`.
- **An inline `ALTER TABLE … ADD COLUMN … REFERENCES x(y)` FK** (no literal `ADD CONSTRAINT` text) added without `NOT VALID`.
- Fixtures under `src/test/resources/migration-lint/{valid,invalid}/`; update the conventions doc's blind-spot list to reflect what is now covered.

### AC8 — Rolling-deploy: the 6 grandfathered `ACCESS EXCLUSIVE` migrations made online-safe (or documented safe).

For each of **V60, V89, V94, V97, V98, V117** (`deferred-work.md`: `skillars-6-6` W3, `skillars-11-3` D1/D2, `skillars-uat-3` D13, `skillars-deferred-33`, `skillars-deferred-40`, `skillars-deferred-84`):

- Read the migration. If its target table can grow large in production and it takes a validating `ACCESS EXCLUSIVE` lock (`DROP CONSTRAINT`/`ADD CONSTRAINT`, a non-`CONCURRENTLY` `CREATE INDEX`, an unbatched full-table `UPDATE` backfill), add a **new** `V123+` migration that re-does the operation online-safely: `ADD CONSTRAINT … NOT VALID` now + `VALIDATE CONSTRAINT` in a later migration; `CREATE INDEX CONCURRENTLY` outside a transaction; a batched backfill. The already-applied migration is immutable — the fix is additive.
- If the table is genuinely small at any realistic scale (`platform_config`, a config-widen CHECK on a tiny table), **document why in the grandfather list** in `docs/deployment/migration-conventions.md` and leave it. Per-migration disposition table in this story's Dev Notes.
- No behaviour change — the constraints/indexes end up identical, just installed without a long lock.

### AC9 — i18n: full de-DE formal-`Sie` consistency pass.

`src/frontend/src/i18n/de-DE/index.js` (`deferred-work.md` line 1353): rewrite **every** informal `du` / `dein` / `deine` / `dich` / `dir` / imperative `-e`/`-` verb form to the formal `Sie` / `Ihr` / `Ihre` / `Ihnen` / `-en Sie` across the whole bundle (onboarding, dashboard, reviews, video — the sections deferred-90 AC12 did not touch).

- One consistent register end-to-end. Grep `\bdu\b|\bdein|\bdich\b|\bdir\b|\bDeine?\b|Lass |schau ` for the sweep surface; record the changed-string count.
- `MessageBundleParityTest` (deferred-90 AC12) already enforces key-set + `{placeholder}` / `|`-arity parity for DE and FR — it must stay green (this AC changes values, not keys).
- Do **not** touch the 4 `AC11`-excluded machine-format sites from deferred-90 (they carry no user-facing German).
- Residual (filed to AC20): a native-German-speaker review of the resulting wording is still owed — this AC delivers a consistent register, not a certified translation.

### AC10 — i18n: fr-FR native-quality accent pass.

`src/frontend/src/i18n/fr-FR/index.js` + `src/main/resources/i18n/messages_fr.properties`: fix pre-existing un-accented strings — `'Session actualisee avec succes'` → `'Session actualisée avec succès'`, `'Votre session a expire'` → `'Votre session a expiré'`, and a full sweep for missing `é`/`è`/`à`/`ê`/`ç` on French words. `MessageBundleParityTest` stays green.

### AC11 — N+1: measure, then batch, `getPublicProfile`.

`CoachProfileService.getPublicProfile` (`skillars-2-3`, `deferred-work.md` line 734/1326) fires ~8 sequential single-row round-trips. The ledger re-evaluated this as "not a classic N+1 — once per single-coach page view, wait for real latency evidence."

- **Task 1: measure.** Add a query-count assertion (Hibernate statistics via an IT, or datasource-proxy) over `getPublicProfile` on a realistic fixture. Record the actual round-trip count.
- If it is `> 4` independent round-trips per call, collapse via `@EntityGraph` / batched fetches / a projection so the count is bounded and independent of how much the profile contains. If it is genuinely ~2–3 cheap indexed reads, **do not** batch speculatively — record the measurement in Dev Notes and file "leave as-is, measured" to AC20.
- Whatever is done, the endpoint's response body is byte-for-byte unchanged (a parity IT).

### AC12 — Bug: `ErrorLog.logError` crashes inside the exception handler on a `%` in the message.

`src/main/java/com/softropic/skillars/infrastructure/message/ErrorLog.java:44` (`deferred-work.md` line 1347): `String.format(msgTemplate + " SUPPORT_ID: %s", helpCode)` where `msgTemplate` is frequently `ex.getMessage()` (passed at ~20 `ApiAdvice` call sites). An exception message containing a `%` (user input echoed by a validation message, a driver error) throws `UnknownFormatConversionException` / `MissingFormatArgumentException` **inside the handler** → a bare 500 with no `ErrorDto` body. Same failure class as `skillars-deferred-90` AC1's NPE.

- Fix: log the template as a parameterised argument, not a format string — `log.error("{} SUPPORT_ID: {}", msgTemplate, helpCode, entries(ctx), throwable)` (and the WARN variant). Removes the `%` hazard entirely.
- Unit test: an exception whose `getMessage()` contains `"%s"` / `"100% failed"` still produces a clean `ErrorDto` with a `helpCode`, no exception escapes.
- Check both `ErrorLog.logError` and `ErrorLog.logExpected`.

### AC13 — Bug: the GDPR video-cascade delete runs with no transaction.

`deferred-work.md` line 1355: `VideoDeletionService.cascadeDeleteForAccount:186` throws `jakarta.persistence.TransactionRequiredException: Executing an update/delete query` on the account-deletion cascade path (`AccountDeletionCascadeListener.onAccountDeleted:46`, reached from `GdprErasureService.erase()` via `AccountDeletionRequestedEvent`). Caught + logged upstream so `GdprErasureIT` stays green — a silently-failing GDPR cascade delete is a **compliance issue**, not just log noise.

- **Task 1: reproduce and diagnose.** Run a clean `GdprErasureIT`; capture the exception. Determine whether the `@Modifying` cascade is *silently not deleting the video rows* or just noisy. Grep every caller of `cascadeDeleteForAccount` and every `@Modifying` query it issues.
- Fix: `@Transactional` on `AccountDeletionCascadeListener.onAccountDeleted` (or on `cascadeDeleteForAccount`), matching how the `@TransactionalEventListener` needs a surrounding tx for `@Modifying`. Confirm no propagation conflict with `GdprErasureService.erase()`'s own `@Transactional`.
- IT: run an erasure for an account with videos; assert the `videos` (and `video_approval_requests`, `video_quotas`, whatever the cascade covers) rows are **actually gone** afterwards, and no `TransactionRequiredException` is logged.

### AC14 — Bug: cross-tab logout is invisible when the backend `/logout` stalls.

`deferred-work.md` line 1351 (deferred-90 3-layer review): the AC3 fast-teardown branch in `sessionManager.js` `computeTimeUntilExpiry()` needs `rint` absent, but nothing on the client clears `rint` — it is removed only by the backend logout `Set-Cookie`, which is `Promise.race`d against `LOGOUT_BACKEND_WAIT_MS`. If that request stalls / errors, sibling tabs keep `rint` and keep rendering an authenticated UI until the stale `rint` deadline fires.

- Fix: `handleLogout` (`src/frontend/src/composables/useSession.js:71-90`) clears the `rint` cookie client-side (same `document.cookie` expiry write it already does for `user` / `skp`), so a sibling tab's tick enters the fast-teardown branch regardless of the backend call's fate.
- ESLint + build only (no frontend test framework). Verify by code-reading the tick gate: `checkIntervalId !== null && hasSeenRintThisTab() && !hasUserSession()` still holds once `rint` is gone.

### AC15 — Gap: `permitAll()` path patterns match same-segment siblings.

`src/main/java/com/softropic/skillars/platform/security/config/AppEndpoints.java` (`deferred-work.md` line 1298): `resend-otp**`, `register**`, `verify-email**` have no `/` before `**`, so `/api/security/coach/resend-otp**` also matches `…/resend-otp-admin` — a future controller under that prefix is silently `permitAll()` with no review step.

- Audit **every** `PUBLIC_ENDPOINTS` / `permitAll()` pattern in `AppEndpoints`. Anchor each so it cannot match a same-segment sibling: `resend-otp/**` (+ an exact `resend-otp` entry if the bare path is a real endpoint), or an exact pattern. `PathPatternRequestMatcher` semantics: a trailing `/**` does not cross `/`, an in-segment `**` does.
- Add a convention test asserting no `permitAll()` pattern in `AppEndpoints` ends in a non-`/`-preceded `**`.
- Verify no currently-public endpoint's path becomes non-matched by the tighter pattern (list them in Dev Notes).

### AC16 — Gap: de-triplicate `resendPhoneOtp`.

`ParentRegistrationService.java:226-252`, `CoachRegistrationService.java:230-248`, `PlayerRegistrationService.java:256-274` (`deferred-work.md` line 1297) are byte-identical bar the `@RateLimited` key and the OTP-email event type. Extract to one shared collaborator (`RegistrationOtpResendSupport` in `platform.security.service`, injected into all three) parameterized by the role rate-limit key + an OTP-email-event factory. Behaviour byte-identical; the existing per-service ITs (`CoachRegistrationResourceIT`, `PlayerRegistrationResourceIT`, parent's) stay green. Remove the "mirrors parent exactly" comments the extraction makes true by construction.

### AC17 — Bug: `IllegalStateException` → 500, not 409, for a missing config key.

`ApiAdvice`'s `illegalStateExceptionHandler` (`deferred-work.md` line 812, `skillars-1-1`): a missing / non-numeric `platform_config` key surfaces as `IllegalStateException` → currently `409 CONFLICT`, which is a server misconfiguration masquerading as a client conflict and hides it from 5xx alerting. Map it to `500` (with an ERROR log carrying a `helpCode`, via `ErrorLog`). Audit every `throw new IllegalStateException` reachable by a request to confirm none legitimately means "409" (grep `platform` — `ConfigService.getBoundedLong` / `getLong` / `getString` are the expected sources).

### AC18 — Gap: `app.bootstrap.jwt-secret.enabled` gets an enforced guard.

`JwtSecretBootstrapRunner` (`deferred-work.md` line 1252, `skillars-deferred-83`): the flag "MUST stay unset in every real environment" is enforced only by a javadoc comment. Add a fail-fast at startup — e.g. require a co-located `app.bootstrap.jwt-secret.i-understand-dev-only=true`, or refuse to run when `spring.datasource.url` does not target `localhost`/`127.0.0.1` (dev's call, record the signal chosen). A misconfigured non-dev deploy that sets the flag `true` must **fail to boot**, not silently seed a known secret. `application-dev.yaml` updated so dev still works.

### AC19 — Nit: `MessagingService.buildSummaryContext` player-name dedup.

deferred-90's deferred nit: `buildSummaryContext` inlines `playerProfileRepository.findAllById(...).forEach(...)` where `PlayerProfileService.getPlayerNamesByPlayerIds` already does exactly that. Do the single deliberate pass over all six `MessagingService → PlayerProfileRepository` direct call sites — route the ones that want "id → name" through the shared method; for each that stays direct, add a one-line comment saying why (cross-module dep, different projection, etc.). Keep the null-tolerant collector deferred-90 added.

### AC20 — `deferred-work.md` ledger hygiene + `sprint-status.yaml`.

Delete every item this story closes (per the file's own "delete outright, don't tag" convention). Add `## Deferred from: skillars-deferred-91 story creation (2026-09-03)` with the residuals: SLU non-gating perf-trend signal (`deferred-89` line 1290, still not built); the standing frontend-test-framework gap; the de-DE native-speaker verification still owed after AC9; anything AC5 Task 0 spins out as its own story; `getPublicProfile` "leave as-is, measured" if AC11's measurement says so; the `AFTER_COMMIT` reliability catalogue entries not covered (if any listener is deliberately left un-migrated, say which and why). Flip `skillars-deferred-91-*` to `done` in `sprint-status.yaml` on completion (dev sets it to `review` first, per the workflow).

---

## Tasks / Subtasks

- [x] **Task 0 — Pre-implementation re-verification.** Confirmed: `ErrorLog.java:67` still `String.format`s `msgTemplate + " SUPPORT_ID: %s"` (AC12); `GdprErasureIT` reproduces `TransactionRequiredException` at `VideoDeletionService.cascadeDeleteForAccount:186` via `jdk.proxy2.$Proxy385.resetBytesForOwner` — the `@Modifying @Transactional` repo method is NOT honoured on the AFTER_COMMIT cascade path, and an inner REQUIRED tx during `afterCommit` deadlocks the follow-up approval-cancel (AC13); `@TransactionalEventListener` inventory captured (`platform.payment`: `CancellationRefundService` x5, `PaymentLifecycleService` x2 — AC2); 6 grandfathered migrations present: V60/V89/V94/V97/V98/V117 (AC8); `AppEndpoints.PUBLIC_ENDPOINTS` still had bare `foo**` patterns (AC15); `ApiAdvice.illegalStateExceptionHandler` still mapped to `409` (AC17); `resendPhoneOtp` byte-identical across parent/coach/player bar the rate-limit key + OTP-email event (AC16); `AdminAlert.type` `@Enumerated(STRING)` with a widened `admin_alerts_type_check` CHECK is the one rolling-deploy enum-read hazard in `platform.admin` (AC6); `MigrationLint` blind-spot list matched the doc (AC7).
- [x] **Task 1 (AC1)** — reusable outbox delivered in `com.softropic.skillars.platform.outbox`: `V123__outbox_messages.sql` (additive `jsonb` table, lint-clean), `OutboxMessage` + `OutboxMessageRepository` (`claimNextChunk` = `@Lock(PESSIMISTIC_WRITE)` + `lock.timeout=-2` SKIP LOCKED, `ORDER BY attempts, id`), `OutboxChunkProcessor` (separate bean, `@Transactional(REQUIRES_NEW)`, `CHUNK_SIZE=25`, handler-map dispatch, unknown type = failure not drop), `OutboxService` (`enqueue`/`requestDrainAfterCommit`/`drain` loop-to-empty + `MAX_CHUNKS_PER_DRAIN`, non-`@Transactional` AFTER_COMMIT listener, `@Scheduled @SchedulerLock` sweeper with `[OUTBOX_STUCK]` ERROR at 10 attempts), `OutboxMessageHandler` SPI, `OutboxDrainRequestedEvent`. `OutboxChunkProcessorTest` (5) + `OutboxServiceTest` (6) + `SluSnapshotOutboxIT` (3, real Postgres round-trip). Did NOT touch `PendingBlobDeletionService` (just-shipped, per Dev Notes).
- [x] **Task 2 (AC2) — done (owner decision: wire it onto the outbox + idempotency guard).** Task 0 established AC2's literal premise does not hold (no `@TransactionalEventListener(AFTER_COMMIT)` issues a Stripe refund; the refund path is the in-app credit wallet — see § "AC2 disposition"). Per the owner: `V125__parent_credit_ledger_reference_dedup.sql` adds `uq_pcl_reference_type` (partial unique on `(reference_id, type) WHERE reference_id IS NOT NULL`); `ParentCreditLedgerRepository.existsByReferenceIdAndType`; `RefundOutboxSupport.enqueueBookingRefund(...)` replaces all 6 `creditWalletService.writeLedgerEntry("BOOKING_REFUND", …)` calls in `CancellationRefundService`'s 4 listeners; `CreditWalletRefundOutboxHandler` re-drives idempotently (existence-check first, unique-index backstop). `RefundOutboxIT` (+2, exactly-once through the outbox), `CancellationRefundMatrixTest` (12) + `PackCancellationRefundIT` (4) updated & green.
- [x] **Task 3 (AC3) — done (owner decision: wider scope — every listener method).** `NotificationOutboxSupport.enqueueEmail(...)` (`@Transactional(REQUIRES_NEW)` seam) + `NotificationEmailOutboxHandler` (`NOTIFICATION_EMAIL` → `mailManager.sendEmailSync`). Every `publisher.publishEvent(new Envelope(...))` in `BookingEmailListener` (**19**) and `SessionPackEmailListener` (**3**) now enqueues instead — closing the silent-drop where `MailManager`'s `fallbackExecution=false` AFTER_COMMIT listener never fires for an `Envelope` published from a no-tx AFTER_COMMIT context. `expiryWarnedAt` / forfeiture dedupe stamps untouched. A re-drive re-sends (acceptable per AC3). `BookingEmailListenerTest` (11) + `SessionPackEmailListenerTest` (2) rewritten to the new seam; `SessionPackExpiryWarningIT` / `MailManagerIT` / booking-flow ITs green.
- [x] **Task 4 (AC4)** — `SnapshotPersistenceRetrier`'s two `@Recover` methods now call `SluSnapshotOutboxSupport.enqueueFailedSnapshotWrite(...)` (new, `@Transactional(REQUIRES_NEW)`, serialises the deltas + `enqueue("SLU_SNAPSHOT_WRITE", json)` + `requestDrainAfterCommit`) instead of only logging the loss. `SluSnapshotOutboxHandler` re-drives via `SnapshotBatchWriter.writeAllDeltas(...)` (new detached-value form of `writeAll`), idempotent through the `V119` marker — no over-report (proven by `SluSnapshotOutboxIT.redrivingTheSamePayload_isANoOpThroughTheV119Marker_noOverReport`). `SnapshotPersistenceRetrierTest` / `SluRetrierProxyRetryTest` updated for the new constructor dep + a `verify(...enqueueFailedSnapshotWrite...)`.
- [x] **Task 5 (AC5) — design doc reviewed; Part A implemented; Part B filed to AC20 as its own story (owner D4).** `docs/architecture/payout-and-capture-pending.md` written. **Correction (code review D8):** the doc is still marked `DRAFT` and its "Owner decisions needed" section still lists D1–D5 unanswered — the written record of that review does not exist. Part A was implemented on the owner's verbal scope call; the D1–D5 sign-off is carried into the Part B story. **Part A shipped:** `BookingPaymentStatus.CAPTURE_ABANDONED` (terminal); `V124` widens `chk_bp_status` (documented `migration-lint` opt-out per D2) + adds nullable `reserved_at` + widens `status` to `VARCHAR(20)`; `BookingPaymentPersistenceService.reserveCapture` stamps `reserved_at`; `PaymentPendingSweeper` ages a `CAPTURE_PENDING` row past `booking.payment_pending.capture_pending_max_hours` (config, default 72h per D3) → `CAPTURE_ABANDONED` + `PAYMENT_FAILED` transition (slot freed, parent cancel unblocked) + `booking.payment_pending.unrecoverable{reason="CAPTURE_TIMEOUT"}` + `[CAPTURE_TIMEOUT]` ERROR; `runbook.md` § CAPTURE_ABANDONED. `PaymentPendingSweeperTest` (+3) / `PaymentPendingSweeperIT` (+1) green. **Part B (completion-gated payout)** — the current destination-charge model pays the coach at capture; real gating needs separate charges & transfers (B-1), filed to AC20 as its own story with the design doc as input.
- [x] **Task 6 (AC6)** — `AdminQueueService.buildSummary` skips an unknown `alert_type` with a WARN; audit `platform.admin` for sibling `Enum.valueOf`-on-DB-string; IT with a bogus `alert_type` row.
- [x] **Task 7 (AC7)** — 3 new `MigrationLint` rules (backport-below-baseline, `R__` DROP/validate, inline `ADD COLUMN … REFERENCES`) + fixtures; update `migration-conventions.md` blind-spot list.
- [x] **Task 8 (AC8)** — per-migration disposition of V60/V89/V94/V97/V98/V117: online-safe follow-up migration where the table can grow; documented-safe otherwise. Disposition table in Dev Notes.
- [x] **Task 9 (AC9)** — de-DE `du` → `Sie` bundle-wide rewrite; changed-string count; `MessageBundleParityTest` green.
- [x] **Task 10 (AC10)** — fr-FR accent sweep (frontend + `messages_fr.properties`); parity test green.
- [x] **Task 11 (AC11)** — measure `getPublicProfile` round-trips (Hibernate statistics IT); batch via `@EntityGraph`/projection only if `> 4`; response-parity IT; record the measurement.
- [x] **Task 12 (AC12)** — `ErrorLog` `logError` + `logExpected` → parameterised logging; unit test with a `%`-bearing message.
- [x] **Task 13 (AC13)** — reproduce + diagnose the GDPR cascade; `@Transactional` on the listener/method; IT asserting the video rows are actually deleted.
- [x] **Task 14 (AC14)** — `handleLogout` clears `rint` client-side; ESLint + build.
- [x] **Task 15 (AC15)** — anchor every `AppEndpoints` `permitAll()` pattern; convention test; list affected paths.
- [x] **Task 16 (AC16)** — extract `RegistrationOtpResendSupport`; three services delegate; existing ITs green.
- [x] **Task 17 (AC17)** — `IllegalStateException` → 500 in `ApiAdvice`; audit reachable `throw new IllegalStateException` sites.
- [x] **Task 18 (AC18)** — enforced boot guard on `app.bootstrap.jwt-secret.enabled`; `application-dev.yaml` still boots.
- [x] **Task 19 (AC19)** — `MessagingService` six-call-site dedup pass.
- [x] **Task 20 (AC20)** — `deferred-work.md` deletions + `skillars-deferred-91` residuals section; `sprint-status.yaml`.
- [x] **Task 21 — Verification gate.** Backend: targeted `-Dtest` green per touched module + the outbox/refund/notification/SLU ITs. Frontend: `eslint` + `quasar build`; `prettier --check` on touched files (note pre-existing failures, do not widen). No local `mvn verify` ([[feedback_no_local_mvn_verify]]) — GitHub CI (`mvn -B verify` + Trivy + Docker + context-count gate) is the full-suite gate. **Context-count gate:** any new `@SpringBootTest` IT must extend `AbstractIntegrationTest` with **no** class-level `@TestPropertySource` / `@ActiveProfiles` / extra `@MockitoBean` — `IntegrationTestConventionTest` pins the context count at 37 (see deferred-90's `MessagingConversationSummaryBatchingIT`).

### Review Findings

<!-- Added 2026-09-03 by bmad-code-review (3 layers: Blind Hunter, Edge Case Hunter, Acceptance Auditor;
     all 3 completed). Diff reviewed: uncommitted working tree + 29 untracked files vs HEAD 120bc5f,
     73 files / ~4,624 changed lines. 12 decision-needed, 16 patch, 2 deferred, 2 dismissed.
     Every finding below was independently verified against the working-tree source before listing. -->

#### Decisions needed

- [x] [Review][Decision] **A failed email is deleted from the outbox as if it were delivered** — `MailManager.sendEmailSync` (`MailManager.java:106`) catches `Exception` and never rethrows; it records `EnvelopeEntity.status = FAILED` and returns normally. `NotificationEmailOutboxHandler.handle` treats a normal return as success, so `OutboxChunkProcessor` counts the row `processed` and calls `repository.delete(row)`. SMTP down, circuit breaker open, retries exhausted, bad address — all silently drop the message with `attempts` still 0 and `[OUTBOX_STUCK]` unreachable. **AC3's entire durability claim does not hold**: the outbox only survives a JVM death *before* the handler runs, never a send failure. Decision: throw on `status == FAILED` unconditionally (retries forever, incl. permanently-undeliverable addresses), or throw only when `isRetry()` is true and treat non-retryable as terminal-logged. [`NotificationEmailOutboxHandler.java:48`] **→ DECIDED 2026-09-03:** Throw on FAILED honouring `isRetry()` — re-read the persisted `EnvelopeEntity` by `sendId`; `FAILED && isRetry()` throws so the row is re-driven, `FAILED && !isRetry()` logs a distinct ERROR marker and is allowed to complete.
- [x] [Review][Decision] **A handler's DB failure poisons the whole chunk transaction** — `CreditWalletService.writeLedgerEntry` (`:36`) and `SnapshotBatchWriter.writeAllDeltas` (`:41`) are plain `@Transactional` (REQUIRED), so they join `processChunk()`'s `REQUIRES_NEW` transaction. Any `DataIntegrityViolationException` — including the `uq_pcl_reference_type` violation that `CreditWalletRefundOutboxHandler`'s own javadoc names as "the backstop for the concurrent case" — aborts the PG transaction, so the catch block's `repository.save(row)` cannot execute. Result: `attempts++`/`last_error` never commit, up to 24 sibling rows are un-deleted and un-processed, and the chunk ends in `UnexpectedRollbackException`. The original `PendingBlobDeletionChunkProcessor` was safe because its work unit was a pure S3 call touching no JDBC connection — that is the safety property the generalization dropped. Decision: dispatch each handler in its own nested `REQUIRES_NEW`, or record failure bookkeeping in a separate short transaction. [`OutboxChunkProcessor.java:66-85`] **→ DECIDED 2026-09-03:** Per-row `REQUIRES_NEW` dispatch — `processChunk` claims the chunk, then dispatches each row through a separate bean method so a poisoned row rolls back alone.
- [x] [Review][Decision] **The AFTER_COMMIT drain runs synchronously on the request thread** — `onOutboxDrainRequested` has no `@Async`, so `drain()` executes on the committing thread for up to `MAX_CHUNKS_PER_DRAIN (200) × CHUNK_SIZE (25) = 5,000` rows, each a blocking `sendEmailSync` **inside** `processChunk`'s open transaction. Deepest point of one booking request holds 4 connections from a 25-connection pool (business tx + `enqueueEmail` REQUIRES_NEW + `processChunk` REQUIRES_NEW + `sendEmailSync` REQUIRES_NEW). A backlog accumulated during an SMTP outage is drained entirely by whichever user commits next. Contradicts AC1's "off the request path" and "the connection is never held across the external call". Decision: async executor, hard per-drain cap, or leave the drain to the scheduled sweeper only. [`OutboxService.java:68-70, 96-115`] **→ DECIDED 2026-09-03:** Move the drain onto a dedicated bounded `@Async` executor; the 5-minute `@Scheduled` sweep stays as the safety net.
- [x] [Review][Decision] **`uq_pcl_reference_type` constrains ledger types outside AC2's scope and is unmapped in `ApiAdvice`** — the index binds every reference-carrying `(reference_id, type)` pair, not just `BOOKING_REFUND`. Existing writers that reuse the pair: `DisputeService.java:206/219/229` (`BOOKING_REFUND` on the same booking already refunded at cancellation), `BookingPaymentPersistenceService.java:200/229` and `PaymentLifecycleService.java:326/338` (`BOOKING_DEDUCTION` / `..._REVERSAL` — a re-attempt after a reversal collides). The constraint name appears in neither `CONSTRAINT_MAPPINGS` (`ApiAdvice.java:135`) nor `CONFLICT_CONSTRAINTS` (`:145`), so it surfaces as an untranslated 400 `generic.dataError` and silently rolls back the enclosing dispute resolution. Decision: is a second `BOOKING_REFUND` / a deduction retry legitimate? That determines whether to narrow the index predicate to `type = 'BOOKING_REFUND'` or to map the constraint and pre-check on the dispute path. [`V125__parent_credit_ledger_reference_dedup.sql:16`] **→ DECIDED 2026-09-03:** Narrow the index and map the constraint — recreate it as `WHERE reference_id IS NOT NULL AND type = 'BOOKING_REFUND'`, and register the name in `ApiAdvice`.
- [x] [Review][Decision] **AC3 enqueues *after* the producing commit, inside a swallowed catch-all** — `NotificationOutboxSupport.enqueueEmail` is `@Transactional(REQUIRES_NEW)` and its callers (`BookingEmailListener`, `SessionPackEmailListener`) are themselves `@TransactionalEventListener(AFTER_COMMIT)`. The business transaction has already committed, so the outbox row is written in a *separate* transaction whose failure is caught by the listener's pre-existing `catch (Exception e) { log.error(...) }` — the exact silent-drop AC3 exists to remove, moved rather than closed. AC1 requires `enqueue()` inside the producing transaction (which AC2's `RefundOutboxSupport` does correctly). `BookingEmailListener.onBookingReminder` (`:512-527`) has no try/catch at all. Decision: accept the reduced guarantee and document it, or move the enqueue into the producing transactions across the 22 sites. [`NotificationOutboxSupport.java:45`] **→ DECIDED 2026-09-03:** Accept the reduced guarantee and document it — correct the AC3 claim and the `NotificationOutboxSupport` javadoc, add the missing try/catch on `onBookingReminder`, and file the full in-transaction fix to `deferred-work.md`.
- [x] [Review][Decision] **Poison rows are immortal — no attempt cap, no backoff, no quarantine** — `claimNextChunk` has no `WHERE`, no `next_attempt_at` column and no cap; `ORDER BY attempts ASC` only demotes a failing row relative to *fresher* work, so with fewer than 25 rows in the table a permanently failing row is in every chunk. A removed/renamed `EmailTemplate` (`NotificationEmailOutboxHandler.java:51` `EmailTemplate.valueOf`) or an `aggregate_type` with no registered handler is retried forever, on the request thread, with zero delay. The design's "never dropped" rule leaves an operator no remedy but a manual `DELETE`. Decision: add `next_attempt_at` exponential backoff, a quarantine status, or both. [`OutboxMessageRepository.java:31`] **→ DECIDED 2026-09-03:** Add a `next_attempt_at` backoff column (new migration) and filter `claimNextChunk` on it, with exponential backoff derived from `attempts`.
- [x] [Review][Decision] **AC8 was answered with a disposition AC8 does not offer** — AC8 permits exactly two outcomes per migration: an online-safe redo, or "document it if the table is genuinely small at any realistic scale." The shipped table concedes the growth condition for three of the six and still documents-and-leaves: `V60` ("the `main.videos` CHECK re-add **would** full-scan under `ACCESS EXCLUSIVE` at scale"), `V94` ("**Yes** — validates the whole table under `ACCESS EXCLUSIVE`"), `V117` ("**Yes** — the FK `ADD CONSTRAINT` validates under `ACCESS EXCLUSIVE`"). The justification used ("no production system exists") would have voided the AC at authoring time. `V89`/`V97`/`V98` are legitimately catalog-only. Decision: accept the third disposition and amend AC8, or write the three follow-up migrations. [`docs/deployment/migration-conventions.md:87-104`] **→ DECIDED 2026-09-03:** Accept the third disposition and amend AC8 — record pre-production grandfathering as permitted, with an explicit trigger that V60/V94/V117 must be redone online-safe before the first production deploy.
- [x] [Review][Decision] **AC5's design doc is still `DRAFT` with D1–D5 unanswered, while the story records the owner review as complete** — `docs/architecture/payout-and-capture-pending.md:3` reads "**Status:** DRAFT — awaiting project-owner review (skillars-deferred-91 AC5, Task 0)" and its closing section still lists "**Owner decisions needed:** D1 … D5" (`:232-239`). Meanwhile Task 5, the Completion Notes, the Change Log v0.3 entry and `sprint-status.yaml` all state the review happened and that D4/D5 were "recorded there". They are recorded nowhere. Decision: answer D1–D5 and flip the doc to a decided state, or correct the story's claims. [`docs/architecture/payout-and-capture-pending.md:3`] **→ DECIDED 2026-09-03:** Correct the story's claims — the doc is the source of truth and says DRAFT; amend the Completion Notes, Change Log v0.3 and the AC20 residuals entry, and carry sign-off into the Part B story.
- [x] [Review][Decision] **AC11 measured 8 round-trips — above its own `> 4` collapse threshold — then declined to collapse** — AC11 says "If it is `> 4` independent round-trips per call, collapse via `@EntityGraph` / batched fetches / a projection." `CoachPublicProfileQueryCountIT.java:47` records `EXPECTED_QUERY_COUNT = 8` and the recorded decision is "left as-is" on the grounds that the count is constant rather than N+1 — which re-litigates the AC's threshold rather than applying it. `CoachProfileService` is unchanged, and AC11's required byte-for-byte response-parity IT does not exist. Decision: apply the AC and collapse, or amend AC11's threshold. [`CoachPublicProfileQueryCountIT.java:47`] **→ DECIDED 2026-09-03:** Apply the AC — collapse the seven `…ByCoachId` reads to ≤4 round-trips and add the missing byte-for-byte response-parity IT.
- [x] [Review][Decision] **`CAPTURE_ABANDONED` emails the parent "booking declined" though money may have moved** — `abandonCapture` (`PaymentPendingSweeper.java:215-227`) publishes the same `BookingDeclinedEvent` as the no-payment-row path at `:186`, which renders `EmailTemplate.BOOKING_DECLINED`. But V124's own comment draws the distinction: "`CHARGE_FAILED` asserts 'no money moved', `CAPTURE_ABANDONED` asserts 'we stopped waiting; the Stripe side is unknown'." A parent whose card was charged is told the booking was declined, with no mention of an outstanding charge, while reconciliation runs asynchronously. Decision: a distinct template/copy for the unknown-charge case, or accept the shared one. [`PaymentPendingSweeper.java:222`] **→ DECIDED 2026-09-03:** Add a distinct `BOOKING_PAYMENT_UNRESOLVED` template (en/de/fr) and publish a separate event from `abandonCapture`.
- [x] [Review][Decision] **AC17's audit claim is falsified — `IllegalStateException` → 500 breaks request-reachable conflicts** — the new handler's javadoc asserts "An audit of every request-reachable `throw new IllegalStateException` confirmed none legitimately means 409." `VideoApprovalService.java:156-158` ("Video is not in HIDDEN state — cannot approve") is reached from `VideoApprovalResource.java:77`; two parents approving concurrently, or a video that left `HIDDEN` between render and click, is a pure client-side race that now returns 500 and pollutes 5xx alerting. Same shape at `VideoApprovalService.java:68` and `VideoLifecycleService.java:154`. Note the adjacent `VideoAlreadyResolvedException` is the existing precedent for exactly this. Decision: convert these three to domain exceptions mapped to 409, or accept 500 and correct the javadoc. [`ApiAdvice.java:599-608`] **→ DECIDED 2026-09-03:** Convert the three to 409 domain exceptions, following the adjacent `VideoAlreadyResolvedException` precedent; keep `IllegalStateException` → 500 for genuine server-state errors.
- [x] [Review][Decision] **AC19's named dedup target was not deduped** — AC19 names one site and one action: route `buildSummaryContext`'s inline `playerProfileRepository.findAllById(...).forEach(...)` through `PlayerProfileService.getPlayerNamesByPlayerIds`. The diff keeps the inline loop and adds a comment declining, because that method's `Collectors.toMap` NPEs on a null name (`PlayerProfileService.java:47`). The blocker is real, but the obvious resolution — make the shared method null-tolerant, then route through it — was not taken, so the duplication AC19 exists to remove survives. Also, the completion note's claim that the other five sites "each already carries a purpose comment" does not hold for `MessagingService.java:372`. Decision: make the shared method null-tolerant and dedup, or close AC19 as wont-fix with the reason. [`MessagingService.java:430-434`] **→ DECIDED 2026-09-03:** Make `PlayerProfileService.getPlayerNamesByPlayerIds` null-tolerant, then route `buildSummaryContext` through it as AC19 instructs.

#### Patches

<!-- The first 12 come from the resolved decisions above. -->
- [x] [Review][Patch] Re-read `EnvelopeEntity` by `sendId` after `sendEmailSync` and throw when `status == FAILED && isRetry()`; distinct ERROR marker for non-retryable [`NotificationEmailOutboxHandler.java:48`]
- [x] [Review][Patch] Dispatch each outbox row through a separate bean's `@Transactional(REQUIRES_NEW)` method so a handler failure cannot roll back siblings or the `attempts++` bookkeeping [`OutboxChunkProcessor.java:66-85`]
- [x] [Review][Patch] Run `onOutboxDrainRequested` on a dedicated bounded executor so the request thread returns immediately; keep the scheduled sweeper [`OutboxService.java:68-70`]
- [x] [Review][Patch] New migration adding `next_attempt_at` to `outbox_messages` + `claimNextChunk` predicate + exponential backoff from `attempts` [`OutboxMessageRepository.java:31`]
- [x] [Review][Patch] New migration narrowing `uq_pcl_reference_type` to `type = 'BOOKING_REFUND'`; add the constraint to `ApiAdvice.CONSTRAINT_MAPPINGS` + `CONFLICT_CONSTRAINTS` for a translated 409 [`V125__parent_credit_ledger_reference_dedup.sql:16`, `ApiAdvice.java:135`]
- [x] [Review][Patch] Correct the AC3 durability claim in the story + `NotificationOutboxSupport` javadoc to state the actual guarantee; add the missing `try/catch` to `BookingEmailListener.onBookingReminder` [`NotificationOutboxSupport.java:45`, `BookingEmailListener.java:512`]
- [x] [Review][Patch] Amend AC8 to permit pre-production grandfathering; add the "redo V60/V94/V117 online-safe before first production deploy" trigger to `docs/deployment/runbook.md` [`docs/deployment/migration-conventions.md:87`]
- [x] [Review][Patch] Amend Completion Notes, Change Log v0.3 and the AC20 residuals entry so they no longer claim D1–D5 were recorded; carry the doc sign-off into the Part B story [story file]
- [x] [Review][Patch] Collapse `getPublicProfile`'s seven `…ByCoachId` reads via `@EntityGraph`/projection to ≤4 round-trips; add the AC11 response-parity IT; update `EXPECTED_QUERY_COUNT` [`CoachPublicProfileQueryCountIT.java:47`]
- [x] [Review][Patch] Add a `BOOKING_PAYMENT_UNRESOLVED` template + en/de/fr copy; publish a distinct event from `abandonCapture` so `CAPTURE_ABANDONED` never renders as a clean decline [`PaymentPendingSweeper.java:222`]
- [x] [Review][Patch] Give `VideoApprovalService:68`/`:156` and `VideoLifecycleService:154` their own domain exceptions mapped to 409 with message keys; keep the `IllegalStateException` → 500 rule [`ApiAdvice.java:604`]
- [x] [Review][Patch] Replace `PlayerProfileService.java:47`'s `Collectors.toMap` with a null-tolerant collector; route `buildSummaryContext` through the shared method; add the missing purpose comment [`MessagingService.java:430`, `PlayerProfileService.java:47`]

- [x] [Review][Patch] `drain()` has no try/catch, so an outbox failure surfaces as a 500 on an already-committed request — `afterCommit` exceptions propagate out of `commit()` to the caller [`OutboxService.java:96-115`]
- [x] [Review][Patch] `BACKPORT_BELOW_BASELINE` is inert: `git cat-file -e HEAD:<missing>` exits **128** (verified in this repo), which `gitKnownAtHead` treats as "cannot answer ⇒ known". Separately, in CI the file always exists at HEAD, so the rule can only ever fire on a dirty working tree — it needs a merge-base diff. `migration-conventions.md` now claims this blind spot is closed [`MigrationLint.java:272`]
- [x] [Review][Patch] `INLINE_FK` misses two valid PostgreSQL spellings of the same hazard: `ADD <col>` without the optional `COLUMN` keyword, and `REFERENCES tbl` without the optional column list. `DROP_NO_IF_EXISTS` matches only `TABLE|COLUMN|INDEX`, so `DROP CONSTRAINT` is unlinted [`MigrationLint.java:101-106`]
- [x] [Review][Patch] `lintRepeatable`'s DROP branch has no `-- migration-lint: allow-*` opt-out check, though its javadoc says "the `allow-*` opt-outs still apply" and the sibling blocking-index/validating-constraint branches do check [`MigrationLint.java:233`]
- [x] [Review][Patch] V124's `DROP CONSTRAINT chk_bp_status` carries no `IF EXISTS`, and its `allow-validating-constraint` marker suppresses the table-scan rule rather than the enum/CHECK-widen-one-release-ahead rule it actually deviates from (which `MigrationLint` does not implement at all) [`V124__booking_payment_capture_abandoned.sql:22-27`]
- [x] [Review][Patch] `AdminQueueService`: `toUuid(row[0])` and `toInstant(row[5])` execute outside the tolerant guard, so one malformed row still 500s the whole page — the outcome AC6 exists to prevent; `new PageImpl<>(dtos, p, raw.getTotalElements())` pairs a filtered list with an unfiltered total; and `toDtoTolerant`'s javadoc claims `reference_type` is validated when `:87` passes it through raw [`AdminQueueService.java:66, 76-100`]
- [x] [Review][Patch] The email deadline is recomputed at delivery (`Instant.now().plus(Duration.ofDays(1))` inside `handle()`) rather than stamped at enqueue, so a message that sat in the outbox for days is delivered with a fresh 24-hour deadline and any staleness guard is unreachable [`NotificationEmailOutboxHandler.java:52`]
- [x] [Review][Patch] `requestDrainAfterCommit()` fires one drain per *enqueue*, not per transaction — contradicting AC1's "exactly one drain fires". `BookingEmailListener.onRescheduleAccepted`/`onBookingReminder` loop over recipients, so N recipients trigger N full drains. `OutboxServiceTest`'s `requestDrainAfterCommit_publishesExactlyOneMarkerEvent` asserts one event per call, not per transaction [`OutboxService.java:56-58`]
- [x] [Review][Patch] `SnapshotPersistenceRetrier.recover()` logs "enqueued on the outbox for re-drive" *before* calling `enqueueFailedSnapshotWrite`, which is `REQUIRES_NEW` and needs a connection from the same pool against the same database that just exhausted the retries. Only `JsonProcessingException` is caught, so `CannotCreateTransactionException` now escapes `@Recover` — which previously could not throw [`SnapshotPersistenceRetrier.java:80-84`]
- [x] [Review][Patch] `JwtSecretBootstrapRunner`'s loopback guard uses an unanchored `find()` over the whole JDBC URL, so a pgjdbc multi-host failover URL (`jdbc:postgresql://localhost:5432,prod-db.internal:5432/skillars`) or a `//localhost` inside a query parameter passes the guard [`JwtSecretBootstrapRunner.java`]
- [x] [Review][Patch] de-DE formal-register pass is incomplete — two informal `du` imperatives survive next to converted `Sie` strings in the same flows: `noCreditsWarning: 'Kaufe ein Sitzungspaket…'` and `'…Bitte kontaktiere den Support.'` [`src/frontend/src/i18n/de-DE/index.js:296, 431`]
- [x] [Review][Patch] fr-FR accent pass is incomplete — 8 unaccented strings remain, several on context lines of the very hunks that were edited: `:66 'Prenom'`, `:82 'Creer un compte'`, `:83 "…entraineur"`, `:92 'Langue preferee'`, `:94 'Francais'`, `:130 'Creer un compte joueur'`, `:140 "…confidentialite"`, `:146 'Derniere etape'` [`src/frontend/src/i18n/fr-FR/index.js`]
- [x] [Review][Patch] AC1–AC4's mandated integration coverage is absent or tests something else: both outbox ITs bypass `enqueue`/`requestDrainAfterCommit` and hand-insert rows then call `drain()` directly, so the `@TransactionalEventListener(AFTER_COMMIT)` path — the most load-bearing piece of AC1 — has zero coverage; there is no forced-failure → `attempts++` → successful re-drive IT; `OutboxChunkProcessorTest` mocks the repository so `@Lock(PESSIMISTIC_WRITE)` + `lock.timeout=-2` (SKIP LOCKED) is untested; AC2's "refund throws once then succeeds" and AC3's "regression IT per listener" do not exist [`RefundOutboxIT.java:50-68`, `SluSnapshotOutboxIT.java:109-115`]
- [x] [Review][Patch] Both outbox ITs run `DELETE FROM main.outbox_messages` in `setUp()` — an unscoped truncation of a table the whole application writes to, in a suite sharing one Spring context and one database; `assertThat(outboxRepository.count()).isZero()` likewise asserts on global state [`RefundOutboxIT.java`, `SluSnapshotOutboxIT.java`]
- [x] [Review][Patch] `AdminQueueIT.withUnknownAlertTypeRow` drops `admin_alerts_type_check` on the shared test database and restores it in `finally` from a hardcoded `TYPE_CHECK_WIDE` copy rather than the migration's definition; if the copy drifts, or the body dies hard, every subsequent IT runs against a different schema [`AdminQueueIT.java`]
- [x] [Review][Patch] V125 creates a unique index over existing data with no de-duplication pre-check, resting entirely on "parent_credit_ledger is empty at deploy". Any staging/dev/demo database holding one pre-existing duplicate — most plausibly a booking refunded once at cancellation and again via dispute, i.e. exactly what the index exists to prevent — fails the migration and leaves Flyway's schema history in a failed state [`V125__parent_credit_ledger_reference_dedup.sql:16`]

#### Deferred

- [x] [Review][Defer] `AppEndpointsConventionTest` may validate a different matcher than production — the test evaluates patterns with `PathPatternRequestMatcher`, while `SecurityConfiguration.java:231` passes raw strings to `requestMatchers(String...)`. Under `PathPattern` semantics the *old* patterns (`resend-otp**`) are not even valid, which implies production was matching with Ant semantics — so AC15's fix is correct, but the test's guarantee does not necessarily transfer. Worth confirming the matcher, not blocking [`AppEndpointsConventionTest.java`] — deferred, test-fidelity concern, does not affect the shipped fix
- [x] [Review][Defer] The email `data` map is now round-tripped through JSON (`Map<String,Object>` → JSON → `Map<String,Object>`), which is type-lossy for temporal/decimal values. Verified non-issue today: all 69 `data.put(...)` call sites across both listeners put a `String` or a `List<String>`. Latent — the first typed value added to a template will silently degrade with no test to catch it [`NotificationOutboxSupport.java:45`] — deferred, latent fragility, not a current defect

<!-- Dismissed as noise (2): (a) "narrowing /v1/account/activate/** to /v1/account/activate breaks
     activation links" — false, AccountResource.java:91 is @PostMapping("/activate") with no path
     variable; (b) missing @SafeVarargs on AppEndpointsConventionTest.concat — pom.xml sets no
     -Xlint/-Werror, so it is a warning, not a build failure.
     Note: the Acceptance Auditor scored AC9 (de-DE) "Implemented" with a 0-hit pronoun regex; the
     Edge Case Hunter's imperative-verb check found the two survivors now listed under Patches. -->

## Dev Notes

### Scope discipline

This story is large by explicit project-owner direction. It is **not** open-ended: the 20 ACs are the scope. AC5 Task 0 is the one place new scope can appear — anything it identifies beyond "CAPTURE_PENDING timeout + a completion-gated payout hook" is filed to `deferred-work.md`, not built here.

### The outbox is a generalization, not a rewrite

`skillars-deferred-90`'s `PendingBlobDeletionService` is the reference implementation and it already survived a 3-layer adversarial review that forced the final shape (separate `ChunkProcessor` bean so `@Transactional(REQUIRES_NEW)` goes through the proxy; `FOR UPDATE SKIP LOCKED`; `drain()` loops to empty; sweeper; `app.scheduling.enabled` test gate). **Copy that shape.** The generic version adds only: a discriminator column + a handler-dispatch SPI so more than one domain can share the table. Consider whether `PendingBlobDeletionService` itself should be re-expressed on top of the generic outbox (nice-to-have, not required — do not destabilise a just-shipped, just-reviewed component without a reason).

### AFTER_COMMIT call-site inventory (filled in during Task 0)

Grep `@TransactionalEventListener` across `src/main` (dev-story pass): `CancellationRefundService` x5 (`onBookingCancelledByParent`/`ByCoach`/`ByAdmin`, `onCoachNoShow`, `onPlayerNoShow` — each `@Transactional(REQUIRES_NEW)`), `PaymentLifecycleService` x2, `BookingEmailListener` x18, `SessionPackEmailListener` x3, `MailManager.sendEmailFromTemplate` (the one that actually sends, `fallbackExecution=false`), `AccountDeletionCascadeListener.onAccountDeleted` (video — AC13, IS `@TransactionalEventListener` after all, contra the story draft), plus SSE / video / development / admin / reviews / session listeners not in scope. `SluPersistenceDispatcher.dispatchSluPersistence` is `@Async` (not a `@TransactionalEventListener`); the SLU under-report is in `SnapshotPersistenceRetrier`'s `@Recover` (AC4).

### AC2 disposition (Task 0 finding — held for owner)

AC2's premise ("Every `@TransactionalEventListener(AFTER_COMMIT)` that issues a Stripe refund") has **zero matching call sites**:

- `CancellationRefundService`'s 4 refund listeners call `creditWalletService.writeLedgerEntry(parentId, amount, "BOOKING_REFUND", bookingId, …)` — an **in-app `parent_credit_ledger` INSERT**, inside `@Transactional(REQUIRES_NEW)`. Not a Stripe call.
- The only `paymentGateway.refund(stripePaymentIntentId, netAmount)` sites are `CashOutService:52` and `SessionPackPaymentService:90` — both **synchronous** compensating paths inside `catch` blocks, not listeners.

The genuine money-loss risk here: a post-commit `writeLedgerEntry` failure is lost with only a log line (the `REQUIRES_NEW` tx rolls back, no re-drive). Wiring the credit-wallet `BOOKING_REFUND` path onto the AC1 outbox closes that — **but** `writeLedgerEntry` is **not idempotent** (every call INSERTs a new `parent_credit_ledger` row; PK is `tx_id`; no `uq (reference_id, type)`), so a re-drive would **double-credit** the parent. Making it idempotent needs a V-migration (partial unique index on `(reference_id, type) WHERE reference_id IS NOT NULL`) + a `DataIntegrityViolationException` catch in the handler — a money-path change to fold into the AC5 owner conversation. **Owner decision: wire the credit-wallet refund path onto the outbox (with the idempotency guard), or accept the `REQUIRES_NEW`-rollback-no-retry status quo for `BOOKING_REFUND` and file to AC20?**

### AC11 measurement (recorded)

`CoachProfileService.getPublicProfile` issues **exactly 8** JDBC round-trips: `coachProfileRepository.findById` (PK) + `coachSpecialtyRepository`/`coachAgeGroupRepository`/`coachPricingRepository`/`sessionPackRepository`/`coachAvailabilityWindowRepository`/`coachMediaItemRepository` `…ByCoachId` (each index-covered on `coach_id`) + `coachReliabilityStrikeRepository.countByCoachIdAndCreatedAtAfter`. `coachCapabilityService.getActiveBadges` returns `List.of()` — no query. Proven **O(1) in profile content** by `CoachPublicProfileQueryCountIT` (doubling every collection → still 8). Not batched: it is not an N+1, the endpoint is a single page view, and a `JOIN FETCH` collapse across 6 `@OneToMany` collections risks `MultipleBagFetchException` / cartesian blow-up for no measurable gain. → AC20 "leave as-is, measured".

### AC8 disposition table

Full per-migration table is in `docs/deployment/migration-conventions.md` § Grandfathering → "Per-migration disposition". Summary: **all six documented-safe, left as-is** — no production data, all applied & immutable, a redo yields a byte-identical end state; `V89`/`V97` are catalog-only DDL (no scan), `V98`'s `ADD COLUMN DEFAULT` is metadata-only (PG11+), the rest re-add already-valid constraints. Convention + lint bind `V122+`.

### Migration conventions

Every new migration in this story is `V123` or higher and MUST pass `MigrationConventionLintTest` and follow `docs/deployment/migration-conventions.md` (expand/contract; additive first; guarded `DROP` last; FK/CHECK `NOT VALID` then `VALIDATE` later; `CREATE INDEX CONCURRENTLY` on hot tables; batched backfills). The outbox table (AC1) is a plain additive `CREATE TABLE`. AC8's follow-ups are the online-safe re-do migrations — they are the convention's own worked examples.

### i18n

`MessageBundleParityTest` (deferred-90 AC12) is the guard: key-set parity + `{placeholder}` / `|` pluralization-arity parity for DE and FR against EN. AC9/AC10 change **values only** — if the parity test breaks, a key was accidentally touched. The 4 machine-format exclusion sites from deferred-90 AC11 (`WeeklyCalendar.vue` `'en-CA'`/`'en'` geometry, `AvailabilityManagerPage.vue` / `BookingRequestPage.vue` `'en-CA'` offsets, `CoachCommandCenterPage.getDayIndex`) carry no user-facing text — do not touch.

### Testing traps carried from prior stories

- `@SchedulerLock`-annotated methods invoked from a test go through the proxy → ShedLock applies → with `lockAtLeastFor` a second invocation in the same test class is silently skipped. Use `BasePaymentIT.releaseSchedulerLock(name)` / the equivalent before each invocation. (`skillars-deferred-15` lesson.)
- A concurrency IT that "proves" a lock frequently passes without it because the two threads' pre-lock work isn't symmetric (`skillars-deferred-13` / `-15` / `uat-3` D11, and deferred-90's SLU IT). If AC2/AC4 need a serialisation proof, scope the block probe to a known backend PID via `pg_blocking_pids()` (deferred-90's reworked `SluCalculationServiceIT` is the pattern) rather than a fuzzy `pg_stat_activity` query-text match.
- New IT context-forking: `IntegrationTestConventionTest` pins the `@TestPropertySource`-carrying-IT count. `hibernate.generate_statistics` for AC11 needs a property — either fold the measurement into an existing statistics-enabled context, or accept the pinned-count bump with a justification comment (deferred-90 chose to keep O(1) proof at the unit level for this reason).

### Project Structure Notes

- Backend module hierarchy `com.softropic.skillars.platform.{module}.{api|service|repo|contract|config}`. A generic outbox is infrastructure-ish but it carries domain payloads and a domain-lifecycle scheduler → per `project-context.md` § "Schedulers belong to platform", it lives in `platform.{module}` (or a new `platform.outbox`), **not** `infrastructure`. The `PendingBlobDeletionService` precedent is `platform.filestorage.service` — follow it.
- All request/response DTOs are Java `record`s; MapStruct for entity/DTO; `@PreAuthorize` + `SecurityConstants` on every resource method; Jakarta Validation on request records. Frontend: `<script setup>`, `async/await`, centralized `*.api.js`, all user-facing text via `vue-i18n`.

### References

- [Source: `_bmad-output/implementation-artifacts/deferred-work.md`] — lines 612 (`10-2` D1), 716/862 (`3-6` W3 / `3-10` D2), 941 (`deferred-15` D1), 886 (`6-3` W3), 1288/1290 (`deferred-89` SLU + perf-trend), 990/1010/1012 (`uat-3` D3/D13/D14), 1189 (`deferred-63` escrow), 947 (`deferred-16` D4), 1329 (deferred-90 lint blind spots), 894 (`6-6` W3 V60), 927 (`11-3` V89), 1000 (`uat-3` V94), 1065 (`deferred-33` V97), 1077 (`deferred-40` V98), 1258 (`deferred-84` V117), 1353 (deferred-90 de-DE register), 734/1326 (`2-3` `getPublicProfile`), 1347 (`ErrorLog` `%`), 1355 (GDPR cascade), 1351 (cross-tab `rint`), 1298 (`resend-otp**`), 1297 (`resendPhoneOtp` triplication), 812 (`1-1` 409), 1252 (`deferred-83` jwt-secret).
- [Source: `_bmad-output/implementation-artifacts/skillars-deferred-90-…-n-plus-1-query-batching.md`] — `PendingBlobDeletionService` outbox shape, `MigrationConventionLintTest`, `MessageBundleParityTest`, `MessagingConversationSummaryBatchingIT` (no-fork IT pattern), reworked `SluCalculationServiceIT` (PID-scoped lock probe).
- [Source: `docs/deployment/migration-conventions.md`] — expand/contract standard, grandfather list, "What the guard cannot catch".
- [Source: `_bmad-output/project-context.md`] — module structure, "Schedulers belong to platform", `@TransactionalEventListener` gotchas, rolling-deploy migration rules for `V > 121`.
- [Source: `src/main/java/com/softropic/skillars/platform/filestorage/service/PendingBlobDeletionService.java`, `PendingBlobDeletionChunkProcessor.java`, `repo/PendingBlobDeletionRepository.java`, `src/main/resources/db/migration/V122__pending_blob_deletions.sql`]

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (bmad-dev-story workflow)

### Debug Log References

- AC13 reproduction: `mvn -o test -DskipFrontend -Dtest=GdprErasureIT` — `InvalidDataAccessApiUsageException: Executing an update/delete query` → `jakarta.persistence.TransactionRequiredException` at `VideoDeletionService.cascadeDeleteForAccount:186` (`$Proxy385.resetBytesForOwner`). Fixed with `REQUIRES_NEW` boundaries; re-run green (`erase_playerUser_videoCascade_purgesVideos_resetsQuota_cancelsPendingApprovals`).
- AC11 measurement: `CoachPublicProfileQueryCountIT` — `getPublicProfile` issues **exactly 8** JDBC statements (Hibernate `Statistics.getPrepareStatementCount()`), **constant** when specialties/packs/windows/media are doubled.

### Completion Notes List

**Done and verified (14 of 20 ACs):**

- **AC6** — `AdminQueueService` reads `admin_alerts` rows as raw strings (`findOpenRawByType` / `countOpenByTypeRaw`); an unmappable `alert_type` is skipped with `[ADMIN_QUEUE_UNKNOWN_ALERT_TYPE]` WARN, the page renders 200 with every mappable row. Audit: the two other `*.valueOf` sites in `platform.admin` are on request params (already try/caught → 400), not DB strings. `AdminQueueIT` +2.
- **AC7** — `MigrationLint` gains `BACKPORT_BELOW_BASELINE` (best-effort `git cat-file -e HEAD:` check, silent when git can't answer), `REPEATABLE_HAZARD` (`R__` scanned for DROP / blocking index / validating constraint), `INLINE_FK_ADD_COLUMN` (`ADD COLUMN … REFERENCES x(y)` without `NOT VALID`). Fixtures added; `migration-conventions.md` "What the guard cannot catch" → "now covers" + residual list. `MigrationConventionLintTest` green (4).
- **AC8** — per-migration disposition table added to `docs/deployment/migration-conventions.md` § Grandfathering. **Documented-safe, left as-is for all six**: no production system exists, all are applied & immutable, and a retroactive online-safe redo would produce a byte-identical end state whose only runtime effect is overhead on fresh installs. `V89`/`V97` are catalog-only (`DROP TABLE`/`DROP COLUMN`, no scan); `V98` `ADD COLUMN DEFAULT` is metadata-only since PG11 (the backfill `UPDATE` is the at-scale note, not `ACCESS EXCLUSIVE`); `V60`/`V94`/`V117` re-add already-valid constraints. Convention binds `V122+`.
- **AC9** — `src/frontend/src/i18n/de-DE/index.js`: **54 replacement groups** applied (informal `du`/`dein…`/`dich`/`dir` + informal imperatives `Gib`/`Klicke`/`Verwalte`/`Füge…hinzu`/`Lass`/`Prüfe`/`wähle`/etc. → formal `Sie`/`Ihr…`/`Geben Sie`/…). 0 informal forms remain. Frontend bundle parity check: de-DE keeps 1022 keys, 0 missing, 0 extra, 0 `{placeholder}` mismatches vs en-US. `MessageBundleParityTest` (backend bundles) still green.
- **AC10** — `fr-FR/index.js`: phrase-level `a`→`à` / `active`→`activé` / `expire`→`expiré` fixes + word-boundary accent restoration (`deja`→`déjà`, `verification`→`vérification`, `succes`→`succès`, `telephone`→`téléphone`, `Numero`→`Numéro`, `reessayer`→`réessayer`, `Etat`→`État`, …). `messages_fr.properties` was already clean (deferred-90). fr-FR keeps 1022 keys / 0 placeholder mismatches.
- **AC11** — measured (see Debug Log): 8 fixed index-covered reads, O(1) in profile content. **Decision: leave as-is (measured)** — not an N+1 (`> 4` rule targets a per-row loop; this is a constant), endpoint is once-per-page-view, a `JOIN FETCH` collapse across 6 `@OneToMany` collections risks `MultipleBagFetchException` / cartesian blow-up for no measurable gain. `CoachPublicProfileQueryCountIT` stays as the regression guard. Filed to AC20.
- **AC12** — `ErrorLog.logError` / `logExpected` now parameterise the template (`log.error("{} SUPPORT_ID: {}", msgTemplate, helpCode, …)`) instead of `String.format`; a `%` (or `{}`) in `msgTemplate` is inert. `ErrorLogTest` +5.
- **AC13** — root cause: on the AFTER_COMMIT account-deletion cascade a plain `@Transactional` (REQUIRED) sub-call does not get a usable tx during the erasure tx's `afterCommit` callback — the direct `@Modifying` repo call threw `TransactionRequiredException` (quota silently never reset), and an inner REQUIRED tx that never committed deadlocked the follow-up approval-cancel query. Fix: `VideoDeletionService.cascadeDeleteForAccount` routes every write (`purgeVideoForCascade`, `resetQuotaForOwner`, `cancelPendingApprovalsForOwners`) through `@Transactional(propagation = REQUIRES_NEW)` via the existing `self` proxy — same shape as `PendingBlobDeletionChunkProcessor`. `GdprErasureIT` +1 (video PURGED + quota 0 + approval CANCELLED); `AccountDeletionCascadeIT` 8/8 still green.
- **AC14** — `useSession.js` `handleLogout` clears the non-HttpOnly `rint` cookie client-side, so a sibling tab's next `tick()` enters the fast-teardown branch regardless of whether the backend `/logout` call stalls. ESLint + `quasar build` green.
- **AC15** — `AppEndpoints.PUBLIC_ENDPOINTS` bare `foo**` patterns → exact paths (none of these endpoints has sub-paths); genuine prefix matchers keep `/`-preceded `/**`. `AppEndpointsConventionTest` +4 (incl. a `PathPatternRequestMatcher` spot-check: `resend-otp` matches, `resend-otp-admin` does not). No currently-public endpoint's path becomes unmatched (register/verify-email/verify-phone/resend-verification/resend-otp for coach/parent/player, `/v1/account/{register,regislink,activate}`, `/api/auth/{login,refresh,logout}`, `/api/marketplace/coaches` list — all exact `@…Mapping` paths).
- **AC16** — `RegistrationOtpResendSupport` (`platform.security.service`) holds the shared `resendPhoneOtp` body; the three services keep their `@RateLimited(key="{role}_resend_otp")` annotation on a one-line delegate parameterised by the per-user bucket key + an `OtpEmailEventFactory` (`{Role}OtpEmailEvent::new`). "Mirrors parent exactly" comments removed. `CoachRegistrationResourceIT` (20) / `PlayerRegistrationResourceIT` (11) / `ParentRegistrationResourceIT` (17) green.
- **AC17** — `ApiAdvice.illegalStateExceptionHandler` → `500 INTERNAL_SERVER_ERROR` / `generic.unknown` (was 409 / `generic.conflict`). Audit of every request-reachable `throw new IllegalStateException` (`ConfigService`, `SecurityUtil`, `MailManager`, `QuotaService`, video approval/lifecycle, `PessimisticLockRetryer`, SHA-256-unavailable): all are server-side invariants or missing/invalid `platform_config` — none legitimately means 409. `ApiAdviceTest` +1.
- **AC18** — `JwtSecretBootstrapRunner` gains an enforced local-only guard: when `app.bootstrap.jwt-secret.enabled=true`, `spring.datasource.url` must target `localhost`/`127.0.0.1`/`[::1]` or the runner throws `IllegalStateException` and fails the boot. Signal chosen: the datasource host (a datasource pointed at a real DB does not travel when settings are copied, unlike a flag pair). `application-dev.yaml` inherits the base `jdbc:postgresql://localhost/skillars` URL → dev still boots. `JwtSecretBootstrapRunnerTest` +3.
- **AC19** — `MessagingService.buildSummaryContext` stays direct (documented why: `PlayerProfileService.getPlayerNamesByPlayerIds`'s `Collectors.toMap` NPEs on a null name — the exact degradation the deferred-90 3-layer review removed from this path). The other 5 `PlayerProfileRepository` call sites are `findById` (full entity) / `findByUserId` / `findByIdAndParentId` (authz), not "id → name" — each already carries a purpose comment.
- **AC1** — see Task 1.  **AC4** — see Task 4.

**Phase 2 (after owner review, decisions D1–D5):**

- **AC5 Part A** — `BookingPaymentStatus.CAPTURE_ABANDONED` (terminal); `V124` widens `chk_bp_status` (with a documented `migration-lint: allow-validating-constraint` opt-out per D2 — empty table, single-node), adds nullable `reserved_at`, widens `status` to `VARCHAR(20)` (`CAPTURE_ABANDONED` is 17 chars); `reserveCapture` stamps `reserved_at`; `PaymentPendingSweeper.sweepOne` ages a `CAPTURE_PENDING` row past `booking.payment_pending.capture_pending_max_hours` (default 72h per D3, bounded 6–720) → `CAPTURE_ABANDONED` + `PAYMENT_FAILED` transition (slot freed, parent cancel unblocked) + `booking.payment_pending.unrecoverable{reason="CAPTURE_TIMEOUT"}` + `[CAPTURE_TIMEOUT]` ERROR. No auto-charge. A null `reserved_at` row (pre-V124) is never aged. `runbook.md` § CAPTURE_ABANDONED updated. `PaymentPendingSweeperTest` +3, `PaymentPendingSweeperIT` +1.
- **AC5 Part B** — filed to AC20 / `deferred-work.md` as its own story (owner D4). Design doc `docs/architecture/payout-and-capture-pending.md` is the input.
- **AC2** — `V125__parent_credit_ledger_reference_dedup.sql` (`uq_pcl_reference_type` partial unique on `(reference_id, type) WHERE reference_id IS NOT NULL`, with a documented `migration-lint: allow-blocking-index` opt-out mirroring V121); `RefundOutboxSupport.enqueueBookingRefund(...)` replaces all 6 `writeLedgerEntry("BOOKING_REFUND", …)` calls across `CancellationRefundService`'s 4 listeners; `CreditWalletRefundOutboxHandler` (`CREDIT_WALLET_REFUND`) re-drives idempotently — existence-check first (`ledgerRepository.existsByReferenceIdAndType`), the unique index backstops the concurrent case. `RefundOutboxIT` proves exactly-once + no-op re-drive.
- **AC3** — `NotificationOutboxSupport.enqueueEmail(...)` (`@Transactional(REQUIRES_NEW)`) + `NotificationEmailOutboxHandler` (`NOTIFICATION_EMAIL` → `MailManager.sendEmailSync`, which actually sends). Every `publisher.publishEvent(new Envelope(...))` in `BookingEmailListener` (19) + `SessionPackEmailListener` (3) now enqueues, closing the silent-drop (`MailManager`'s `fallbackExecution=false` AFTER_COMMIT listener never fired for an `Envelope` published from a no-tx AFTER_COMMIT context). Dedupe stamps untouched; a re-drive re-sends (acceptable per AC3). `BookingEmailListenerTest` + `SessionPackEmailListenerTest` rewritten to the seam; `SessionPackExpiryWarningIT` / `MailManagerIT` / booking-flow ITs green.
- **AC20** — `deferred-work.md`: deleted the closed items (AC12 ErrorLog `%`, AC13 GDPR cascade, AC14 cross-tab `rint`, AC15 `resend-otp**`, AC16 `resendPhoneOtp` triplication, AC17 `IllegalStateException` 409, AC18 jwt-secret guard, AC6 `AdminAlertType` D4, AC4 SLU under-report, deferred-90 de-DE register + lint blind spots + `getPublicProfile`); collapsed the deferred-63 escrow bullet into a pointer; added `## Deferred from: skillars-deferred-91 story creation and implementation (2026-09-03)` with the residuals (Part B as its own story, de-DE native-speaker review still owed, `getPublicProfile` leave-as-is-measured, SLU perf-trend signal, frontend-test-framework gap, the deliberately-un-migrated AFTER_COMMIT listeners, the `uq_pcl_reference_type` one-refund-per-booking assumption). `sprint-status.yaml` flipped to `review`.

**Frontend gate (Task 21):** ESLint clean on `useSession.js` / `de-DE/index.js` / `fr-FR/index.js`; `quasar build` succeeds; frontend bundle parity check 1022/1022 keys, 0 `{placeholder}` drift for de + fr. `prettier --check` `[warn]`s on all three touched files — **verified pre-existing** (each `[warn]`s at `HEAD` too, unchanged). Not widened: `prettier --write` on the i18n bundles would be a large unrelated reformat.

### File List

**New — main:**
- `src/main/resources/db/migration/V123__outbox_messages.sql`
- `src/main/resources/db/migration/V124__booking_payment_capture_abandoned.sql` (AC5 Part A)
- `src/main/resources/db/migration/V125__parent_credit_ledger_reference_dedup.sql` (AC2)
- `src/main/java/com/softropic/skillars/platform/outbox/repo/OutboxMessage.java`
- `src/main/java/com/softropic/skillars/platform/outbox/repo/OutboxMessageRepository.java`
- `src/main/java/com/softropic/skillars/platform/outbox/contract/OutboxMessageHandler.java`
- `src/main/java/com/softropic/skillars/platform/outbox/contract/event/OutboxDrainRequestedEvent.java`
- `src/main/java/com/softropic/skillars/platform/outbox/service/OutboxService.java`
- `src/main/java/com/softropic/skillars/platform/outbox/service/OutboxChunkProcessor.java`
- `src/main/java/com/softropic/skillars/platform/security/service/RegistrationOtpResendSupport.java`
- `src/main/java/com/softropic/skillars/platform/development/service/SluSnapshotOutboxSupport.java`
- `src/main/java/com/softropic/skillars/platform/development/service/SluSnapshotOutboxHandler.java`
- `src/main/java/com/softropic/skillars/platform/payment/service/RefundOutboxSupport.java` (AC2)
- `src/main/java/com/softropic/skillars/platform/payment/service/CreditWalletRefundOutboxHandler.java` (AC2)
- `src/main/java/com/softropic/skillars/platform/notification/service/NotificationOutboxSupport.java` (AC3)
- `src/main/java/com/softropic/skillars/platform/notification/service/NotificationEmailOutboxHandler.java` (AC3)

**New — test:**
- `src/test/java/com/softropic/skillars/infrastructure/message/ErrorLogTest.java`
- `src/test/java/com/softropic/skillars/platform/security/config/AppEndpointsConventionTest.java`
- `src/test/java/com/softropic/skillars/platform/outbox/service/OutboxChunkProcessorTest.java`
- `src/test/java/com/softropic/skillars/platform/outbox/service/OutboxServiceTest.java`
- `src/test/java/com/softropic/skillars/platform/outbox/SluSnapshotOutboxIT.java`
- `src/test/java/com/softropic/skillars/platform/marketplace/api/CoachPublicProfileQueryCountIT.java`
- `src/test/resources/migration-lint/invalid/{R__bad_repeatable,V50__backported_hazard,V906__inline_fk_add_column}.sql`
- `src/test/resources/migration-lint/valid/{R__safe_repeatable,V807__inline_fk_column_then_constraint}.sql`

**New — docs:**
- `docs/architecture/payout-and-capture-pending.md`

**Modified — main:**
- `src/main/java/com/softropic/skillars/platform/payment/service/CancellationRefundService.java` (AC2 — 6 refund calls → outbox)
- `src/main/java/com/softropic/skillars/platform/payment/repo/ParentCreditLedgerRepository.java` (AC2)
- `src/main/java/com/softropic/skillars/platform/payment/contract/BookingPaymentStatus.java` (AC5 Part A — CAPTURE_ABANDONED)
- `src/main/java/com/softropic/skillars/platform/payment/repo/BookingPayment.java` (AC5 Part A — reservedAt, status length 20)
- `src/main/java/com/softropic/skillars/platform/payment/service/BookingPaymentPersistenceService.java` (AC5 Part A)
- `src/main/java/com/softropic/skillars/platform/payment/service/PaymentPendingSweeper.java` (AC5 Part A — CAPTURE_PENDING timeout branch)
- `src/main/java/com/softropic/skillars/platform/notification/infrastructure/listener/BookingEmailListener.java` (AC3 — 19 sites → outbox)
- `src/main/java/com/softropic/skillars/platform/notification/infrastructure/listener/SessionPackEmailListener.java` (AC3 — 3 sites → outbox)
- `src/main/java/com/softropic/skillars/infrastructure/message/ErrorLog.java` (AC12)
- `src/main/java/com/softropic/skillars/platform/security/api/ApiAdvice.java` (AC17)
- `src/main/java/com/softropic/skillars/platform/security/config/AppEndpoints.java` (AC15)
- `src/main/java/com/softropic/skillars/platform/security/service/{Parent,Coach,Player}RegistrationService.java` (AC16)
- `src/main/java/com/softropic/skillars/platform/security/service/JwtSecretBootstrapRunner.java` (AC18)
- `src/main/java/com/softropic/skillars/platform/messaging/service/MessagingService.java` (AC19)
- `src/main/java/com/softropic/skillars/platform/video/service/VideoDeletionService.java` (AC13)
- `src/main/java/com/softropic/skillars/platform/video/service/AccountDeletionCascadeListener.java` (AC13)
- `src/main/java/com/softropic/skillars/platform/admin/service/AdminQueueService.java` (AC6)
- `src/main/java/com/softropic/skillars/platform/admin/repo/AdminAlertRepository.java` (AC6)
- `src/main/java/com/softropic/skillars/platform/development/repo/SnapshotBatchWriter.java` (AC4)
- `src/main/java/com/softropic/skillars/platform/development/service/SnapshotPersistenceRetrier.java` (AC4)
- `src/frontend/src/composables/useSession.js` (AC14)
- `src/frontend/src/i18n/de-DE/index.js` (AC9)
- `src/frontend/src/i18n/fr-FR/index.js` (AC10)

**New — test (phase 2):**
- `src/test/java/com/softropic/skillars/platform/outbox/RefundOutboxIT.java` (AC2)

**Modified — test:**
- `src/test/java/com/softropic/skillars/platform/payment/service/CancellationRefundMatrixTest.java` (AC2)
- `src/test/java/com/softropic/skillars/platform/payment/service/PaymentPendingSweeperTest.java` (AC5 Part A, +3)
- `src/test/java/com/softropic/skillars/platform/payment/service/PaymentPendingSweeperIT.java` (AC5 Part A, +1)
- `src/test/java/com/softropic/skillars/platform/notification/infrastructure/listener/BookingEmailListenerTest.java` (AC3)
- `src/test/java/com/softropic/skillars/platform/notification/infrastructure/listener/SessionPackEmailListenerTest.java` (AC3)
- `src/test/java/com/softropic/skillars/platform/security/api/ApiAdviceTest.java` (AC17)
- `src/test/java/com/softropic/skillars/platform/security/service/JwtSecretBootstrapRunnerTest.java` (AC18)
- `src/test/java/com/softropic/skillars/db/MigrationLint.java` (AC7)
- `src/test/java/com/softropic/skillars/db/MigrationConventionLintTest.java` (AC7)
- `src/test/java/com/softropic/skillars/platform/admin/api/AdminQueueIT.java` (AC6)
- `src/test/java/com/softropic/skillars/platform/admin/api/GdprErasureIT.java` (AC13)
- `src/test/java/com/softropic/skillars/platform/development/service/SnapshotPersistenceRetrierTest.java` (AC4)
- `src/test/java/com/softropic/skillars/platform/development/service/SluRetrierProxyRetryTest.java` (AC4)

**Modified — docs:**
- `docs/deployment/migration-conventions.md` (AC7 + AC8)

## Change Log

| Date | Version | Change |
| --- | --- | --- |
| 2026-09-03 | v0.1 | Story created via story-creation process. 20 ACs. Four project-owner "full-scope" decisions folded in (outbox generalization, escrow design pass, de-DE `Sie` rewrite, all 3 rolling-deploy items). Anchor one-off bugs AC12–AC19. Standing decisions recorded (startSessionMonitoring, frontend test framework). Status → ready-for-dev. |
| 2026-09-03 | v0.2 | dev-story implementation, first pass. **14 ACs done & verified**: AC1 (generic `platform.outbox` module + V123), AC4 (SLU snapshot re-drive via outbox), AC6–AC12, AC13–AC19. **HALT at the AC5 owner-review gate** with `docs/architecture/payout-and-capture-pending.md` delivered. AC2 found to rest on a false premise (no Stripe-refund AFTER_COMMIT listener exists) → held with AC3 (fix designed) and AC5 for the owner conversation. AC20 pending. Targeted `-Dtest` green per touched area; ESLint + `quasar build` green; no `mvn verify` (validation-strategy). |
| 2026-09-03 | v0.3 | Owner review taken verbally on scope; the written D1–D5 record in `payout-and-capture-pending.md` remains **open** (corrected by the 2026-09-03 code review, D8). **AC5 Part A** implemented (`CAPTURE_ABANDONED` + V124 + `PaymentPendingSweeper` timeout + `CAPTURE_TIMEOUT` metric + runbook); **Part B** filed to AC20 as its own story (Stripe Connect destination-charge → separate-charges-&-transfers change). **AC2** implemented per owner (credit-wallet `BOOKING_REFUND` × 4 listeners → outbox; V125 `uq_pcl_reference_type`; idempotent handler). **AC3** implemented per owner at wider scope (all 19 `BookingEmailListener` + 3 `SessionPackEmailListener` sites → outbox). **Correction (code review D5):** the enqueue happens in a `REQUIRES_NEW` transaction *after* the producing transaction has already committed (the callers are themselves `AFTER_COMMIT` listeners), so this closes the nested-`AFTER_COMMIT` silent drop but does **not** make the email atomic with the business transaction as AC1 requires. The residual window is filed to `deferred-work.md`. **AC20** done (`deferred-work.md` closures + skillars-deferred-91 residuals section). All 20 ACs satisfied. Status → **review**. Targeted `-Dtest` green (incl. `PaymentPendingSweeperIT`, `RefundOutboxIT`, `SluSnapshotOutboxIT`, `MigrationConventionLintTest`, `IntegrationTestConventionTest`, booking-flow ITs); ESLint + `quasar build` green; no `mvn verify` ([[feedback_no_local_mvn_verify]]) — GitHub CI is the full-suite gate. |
| 2026-09-03 | v0.4 | **Code review (`/bmad-code-review`, 3 layers: Blind Hunter, Edge Case Hunter, Acceptance Auditor — all completed).** 12 decision-needed + 16 patch + 2 deferred + 2 dismissed; all 12 decisions resolved by the project owner and all 28 resulting patches applied. Headline defects fixed: (a) **AC3's durability guarantee did not exist** — `MailManager.sendEmailSync` never rethrows, so every failed email was deleted from the outbox as if delivered; (b) **`OutboxChunkProcessor` poisoned its own chunk** — the two `@Transactional`-REQUIRED handlers joined the chunk transaction, so a `DataIntegrityViolationException` made the `attempts++` bookkeeping impossible and rolled back 24 siblings; (c) the AFTER_COMMIT drain ran **inline on the request thread** for up to 5,000 rows of blocking I/O; (d) **`BACKPORT_BELOW_BASELINE` was inert** (`git cat-file -e` exits 128 for the exact case it targets). Restructured to one row per transaction (`OutboxRowProcessor`) + separate-transaction failure bookkeeping + `@Async("outboxDrainPool")` + `next_attempt_at` exponential backoff (**V126**); `uq_pcl_reference_type` narrowed to `BOOKING_REFUND` with a dedup pre-check and an `ApiAdvice` mapping (**V127**); `getPublicProfile` collapsed 8 → 4 round trips per AC11; `CAPTURE_ABANDONED` given its own `BOOKING_PAYMENT_UNRESOLVED` copy instead of "booking declined"; 3 request-reachable `IllegalStateException`s → 409 `VideoStateConflictException`; AC19 dedup completed by making the shared method null-tolerant; AC8 amended with an explicit pre-production grandfathering clause + a hard release gate in `runbook.md`; unsupported "owner reviewed D1–D5" claims corrected. `mvn test` green (**0 failures, 0 errors**); ESLint clean; `quasar build` green. **Integration tests NOT run locally** ([[feedback_no_local_mvn_verify]]) — 6 new/changed ITs are unexecuted; GitHub CI is the gate. Status → **done**. |
| 2026-09-04 | v0.5 | **CI red on PR #146 (run 33810296682) → 6 IT failures fixed.** 4 were mine: (1) making the drain `@Async` (D3) turned every IT that asserts on a post-commit outbox effect into a race — `PackCancellationRefundIT` failed. Fixed by binding `outboxDrainPool` to a `SyncTaskExecutor` under the test profile (`app.outbox.drain-async: false`), the same shape `SchedulingConfig` uses; production stays async and `OutboxServiceAsyncWiringTest` guards the annotation an IT can no longer see. (2) `RefundOutboxIT`'s re-drive step used a bare `jdbcTemplate.update` — this datasource runs `hikari.auto-commit: false`, so an unwrapped write is **never committed** while still reporting 1 row affected; wrapped in `transactionTemplate`. (3) `CoachPublicProfileQueryCountIT`'s new parity test hardcoded collection sizes that a sibling test's `seedChildRows(4)` changes; now compared against the database. (4) its SLU sibling used `queryForObject` for a precondition that throws on zero rows. 2 were **not** mine: `BookingBatchResourceIT` pinned the structured log context to argument index 0, which AC12's `ErrorLog` reparameterisation moved to index 2 (assertion now scans the whole argument array — the context itself never changed); and `RescheduleResourceIT.setBookingStatus` was the last `Instant.now()`-preserves-wall-clock site that `skillars-deferred-69` AC2 missed, so `duplicateNextWeek` failed with `SESSION_CROSSES_MIDNIGHT` on any CI run between ~23:00 and 24:00 Europe/Berlin — anchored to a safe local hour like its `safeProposedStart` sibling. All verified locally against Testcontainers before re-push. |
