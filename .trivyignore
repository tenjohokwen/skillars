# Temporary ignore — remove once Alpine publishes the patched openssl/libssl3/libcrypto3
# build (3.5.8-r0) to the apk repo the eclipse-temurin:17-jre-alpine base image pulls from.
#
# The Dockerfile already runs `apk upgrade --no-cache` specifically to pick up fixed OS
# packages before Trivy scans the built image (see the Dockerfile's own comment), but that
# only helps once the fix is actually published upstream. As of 2026-08-26, Trivy's vulnerability
# DB already lists a fix (3.5.8-r0) but Alpine 3.24's apk repo still only serves 3.5.7-r0 —
# confirmed via two identical CI failures on skillars-deferred-69's PR #107, ~10 minutes apart,
# both installed-version 3.5.7-r0. Added by skillars-deferred-70 story-creation with the project
# owner's explicit sign-off (2026-08-26), scoped to this one CVE only, not a blanket suppression.
#
# openssl: OpenSSL: Denial of Service via unbounded memory growth in QUIC server
CVE-2026-14456

# Temporary ignore — no fixed tomcat-embed-core has been published on ANY line.
#
# Trivy flags org.apache.tomcat.embed:tomcat-embed-core 10.1.55 (managed by the
# spring-boot 3.5.16 parent) for three CRITICAL CVEs and names the fixes as
# 10.1.58 / 11.0.25 / 9.0.121 — none of which exist on Maven Central yet (max
# published: 10.1.55 on the 10.1.x line, 11.0.22 on 11.0.x). A pin to 11.0.22 was
# tried on skillars-deferred-90's PR #145: `mvn -B verify` passed clean (full unit +
# IT suite, Tomcat 11 is runtime-compatible) but Trivy still failed because 11.0.22
# is also pre-fix. There is nothing to bump to. Staying on the boot-managed 10.1.55.
#
# This is the embedded servlet container, so the CVEs are reachable in principle;
# scoped to these three IDs only, not a blanket tomcat suppression. Added by
# skillars-deferred-90 with the project owner's explicit sign-off (2026-09-03).
# REMOVE the moment tomcat-embed-core 10.1.58+ ships (or the boot parent moves past
# it) — the pr-build Trivy scan will start passing without this once it does.
#
# tomcat: security-constraint bypass / auth-replay / improper resource access
CVE-2026-65182
CVE-2026-65905
CVE-2026-68525
